# 제2과제 추가 예상문제: Cloud Operations Automation

> **비공식 예상문제입니다.** 공식 과제지·채점표·공지와 이름이나 값이 다르면 반드시 공식 문서를 우선합니다.  
> 예상 리전: `eu-west-1` / 예상 배점: 7.5점 / 권장 연습시간: 60분

## 1. 출제 가능성 판단

최신 RC에서 기존 Cloud Event Handling은 서비스 범위가 아니라 문제지와 채점표의 불일치 때문에 삭제되었습니다. 따라서 오류를 바로잡은 운영 자동화 문제가 추가될 가능성을 1순위로 봅니다.

| 현재 RC 역량 | 추가 문제로 보완할 가능성이 높은 역량 |
|---|---|
| Workflow 데이터 처리 | AWS API 변경 이벤트 탐지 |
| Kinesis·Flink 분석 | 보안 위반 자동 복구 |
| MSK 이벤트 스트리밍 | 구성 규정 준수와 운영 알림 |

예상 핵심 서비스는 CloudTrail, EventBridge, AWS Config, Lambda, SNS입니다. 아래 이름과 배점은 연습용 예상값입니다.

| 번호 | 예상 채점 항목 | 배점 |
|---|---|---:|
| E-1 | VPC·EC2·Security Group | 1.0 |
| E-2 | CloudTrail·SNS | 1.0 |
| E-3 | Lambda 함수·Runtime·환경변수 | 1.5 |
| E-4 | EventBridge Rule·Target | 1.0 |
| E-5 | AWS Config Rule ACTIVE | 1.0 |
| E-6 | EC2 Stop 자동 복구 | 1.0 |
| E-7 | Public SSH 자동 제거 | 1.0 |

## 2. 예상문제 상세 풀이

### 2.1 목표

보안 또는 비용 관련 이벤트를 감지하면 Lambda가 자동 복구하거나 SNS 알림을 보냅니다. 채점 스크립트는 EC2 stop과 SG SSH open 상황을 먼저 만들어 놓고, 복구 결과를 확인합니다.

### 2.2 필수 리소스

| 항목 | 값 |
|---|---|
| Region | `eu-west-1` |
| VPC | `event-vpc`, `172.16.0.0/16` |
| EC2 | `wsc2026-event-ec2`, `t3.micro` |
| EC2 Role | `wsc2026-event-ec2-role` |
| Security Group | `wsc2026-event-sg` |
| CloudTrail | `wsc2026-event-trail` |
| SNS | `wsc2026-event-alert` |
| Lambda Role | `wsc2026-event-lambda-role` |
| Lambda Runtime | `python3.12` |
| Config Rules | `wsc2026-sg-ssh-rule`, `wsc2026-required-tags-rule` |

채점 스크립트가 확인하는 Lambda:

| Function | 역할 |
|---|---|
| `wsc2026-ec2-stop-remediation` | stopped EC2 재시작 |
| `wsc2026-ec2-terminate-alert` | terminated 알림 |
| `wsc2026-sg-remediation` | SSH 0.0.0.0/0 제거 |
| `wsc2026-tag-alert` | 태그 미준수 알림 |

### 2.3 SNS

```bash
aws configure set region eu-west-1
aws sns create-topic --name wsc2026-event-alert
```

모든 Lambda에 환경변수 `SNS_TOPIC_ARN`을 넣습니다.

### 2.4 Lambda TODO 완성

제공 코드의 핵심 TODO:

```python
def ec2_stop_handler(event, context):
    instance_id = os.environ.get("INSTANCE_ID")
    detail = event.get("detail", {})
    state = detail.get("state", "")

    if state != "stopped":
        return

    ec2_client.start_instances(InstanceIds=[instance_id])

    publish_alert(
        "EC2_STOPPED",
        f"EC2 instance {instance_id} was stopped and restarted",
        "RESTORED",
    )
```

```python
def ec2_terminate_handler(event, context):
    detail = event.get("detail", {})
    instance_id = detail.get("instance-id", "unknown")

    publish_alert(
        "EC2_TERMINATED",
        f"EC2 instance {instance_id} was terminated",
        "ALERT_ONLY",
    )
```

```python
def sg_remediation_handler(event, context):
    sg_id = os.environ.get("SECURITY_GROUP_ID")
    detail = event.get("detail", {})

    compliance = detail.get("newEvaluationResult", {}).get("complianceType", "")
    if compliance != "NON_COMPLIANT":
        return

    try:
        ec2_client.revoke_security_group_ingress(
            GroupId=sg_id,
            IpPermissions=[
                {
                    "IpProtocol": "tcp",
                    "FromPort": 22,
                    "ToPort": 22,
                    "IpRanges": [{"CidrIp": "0.0.0.0/0"}],
                }
            ],
        )
    except Exception:
        pass

    publish_alert(
        "SG_SSH_OPEN",
        f"Unauthorized SSH rule removed from {sg_id}",
        "RESTORED",
    )
```

```python
def tag_alert_handler(event, context):
    detail = event.get("detail", {})
    resource_id = detail.get("resourceId", "unknown")

    compliance = detail.get("newEvaluationResult", {}).get("complianceType", "")
    if compliance != "NON_COMPLIANT":
        return

    publish_alert(
        "TAG_MISSING",
        f"Required tag missing on {resource_id}",
        "ALERT_ONLY",
    )
```

각 Lambda는 handler가 다르므로, 같은 zip을 쓰더라도 handler를 다음처럼 분리합니다.

| Function | Handler |
|---|---|
| `wsc2026-ec2-stop-remediation` | `index.ec2_stop_handler` |
| `wsc2026-ec2-terminate-alert` | `index.ec2_terminate_handler` |
| `wsc2026-sg-remediation` | `index.sg_remediation_handler` |
| `wsc2026-tag-alert` | `index.tag_alert_handler` |

### 2.5 EventBridge Rules

채점 스크립트는 두 rule target을 확인합니다.

| Rule | Target |
|---|---|
| `wsc2026-ec2-stop-rule` | `wsc2026-ec2-stop-remediation` |
| `wsc2026-ec2-terminate-rule` | `wsc2026-ec2-terminate-alert` |

EC2 stopped rule event pattern:

```json
{
  "source": ["aws.ec2"],
  "detail-type": ["EC2 Instance State-change Notification"],
  "detail": {
    "state": ["stopped"]
  }
}
```

terminated rule:

```json
{
  "source": ["aws.ec2"],
  "detail-type": ["EC2 Instance State-change Notification"],
  "detail": {
    "state": ["terminated"]
  }
}
```


### 2.5.1 문제지 EventBridge Rule도 함께 구성

기존 3.5의 두 Rule은 채점표가 직접 조회하므로 필수입니다. 여기에 문제지의 다음 Rule도 구성합니다.

| 문제지 Rule | 감지할 CloudTrail API 예시 | 처리 방향 |
|---|---|---|
| wsc2026-sg-change-rule | AuthorizeSecurityGroupIngress | 허용되지 않은 Ingress 제거·SNS |
| wsc2026-role-change-rule | AssociateIamInstanceProfile, ReplaceIamInstanceProfileAssociation | 원래 Role 복구·SNS |
| wsc2026-ec2-terminate-rule | TerminateInstances | 복구 불가, SNS 알림 |
| wsc2026-ec2-type-change-rule | ModifyInstanceAttribute | 원래 Instance Type 복구·SNS |

동일한 `wsc2026-ec2-terminate-rule`은 중복 생성하지 말고 Target을 `wsc2026-ec2-terminate-alert`로 연결합니다. API 호출 감지를 위해 `wsc2026-event-trail`에서 Management Read/Write Events를 기록해야 합니다.

### 2.6 AWS Config Rules

채점은 rule state가 `ACTIVE`인지 확인합니다.

| Rule | 목적 |
|---|---|
| `wsc2026-sg-ssh-rule` | SG SSH 0.0.0.0/0 미준수 감지 |
| `wsc2026-required-tags-rule` | 필수 태그 미준수 감지 |

Config를 처음 쓰는 리전이면 Configuration Recorder와 Delivery Channel이 먼저 필요합니다. S3 delivery bucket도 준비합니다.

### 2.7 채점 확인

채점 스크립트는 먼저 EC2를 stop하고 SG에 SSH open을 추가합니다.

```bash
aws ec2 stop-instances --instance-ids ${INSTANCE_ID}
aws ec2 authorize-security-group-ingress \
  --group-id ${SG_ID} \
  --protocol tcp \
  --port 22 \
  --cidr 0.0.0.0/0
```

그 뒤 Lambda를 invoke하고 연습 기준 최대 **60초** 동안 Polling하여 확인합니다. 실제 대기시간은 공식 채점표를 우선합니다.

```bash
aws lambda invoke \
  --function-name wsc2026-sg-remediation \
  --payload '{"detail":{"configRuleName":"wsc2026-sg-ssh-rule","resourceType":"AWS::EC2::SecurityGroup","resourceId":"'"${SG_ID}"'","newEvaluationResult":{"complianceType":"NON_COMPLIANT"}}}' \
  /tmp/sg-out.json

sleep 60
aws ec2 describe-instances --instance-ids ${INSTANCE_ID} \
  --query "Reservations[0].Instances[0].State.Name" \
  --output text
aws ec2 describe-security-groups --group-ids ${SG_ID} \
  --query "SecurityGroups[0].IpPermissions | length(@)" \
  --output text
```

기대값:

- EC2 State: `running`
- SG Inbound Count: `0`

---
## 3. 예상 채점 명령

```bash
aws configure set region eu-west-1
INSTANCE_ID=$(aws ec2 describe-instances --filters "Name=tag:Name,Values=wsc2026-event-ec2" --query "Reservations[0].Instances[0].InstanceId" --output text)
SG_ID=$(aws ec2 describe-security-groups --filters "Name=group-name,Values=wsc2026-event-sg" --query "SecurityGroups[0].GroupId" --output text)

# E-1: EC2와 SG
aws ec2 describe-instances --instance-ids "$INSTANCE_ID" --query "Reservations[0].Instances[0].[InstanceType,State.Name,Tags[?Key=='Name'].Value|[0]]" --output text
aws ec2 describe-security-groups --group-ids "$SG_ID" --query "SecurityGroups[0].GroupName" --output text

# E-2: CloudTrail과 SNS
aws cloudtrail get-trail-status --name wsc2026-event-trail --query "[IsLogging,LatestDeliveryError]" --output text
aws sns list-topics --query "Topics[?ends_with(TopicArn,':wsc2026-event-alert')].TopicArn" --output text

# E-3: Lambda
for fn in wsc2026-ec2-stop-remediation wsc2026-ec2-terminate-alert wsc2026-sg-remediation wsc2026-tag-alert; do
  aws lambda get-function-configuration --function-name "$fn" --query "[FunctionName,Runtime,Environment.Variables]" --output json
done

# E-4: Rule과 Target
for rule in wsc2026-ec2-stop-rule wsc2026-ec2-terminate-rule; do
  aws events describe-rule --name "$rule" --query "[Name,State]" --output text
  aws events list-targets-by-rule --rule "$rule" --query "Targets[].Arn" --output text
done

# E-5: Config Rule
aws configservice describe-config-rules --config-rule-names wsc2026-sg-ssh-rule wsc2026-required-tags-rule --query "ConfigRules[].[ConfigRuleName,ConfigRuleState]" --output text

# E-6/E-7: 복구 시험
aws ec2 stop-instances --instance-ids "$INSTANCE_ID"
aws ec2 authorize-security-group-ingress --group-id "$SG_ID" --protocol tcp --port 22 --cidr 0.0.0.0/0
aws lambda invoke --function-name wsc2026-sg-remediation --cli-binary-format raw-in-base64-out --payload '{"detail":{"newEvaluationResult":{"complianceType":"NON_COMPLIANT"}}}' /tmp/sg-out.json

for i in {1..12}; do
  STATE=$(aws ec2 describe-instances --instance-ids "$INSTANCE_ID" --query "Reservations[0].Instances[0].State.Name" --output text)
  SSH_COUNT=$(aws ec2 describe-security-groups --group-ids "$SG_ID" --query "length(SecurityGroups[0].IpPermissions[?IpProtocol=='tcp' && FromPort==`22`].IpRanges[?CidrIp=='0.0.0.0/0'])" --output text)
  echo "[$i] state=$STATE public_ssh=$SSH_COUNT"
  [ "$STATE" = "running" ] && [ "$SSH_COUNT" = "0" ] && break
  sleep 10
done
```

## 4. 시험 당일 변형 대응

| 바뀔 수 있는 항목 | 즉시 수정할 위치 |
|---|---|
| Region | Console, `aws configure`, ARN |
| Stop 대신 Terminate | 자동 복구 대신 SNS 알림 설계 |
| SSH 22 대신 RDP 3389 | Config Parameter와 Lambda Port |
| IPv4 대신 IPv6 `::/0` | `Ipv6Ranges` 검사 추가 |
| SNS 대신 SQS | Lambda 권한과 검증 명령 |
| Config Rule 이름 | Event Pattern과 채점 CLI |
| 대기시간 | 고정 sleep 대신 Polling 횟수 |

- EventBridge는 이벤트 발생, AWS Config는 현재 구성의 준수 여부를 판단합니다.
- Rule뿐 아니라 Target과 Lambda Resource Policy가 모두 필요합니다.
- Security Group 전체가 아니라 위험한 Ingress만 제거합니다.
- 공식 추가 과제가 공개되면 예상 이름을 복사하지 말고 문제지와 채점 Query에서 정확한 값을 다시 뽑습니다.
