# 내일 변경과제 30% 대응센터

대상: 제2과제 Small Challenge / Workflow · Analytics · Event Handling · MSK

최종 과제지가 공개되면 예측보다 **최종 과제지, 최신 공지, 채점 명령**을 우선합니다. 바로 구축하지 말고 첫 10분 동안 변경점을 먼저 표시하세요.

## 시험 시작 후 첫 10분

| 시간 | 작업 | 완료 기준 |
|---:|---|---|
| 0~2분 | 네 Module Region·배점 확인 | Region 표 작성 |
| 2~4분 | 이름·Runtime·Port 비교 | 변경값 표시 |
| 4~6분 | PK/SK·JSON 필드·Timestamp 확인 | 스키마 확정 |
| 6~8분 | 채점 명령의 이름·Query 확인 | 검사 대상 확정 |
| 8~10분 | 문제지·채점표·공지 충돌 확인 | 작업 순서 결정 |

이름과 CIDR을 확인하지 않은 VPC, PK/SK를 확인하지 않은 DynamoDB, Runtime을 확인하지 않은 Lambda, Topic을 확인하지 않은 MSK는 먼저 만들지 않습니다.

## 변경점 기록표

| 구분 | 현재 공개값 | 최종 과제 값 | 수정 위치 |
|---|---|---|---|
| M1 Region | ap-southeast-1 |  | Console·CLI |
| M1 Bucket | student-score-bucket-번호 |  | S3·Env |
| M1 PK/SK | studentId / examDate |  | DB·Code |
| M1 Runtime | python3.12 |  | Lambda |
| M1 검증·등급 | 현재 TODO 기준 |  | Python |
| M2 Region | ap-northeast-2 |  | Console·CLI |
| M2 EC2·Port | t3.small / 5000 |  | EC2·TG |
| M2 Kinesis | ON_DEMAND |  | Kinesis |
| M2 Flink | ZEPPELIN-FLINK-3_0 |  | Notebook |
| M2 SQL | 1분 COUNT·SUM |  | SQL |
| M3 Region | eu-west-1 |  | Console·CLI |
| M3 감지·복구 | Stop·SG 등 |  | Rule·Lambda |
| M3 대기 | 60초 |  | 검증 |
| M4 Region | ap-northeast-1 |  | Console·CLI |
| M4 Kafka | 3.6.0 / t3.small |  | MSK |
| M4 Topic | raw 3/2, alert 1/2 |  | Kafka CLI |
| M4 PK/SK | sensorId / timestamp |  | DB·Lambda |
| M4 임계값 | 온도·습도 기준 |  | Lambda |
| M4 Timestamp | 실제 시각+Offset |  | Producer |

## 유지될 가능성이 높은 구조

### Module 1 Workflow

```text
S3 input -> Trigger -> Step Functions Standard
 -> Processing Lambda -> DynamoDB
 -> processed/ 또는 error/
```

변경 가능성이 높은 부분은 CSV 필드, 검증 규칙, 평균 반올림, 등급 기준과 오류 파일명입니다.

- [ ] Region·Bucket·Prefix
- [ ] PK/SK
- [ ] Lambda 이름·Runtime·Handler·환경변수
- [ ] CSV Header와 필수 필드
- [ ] 평균·Grade 규칙
- [ ] 오류 사유·파일 개수
- [ ] State Machine Type
- [ ] 정상·오류 기대 출력

과목 목록, 검증, 평균, 등급, 오류 저장을 별도 함수로 분리하고 학생 ID를 하드코딩하지 않습니다.

### Module 2 Analytics

```text
ALB -> Private EC2 App -> Kinesis On-demand
                         -> Flink Studio SQL
```

- [ ] VPC·Subnet CIDR
- [ ] EC2 Name·Type·Subnet
- [ ] App Port·Health Path
- [ ] IAM Role 철자
- [ ] Kinesis Name·Mode
- [ ] JSON 필드와 타입
- [ ] Flink Runtime
- [ ] SQL Window·집계
- [ ] systemd Unit 이름

긴급 SQL 수정 예:

```sql
SELECT COUNT(*) AS order_count
FROM order_stream
WHERE event_time > CURRENT_TIMESTAMP - INTERVAL ''1'' MINUTE;

SELECT product_name, SUM(price * quantity) AS total_revenue
FROM order_stream
GROUP BY product_name;
```

Window가 5분으로 바뀌면 INTERVAL 값부터 바꾸고, 평균 주문금액이면 `AVG(price * quantity)`로 교체합니다.

### Module 3 Event Handling

```text
CloudTrail / AWS Config -> EventBridge
 -> Lambda Remediation -> SNS
```

- [ ] CloudTrail 이름·Management Events
- [ ] SNS Topic
- [ ] Lambda 이름·Runtime·Handler
- [ ] EventBridge Rule 이름
- [ ] Event Pattern의 API
- [ ] Target ARN
- [ ] Config Rule 이름·ACTIVE
- [ ] 복구 후 기대 상태
- [ ] 최신 대기시간

문제지와 채점표 이름이 다르면 채점표가 직접 조회하는 리소스를 반드시 만들고 문제지 전용 Rule도 유지합니다.

동작하지 않을 때 확인 순서:

1. CloudTrail에 API 호출이 기록되는가
2. Event Pattern이 실제 이벤트와 일치하는가
3. Rule이 ENABLED인가
4. Target ARN이 맞는가
5. Lambda에 EventBridge Invoke 권한이 있는가
6. 실행 Role에 복구 API 권한이 있는가

### Module 4 MSK

```text
Producer -> MSK raw -> Sensor Lambda -> DynamoDB
                                  -> MSK alert
             Alert Lambda -> SNS + S3
```

- [ ] VPC·Subnet AZ
- [ ] Cluster·Kafka Version·Broker Type
- [ ] IAM Authentication
- [ ] Topic·Partition·Replication
- [ ] Producer EC2
- [ ] Lambda Runtime·Trigger Topic
- [ ] DynamoDB PK/SK
- [ ] 메시지 필드·임계값
- [ ] 실제 Timestamp
- [ ] S3 Bucket·Key와 SNS

안전한 Timestamp 구현:

```python
from datetime import datetime
timestamp = datetime.now().astimezone().isoformat(timespec="seconds")
```

`2026-08-24T09:15:30+09:00`처럼 실제 시각이어야 하며 `(timestamp)`나 고정 샘플을 저장하면 안 됩니다.

## 채점 명령에서 정답 찾기

| 명령 요소 | 확인할 정답 |
|---|---|
| Region | 생성 위치 |
| --name / --table-name | 정확한 이름 |
| --query | 실제 검사 필드와 타입 |
| sleep | 허용 처리시간 |
| 기대 출력 | 값·개수·형식 |

```bash
--query "Items[0].{sensorId:sensorId.S,temperature:temperature.S,status:status.S}"
```

위 Query의 `.S`는 String 타입입니다. 숫자처럼 보여도 DynamoDB Number로 저장하면 결과가 비어 채점에 실패할 수 있습니다.

## 시험용 공통 변수

```bash
export NUMBER=<비번호>
export ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
export M1_REGION=ap-southeast-1
export M1_BUCKET=wsc2026-student-score-bucket-$NUMBER
export M2_REGION=ap-northeast-2
export M3_REGION=eu-west-1
export M4_REGION=ap-northeast-1
export M4_BUCKET=wsc2026-sensor-alert-bucket-$NUMBER
```

가능하면 모든 조회 명령에 `--region`을 붙여 다른 Module Region에서 생성하는 실수를 막습니다.

## 4시간 운영 전략

| 시간 | 목표 |
|---:|---|
| 0:00~0:10 | 변경점 분석 |
| 0:10~0:55 | Module 1 |
| 0:55~1:45 | Module 2 |
| 1:45~2:35 | Module 3 |
| 2:35~3:30 | Module 4 |
| 3:30~3:50 | 전체 채점·수정 |
| 3:50~4:00 | 부하 중지·제출 |

NAT Gateway와 MSK처럼 오래 걸리는 리소스는 먼저 생성하고 기다리는 동안 IAM·DynamoDB·코드를 구성합니다.

## 3분 장애 진단

| 증상 | 우선 확인 |
|---|---|
| Lambda AccessDenied | Execution Role·Resource ARN·Region |
| S3 Trigger 미동작 | Prefix·Suffix·Invoke permission |
| Step Functions 실패 | Execution event·Lambda log |
| SSM 접속 불가 | Role·NAT/Endpoint·SG |
| ALB 503 | Target Health·Port·SG |
| Kinesis 데이터 없음 | App log·PutRecord 권한 |
| Flink 결과 없음 | Source DDL·Region·타입 |
| EventBridge 미동작 | Pattern·Target·Invoke permission |
| MSK Timeout | SG·Subnet·IAM auth·Bootstrap |
| MSK Mapping Disabled | State reason·VPC·Role·Topic |
| Timestamp 없음 | Producer payload·Consumer parsing |

## 제출 직전

- [ ] 네 Region과 모든 리소스 이름 확인
- [ ] IAM Trust와 최소 권한 확인
- [ ] M1 이전 결과 정리 후 지정 CSV 1회 처리
- [ ] M2 health·order·systemd active/enabled
- [ ] M3 60초 후 EC2 Running·SG Inbound 0
- [ ] M4 MSK ACTIVE·IAM True·Mapping Enabled
- [ ] DynamoDB 실제 Timestamp 확인
- [ ] 테스트 Loop와 부하 중지
- [ ] 채점 출력 보관

## 관련 페이지

- [2과제 전체 콘솔·CLI 풀이](guide.html?doc=task02second)
- [M1 Workflow](guide.html?doc=task02m1)
- [M2 Analytics](guide.html?doc=task02m2)
- [M3 Event Handling](guide.html?doc=task02m3)
- [M4 MSK](guide.html?doc=task02m4)
