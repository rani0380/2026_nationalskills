# 02_1과제_1등 상세 풀이

대상 폴더: `select/1과제/02_1과제_1등`

이 과제는 `Korea Skills Concert` 예약 서비스를 AWS 위에 구축하는 문제입니다. 단순히 리소스를 만드는 문제가 아니라, 채점 스크립트가 정해진 이름과 경로를 조회하므로 **리소스명, 태그, 경로, 헤더, KMS alias**를 정확히 맞추는 것이 가장 중요합니다.

## 시작하기 전에: 이 문서의 사용법

이 문서는 단순한 AWS 구축 예제가 아니라 **채점 스크립트에서 요구하는 결과를 만드는 순서**로 작성되어 있습니다. 각 절에서 리소스를 만든 직후 `확인` 명령을 실행하고, 기대값이 다르면 다음 절로 넘어가지 않는 방식으로 사용하세요.

시험 중에는 다음 세 종류의 값을 구분해야 합니다.

| 종류 | 예시 | 처리 원칙 |
|---|---|---|
| 반드시 그대로 입력 | `wskorea26-vpc`, `wskorea26-cluster`, `stable` | 대소문자, 하이픈, 경로까지 변경 금지 |
| 본인 값으로 치환 | `<ACCOUNT_ID>`, `<NO>`, `<VPC_ID>` | 명령 실행 전에 실제 값으로 치환 |
| 명령 결과를 변수로 저장 | `VPC_ID`, `CF_ID`, `ALB_ARN` | 새 터미널을 열면 다시 조회 |

가장 안전한 진행 방식은 **생성 → 대기 → 확인 → 다음 단계**입니다. EKS, NAT Gateway, ALB, CloudFront처럼 비동기로 만들어지는 서비스는 생성 명령이 성공해도 즉시 사용할 수 없습니다.

### 권장 구축 순서

| 순서 | 작업 | 완료 판정 |
|---|---|---|
| 1 | 공통 변수와 도구 확인 | 계정 ID와 리전이 정상 출력 |
| 2 | KMS 키 3개 생성 | 세 alias가 모두 조회됨 |
| 3 | VPC, Subnet, IGW, NAT, Route Table | 모든 기본 경로가 `active` |
| 4 | S3, DynamoDB, ECR | 암호화와 이름이 채점값과 일치 |
| 5 | EKS와 NodeGroup | 모든 node가 `Ready` |
| 6 | book 앱과 Lambda | 내부 health/API 테스트 성공 |
| 7 | ALB | 직접 접속 403, 헤더 포함 요청만 전달 |
| 8 | CloudFront | HTTPS root, 이미지, POST, GET 성공 |
| 9 | Monitoring | Grafana URL과 dashboard 확인 |
| 10 | `mark.sh` | 각 영역 출력값을 문제지와 대조 |

### 사전 도구와 계정 점검

아래 명령 중 하나라도 실패하면 먼저 도구 또는 인증 상태를 해결합니다.

```bash
aws --version
kubectl version --client
eksctl version
helm version
docker --version

aws sts get-caller-identity
aws configure get region
```

오래된 환경 변수나 다른 계정 프로필이 남아 있으면 엉뚱한 계정에 리소스를 만들 수 있습니다.

```bash
echo "AWS_PROFILE=${AWS_PROFILE}"
echo "AWS_REGION=${AWS_REGION}"
aws sts get-caller-identity --query '[Account,Arn]' --output table
```

`Account`가 시험 계정과 다르면 작업을 시작하지 말고 자격 증명부터 다시 설정합니다.

## 0. 전체 아키텍처

사용자 요청 흐름은 다음과 같이 잡습니다.

1. 사용자는 CloudFront 도메인으로 접속합니다.
2. `/` 요청은 S3의 `web/main/index.html`로 전달됩니다.
3. `/main.jpeg` 요청은 S3의 `web/main/main.jpeg`로 전달됩니다.
4. `/book` POST 요청은 CloudFront -> ALB -> EKS book Pod로 전달되어 DynamoDB에 예약 정보를 저장합니다.
5. `/book?concert_name=...` GET 요청은 CloudFront -> ALB -> Lambda로 전달되어 DynamoDB에서 예약 정보를 조회합니다.
6. ALB에 직접 접근하면 `403 Forbidden`을 반환합니다.
7. EKS의 시스템/모니터링 컴포넌트는 addon nodegroup, book 애플리케이션은 app nodegroup에서만 동작합니다.

채점 스크립트는 다음 값을 직접 확인합니다.

| 영역 | 채점에서 찾는 값 |
|---|---|
| VPC | `wskorea26-vpc`, `172.16.0.0/16` |
| Subnet | `wskorea26-pub-subnet-c`, `wskorea26-pub-subnet-d`, `wskorea26-priv-subnet-c`, `wskorea26-priv-subnet-d` |
| Route Table | `wskorea26-public-rtb`, `wskorea26-private-rtb-c`, `wskorea26-private-rtb-d` |
| S3 | `wskorea26-concert-bucket-`로 시작하는 버킷 |
| S3 Object | `web/main/index.html`, `web/main/main.jpeg` |
| S3 KMS alias | `alias/wskorea26-s3-key` |
| ECR | `wskorea26-book-repo`, image tag `stable` |
| DynamoDB | `wskorea26-data-table`, PK `client_id` |
| DynamoDB KMS alias | `alias/wskorea26-dynamodb-key` |
| EKS | `wskorea26-cluster`, version `1.35` |
| EKS KMS alias | `alias/wskorea26-eks-key` |
| NodeGroup | `wskorea26-addon-ng`, `wskorea26-app-ng` |
| Node label | addon: `node-type=addon`, app: `node-type=app` |
| Namespace | `wskorea26` |
| Lambda | `wskorea26-book-lambda`, env `TABLE_NAME=wskorea26-data-table` |
| ALB | `wskorea26-book-alb`, internet-facing, HTTP 80 |
| CloudFront Comment | `wskorea26-concert-cf` |
| CloudFront Header | `X-Origin-Verify: wskorea26-cf`, `wskorea26-s3-access` |
| Grafana | service `grafana` in namespace `monitoring`, dashboard path `/d/wskorea26/wskorea26-monitoring` |
## 콘솔 중심 풀이: 화면에서 만드는 순서

이 절은 AWS Management Console을 기준으로 전체 구축 순서를 설명합니다. 우측 상단 리전은 먼저 **서울 `ap-northeast-2`**로 바꿉니다. CloudFront와 IAM은 글로벌 서비스이므로 리전 선택이 표시되지 않을 수 있습니다.

콘솔에서 가능한 작업은 콘솔로 진행하고, 다음 작업만 CloudShell을 사용합니다.

| CloudShell이 필요한 작업 | 이유 |
|---|---|
| ECR 이미지 build/push | 컨테이너 이미지를 만들어 registry에 올려야 함 |
| `kubectl apply` | Kubernetes Deployment, Service, Ingress 생성 |
| Helm 설치 | ALB Controller, Prometheus, Grafana 배포 |
| 최종 `curl`과 `mark.sh` | 실제 채점 요청과 동일하게 검증 |

### 콘솔 1단계: KMS 키 3개

경로: **KMS → 고객 관리형 키 → 키 생성**

각 키를 다음 설정으로 한 번씩, 총 3개 만듭니다.

| 항목 | S3 키 | DynamoDB 키 | EKS 키 |
|---|---|---|---|
| 키 유형 | 대칭 | 대칭 | 대칭 |
| 키 용도 | 암호화 및 복호화 | 암호화 및 복호화 | 암호화 및 복호화 |
| 별칭 | `wskorea26-s3-key` | `wskorea26-dynamodb-key` | `wskorea26-eks-key` |
| 최종 alias | `alias/wskorea26-s3-key` | `alias/wskorea26-dynamodb-key` | `alias/wskorea26-eks-key` |

키 관리자와 키 사용자는 시험 계정에서 지정된 IAM 사용자 또는 관리자 역할을 선택합니다. 생성 후 고객 관리형 키 목록에서 세 alias가 모두 보이는지 확인합니다.

### 콘솔 2단계: VPC와 Subnet

경로: **VPC → Your VPCs → Create VPC**

1. Resources to create는 `VPC only`를 선택합니다.
2. Name tag에 `wskorea26-vpc`를 입력합니다.
3. IPv4 CIDR manual input에 `172.16.0.0/16`을 입력합니다.
4. Tenancy는 `Default`로 두고 생성합니다.
5. 생성한 VPC 선택 → Actions → Edit VPC settings에서 DNS resolution과 DNS hostnames를 모두 켭니다.

경로: **VPC → Subnets → Create subnet**

VPC에서 `wskorea26-vpc`를 선택하고 다음 4개 subnet을 만듭니다.

| Subnet name | Availability Zone | IPv4 CIDR |
|---|---|---|
| `wskorea26-pub-subnet-c` | `ap-northeast-2c` | `172.16.1.0/24` |
| `wskorea26-pub-subnet-d` | `ap-northeast-2d` | `172.16.2.0/24` |
| `wskorea26-priv-subnet-c` | `ap-northeast-2c` | `172.16.201.0/24` |
| `wskorea26-priv-subnet-d` | `ap-northeast-2d` | `172.16.202.0/24` |

Public subnet 두 개는 각각 선택 → Actions → Edit subnet settings → **Enable auto-assign public IPv4 address**를 켭니다.

각 subnet의 Tags 탭에서 다음 태그도 추가합니다.

| 대상 | Key | Value |
|---|---|---|
| Public C, D | `kubernetes.io/role/elb` | `1` |
| Private C, D | `kubernetes.io/role/internal-elb` | `1` |

### 콘솔 3단계: IGW, NAT Gateway, Route Table

경로: **VPC → Internet gateways → Create internet gateway**

1. 이름은 문제지 Reference01과 같은 `book-igw`로 생성합니다.
2. 생성 직후 Actions → Attach to a VPC를 누릅니다.
3. `wskorea26-vpc`를 선택해 연결합니다.

경로: **VPC → NAT gateways → Create NAT gateway**

| 이름 | Subnet | Elastic IP allocation |
|---|---|---|
| `book-ngw-c` | `wskorea26-pub-subnet-c` | Allocate Elastic IP |
| `book-ngw-d` | `wskorea26-pub-subnet-d` | Allocate Elastic IP |

두 NAT Gateway의 상태가 `Available`이 된 다음 Route Table을 만듭니다.

경로: **VPC → Route tables → Create route table**

| 이름 | 연결 Subnet | `0.0.0.0/0` Target |
|---|---|---|
| `wskorea26-public-rtb` | Public C, Public D | Internet Gateway `book-igw` |
| `wskorea26-private-rtb-c` | Private C | NAT Gateway C |
| `wskorea26-private-rtb-d` | Private D | NAT Gateway D |

각 Route Table에서 다음 두 탭을 모두 설정해야 합니다.

1. Routes → Edit routes → Add route → `0.0.0.0/0`과 위 Target 선택
2. Subnet associations → Edit subnet associations → 해당 subnet 선택

### 콘솔 3-1단계: 채점용 보안 그룹

경로: **VPC → Security groups → Create security group**

1. Security group name: `wskorea26-vpc-environment-sg`
2. VPC: `wskorea26-vpc`
3. EKS API endpoint에 접근할 수 있도록 HTTPS 443 inbound를 시험 환경의 허용 범위에 맞게 추가합니다.
4. Outbound의 HTTP 80과 HTTPS 443은 문제지 유의사항에 따라 허용합니다.

이 보안 그룹은 경기 종료 후 채점자가 Private D subnet에 CloudShell VPC environment를 만들 때 사용합니다. 이름이 다르면 사전 준비 단계가 실패합니다.

### 콘솔 4단계: S3 버킷과 정적 파일

경로: **S3 → Buckets → Create bucket**

1. Bucket name은 `wskorea26-concert-bucket-비번호`로 입력합니다.
2. AWS Region은 `ap-northeast-2`입니다.
3. Block Public Access 네 항목을 모두 켠 상태로 둡니다.
4. Default encryption에서 `AWS Key Management Service key (SSE-KMS)`를 선택합니다.
5. KMS key는 `alias/wskorea26-s3-key`를 선택합니다.
6. 버킷을 생성합니다.

객체 경로는 반드시 다음 구조가 되어야 합니다.

```text
web/
└─ main/
   ├─ index.html
   └─ main.jpeg
```

버킷 → Create folder로 `web`, 그 안에 `main` 폴더를 만든 뒤 `index.html`, `main.jpeg`를 업로드합니다. 업로드 화면의 Properties → Server-side encryption에서 bucket setting을 사용하거나 `alias/wskorea26-s3-key`를 직접 선택합니다.

### 콘솔 5단계: DynamoDB

경로: **DynamoDB → Tables → Create table**

1. Table name: `wskorea26-data-table`
2. Partition key: `client_id`, Type: `String`
3. Table settings는 `Customize settings` 선택
4. Read/write capacity settings는 `On-demand`
5. Encryption at rest는 `Owned by your account` 또는 Customer managed key 선택
6. KMS key: `alias/wskorea26-dynamodb-key`
7. Deletion protection을 활성화하고 생성

최신 예약순 조회를 안정적으로 처리하려면 생성 후 Indexes → Create index에서 다음 GSI를 추가합니다.

| 항목 | 값 |
|---|---|
| Partition key | `concert_name` String |
| Sort key | `created_at` String |
| Index name | `concert-created-index` |
| Attribute projections | All |

### 콘솔 6단계: ECR

경로: **Elastic Container Registry → Private registry → Repositories → Create repository**

1. Repository name: `wskorea26-book-repo`
2. Image tag mutability: Mutable
3. Scan on push: 활성화
4. Encryption: KMS
5. KMS key: 사용할 고객 관리형 키 선택
6. Create repository

Repository를 연 뒤 **View push commands**를 누르면 로그인, build, tag, push 명령이 계정 ID에 맞게 표시됩니다. 제공 Dockerfile이 있는 폴더에서 명령을 실행하되 image tag는 반드시 `stable`로 바꿉니다. 이미지 목록에 `stable`이 나타나고 scan 상태가 Complete인지 확인합니다.

### 콘솔 7단계: IAM 역할

EKS Cluster role, Node role, Lambda role, book Pod role이 필요합니다. 경로는 **IAM → Roles → Create role**입니다.

| Role | Trusted entity | 핵심 권한 |
|---|---|---|
| Cluster role | EKS Cluster | `AmazonEKSClusterPolicy` |
| Node role | EC2 | `AmazonEKSWorkerNodePolicy`, `AmazonEC2ContainerRegistryPullOnly`, `AmazonEKS_CNI_Policy` |
| Lambda role | Lambda | CloudWatch Logs, DynamoDB Query/Scan |
| book Pod role | Web identity 또는 EKS Pod Identity | DynamoDB PutItem/UpdateItem/GetItem/Query/Scan |

DynamoDB 권한의 Resource에는 table ARN뿐 아니라 `table/wskorea26-data-table/index/*`도 포함합니다.

### 콘솔 8단계: EKS Cluster

경로: **EKS → Clusters → Create cluster**

1. Cluster name: `wskorea26-cluster`
2. Kubernetes version: 문제에서 요구한 `1.35`
3. Cluster service role: 앞에서 만든 EKS Cluster role
4. VPC: `wskorea26-vpc`
5. Subnets: Private C와 Private D만 선택
6. Cluster endpoint access는 시험 요구사항에 맞게 설정합니다. CloudShell에서 kubectl을 쓸 경우 접근 가능한 endpoint가 필요합니다.
7. Control plane logging의 `API`, `Audit`, `Authenticator`, `Controller manager`, `Scheduler`를 모두 활성화합니다.
8. Secrets encryption에서 `alias/wskorea26-eks-key`를 선택합니다.
9. Create 후 Status가 `Active`가 될 때까지 기다립니다.

경로: **EKS → wskorea26-cluster → Compute → Add node group**

NodeGroup을 두 번 만듭니다.

| 항목 | Addon NodeGroup | App NodeGroup |
|---|---|---|
| Name | `wskorea26-addon-ng` | `wskorea26-app-ng` |
| Node IAM role | 앞에서 만든 Node role | 앞에서 만든 Node role |
| Instance type | `t3.medium` | `t3.medium` |
| Subnets | Private C, Private D | Private C, Private D |
| Desired/min/max | 문제지 기준 | 문제지 기준 |
| Kubernetes label | `node-type=addon` | `node-type=app` |
| Tag | `Name=wskorea26-addon-node` | `Name=wskorea26-app-node` |

채점은 VPC 환경의 CloudShell에서 진행합니다. 콘솔의 CloudShell → Actions → Create VPC environment에서 다음 값을 선택합니다.

| 항목 | 값 |
|---|---|
| VPC | `wskorea26-vpc` |
| Subnet | `wskorea26-priv-subnet-d` |
| Security group | `wskorea26-vpc-environment-sg` |

환경 접속 후 기본 리전과 EKS 연결을 확인합니다.

```bash
aws configure set region ap-northeast-2
aws eks update-kubeconfig --region ap-northeast-2 --name wskorea26-cluster
kubectl get nodes -L node-type
```

NodeGroup이 `Active`가 된 뒤 CloudShell에서 kubeconfig와 namespace를 준비합니다.

```bash
aws eks update-kubeconfig --region ap-northeast-2 --name wskorea26-cluster
kubectl get nodes -L node-type
kubectl create namespace wskorea26
kubectl create namespace monitoring
```

### 콘솔 9단계: Lambda

경로: **Lambda → Functions → Create function → Author from scratch**

1. Function name: `wskorea26-book-lambda`
2. Runtime: 문제에서 요구한 Python 버전
3. Architecture: `x86_64`
4. Execution role: 앞에서 만든 Lambda role
5. Create function
6. Code 탭에 10절의 `lambda_function.py` 내용을 붙여넣고 Deploy
7. Configuration → Environment variables에서 다음 값을 입력

| Key | Value |
|---|---|
| `TABLE_NAME` | `wskorea26-data-table` |
| `INDEX_NAME` | `concert-created-index` |
| `AWS_REGION` | `ap-northeast-2` |

Test 탭에서 `{ "queryStringParameters": { "concert_name": "TEST" } }` 이벤트를 실행해 JSON 응답이 반환되는지 확인합니다.

### 콘솔 10단계: Kubernetes 앱과 ALB

book 앱의 Deployment와 Service는 9절의 YAML을 CloudShell에서 적용합니다. AWS Load Balancer Controller 설치도 Helm 또는 eksctl 명령이 필요합니다.

```bash
kubectl apply -f book-deploy.yaml
kubectl get pods -n wskorea26 -o wide
kubectl apply -f book-ingress.yaml
kubectl get ingress -n wskorea26
```

ALB 생성 후 경로: **EC2 → Load Balancers → `wskorea26-book-alb` → Listeners and rules**

HTTP:80 listener의 규칙을 다음 순서로 맞춥니다.

| Priority | 조건 | 작업 |
|---|---|---|
| 10 | Header `X-Origin-Verify=wskorea26-cf`, Path `/v1/book`, Method POST | EKS target group으로 Forward |
| 20 | Header `X-Origin-Verify=wskorea26-cf`, Path `/reserv-query`, Method GET | Lambda target group으로 Forward |
| Default | 조건 없음 | Fixed response `403`, Content type `text/plain`, Body `Forbidden` |

Lambda target group은 **EC2 → Target Groups → Create target group**에서 Target type을 `Lambda function`으로 선택해 만들고 `wskorea26-book-lambda`를 등록합니다.

### 콘솔 11단계: CloudFront

경로: **CloudFront → Distributions → Create distribution**

먼저 S3 origin을 기본 origin으로 추가합니다.

| 항목 | 값 |
|---|---|
| Origin domain | 생성한 S3 bucket의 regional domain |
| Origin path | `/web/main` |
| Origin access | Origin access control settings (recommended) |
| Default root object | `index.html` |

## 1. 공통 변수 설정

```bash
export AWS_REGION=ap-northeast-2
export ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
export NO=<비번호>
export BUCKET=wskorea26-concert-bucket-${NO}

aws configure set region ${AWS_REGION}
aws sts get-caller-identity
```

비번호가 `12`라면 버킷명은 예를 들어 `wskorea26-concert-bucket-12`처럼 만듭니다. 채점 스크립트는 `starts_with(Name, 'wskorea26-concert-bucket-')`로 첫 번째 버킷을 찾으므로 같은 prefix의 불필요한 버킷을 여러 개 만들지 않는 것이 좋습니다.

## 2. KMS 키 먼저 만들기

이 과제는 S3, DynamoDB, EKS Secret 암호화에서 KMS alias를 확인합니다. 리소스를 만들기 전에 키를 먼저 준비합니다.

```bash
S3_KEY_ID=$(aws kms create-key --description wskorea26-s3-key --query KeyMetadata.KeyId --output text)
aws kms create-alias --alias-name alias/wskorea26-s3-key --target-key-id ${S3_KEY_ID}

DDB_KEY_ID=$(aws kms create-key --description wskorea26-dynamodb-key --query KeyMetadata.KeyId --output text)
aws kms create-alias --alias-name alias/wskorea26-dynamodb-key --target-key-id ${DDB_KEY_ID}

EKS_KEY_ID=$(aws kms create-key --description wskorea26-eks-key --query KeyMetadata.KeyId --output text)
aws kms create-alias --alias-name alias/wskorea26-eks-key --target-key-id ${EKS_KEY_ID}
```

확인:

```bash
aws kms list-aliases --query "Aliases[?starts_with(AliasName, 'alias/wskorea26')].[AliasName,TargetKeyId]" --output table
```

## 3. 네트워크 구성

### 3.1 VPC와 Subnet

Reference01 그대로 구성합니다.

| 리소스 | CIDR | 라우팅 |
|---|---|---|
| `wskorea26-vpc` | `172.16.0.0/16` | - |
| `wskorea26-pub-subnet-c` | `172.16.1.0/24` | `wskorea26-public-rtb` -> IGW |
| `wskorea26-pub-subnet-d` | `172.16.2.0/24` | `wskorea26-public-rtb` -> IGW |
| `wskorea26-priv-subnet-c` | `172.16.201.0/24` | `wskorea26-private-rtb-c` -> NAT c |
| `wskorea26-priv-subnet-d` | `172.16.202.0/24` | `wskorea26-private-rtb-d` -> NAT d |

```bash
VPC_ID=$(aws ec2 create-vpc \
  --cidr-block 172.16.0.0/16 \
  --tag-specifications 'ResourceType=vpc,Tags=[{Key=Name,Value=wskorea26-vpc}]' \
  --query Vpc.VpcId --output text)

aws ec2 modify-vpc-attribute --vpc-id ${VPC_ID} --enable-dns-hostnames
aws ec2 modify-vpc-attribute --vpc-id ${VPC_ID} --enable-dns-support
```

AZ 이름은 계정마다 다를 수 있으므로 먼저 확인합니다.

```bash
aws ec2 describe-availability-zones \
  --query "AvailabilityZones[?State=='available'].ZoneName" \
  --output text
```

예시에서는 `ap-northeast-2c`, `ap-northeast-2d`를 사용합니다.

```bash
PUB_C=$(aws ec2 create-subnet --vpc-id ${VPC_ID} --cidr-block 172.16.1.0/24 --availability-zone ap-northeast-2c \
  --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=wskorea26-pub-subnet-c}]' --query Subnet.SubnetId --output text)
PUB_D=$(aws ec2 create-subnet --vpc-id ${VPC_ID} --cidr-block 172.16.2.0/24 --availability-zone ap-northeast-2d \
  --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=wskorea26-pub-subnet-d}]' --query Subnet.SubnetId --output text)
PRIV_C=$(aws ec2 create-subnet --vpc-id ${VPC_ID} --cidr-block 172.16.201.0/24 --availability-zone ap-northeast-2c \
  --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=wskorea26-priv-subnet-c}]' --query Subnet.SubnetId --output text)
PRIV_D=$(aws ec2 create-subnet --vpc-id ${VPC_ID} --cidr-block 172.16.202.0/24 --availability-zone ap-northeast-2d \
  --tag-specifications 'ResourceType=subnet,Tags=[{Key=Name,Value=wskorea26-priv-subnet-d}]' --query Subnet.SubnetId --output text)
```

ALB Controller를 사용할 경우 subnet discovery를 위해 태그를 추가해두면 좋습니다.

```bash
aws ec2 create-tags --resources ${PUB_C} ${PUB_D} --tags Key=kubernetes.io/role/elb,Value=1
aws ec2 create-tags --resources ${PRIV_C} ${PRIV_D} --tags Key=kubernetes.io/role/internal-elb,Value=1
```

### 3.2 IGW, NAT Gateway, Route Table

```bash
IGW_ID=$(aws ec2 create-internet-gateway \
  --tag-specifications 'ResourceType=internet-gateway,Tags=[{Key=Name,Value=book-igw}]' \
  --query InternetGateway.InternetGatewayId --output text)
aws ec2 attach-internet-gateway --vpc-id ${VPC_ID} --internet-gateway-id ${IGW_ID}

PUBLIC_RTB=$(aws ec2 create-route-table --vpc-id ${VPC_ID} \
  --tag-specifications 'ResourceType=route-table,Tags=[{Key=Name,Value=wskorea26-public-rtb}]' \
  --query RouteTable.RouteTableId --output text)
aws ec2 create-route --route-table-id ${PUBLIC_RTB} --destination-cidr-block 0.0.0.0/0 --gateway-id ${IGW_ID}
aws ec2 associate-route-table --route-table-id ${PUBLIC_RTB} --subnet-id ${PUB_C}
aws ec2 associate-route-table --route-table-id ${PUBLIC_RTB} --subnet-id ${PUB_D}
```

NAT는 private subnet의 EKS node가 ECR, CloudWatch, AWS API에 접근하기 위해 필요합니다.

```bash
EIP_C=$(aws ec2 allocate-address --domain vpc --query AllocationId --output text)
EIP_D=$(aws ec2 allocate-address --domain vpc --query AllocationId --output text)

NGW_C=$(aws ec2 create-nat-gateway --subnet-id ${PUB_C} --allocation-id ${EIP_C} \
  --tag-specifications 'ResourceType=natgateway,Tags=[{Key=Name,Value=book-ngw-c}]' \
  --query NatGateway.NatGatewayId --output text)
NGW_D=$(aws ec2 create-nat-gateway --subnet-id ${PUB_D} --allocation-id ${EIP_D} \
  --tag-specifications 'ResourceType=natgateway,Tags=[{Key=Name,Value=book-ngw-d}]' \
  --query NatGateway.NatGatewayId --output text)

aws ec2 wait nat-gateway-available --nat-gateway-ids ${NGW_C} ${NGW_D}
```

```bash
PRIV_RTB_C=$(aws ec2 create-route-table --vpc-id ${VPC_ID} \
  --tag-specifications 'ResourceType=route-table,Tags=[{Key=Name,Value=wskorea26-private-rtb-c}]' \
  --query RouteTable.RouteTableId --output text)
aws ec2 create-route --route-table-id ${PRIV_RTB_C} --destination-cidr-block 0.0.0.0/0 --nat-gateway-id ${NGW_C}
aws ec2 associate-route-table --route-table-id ${PRIV_RTB_C} --subnet-id ${PRIV_C}

PRIV_RTB_D=$(aws ec2 create-route-table --vpc-id ${VPC_ID} \
  --tag-specifications 'ResourceType=route-table,Tags=[{Key=Name,Value=wskorea26-private-rtb-d}]' \
  --query RouteTable.RouteTableId --output text)
aws ec2 create-route --route-table-id ${PRIV_RTB_D} --destination-cidr-block 0.0.0.0/0 --nat-gateway-id ${NGW_D}
aws ec2 associate-route-table --route-table-id ${PRIV_RTB_D} --subnet-id ${PRIV_D}
```

채점 대비 확인:

```bash
aws ec2 describe-vpcs \
  --filter Name=tag:Name,Values=wskorea26-vpc \
  --query "Vpcs[0].CidrBlock" --output text

aws ec2 describe-subnets \
  --filters "Name=tag:Name,Values=wskorea26-pub-subnet-c,wskorea26-pub-subnet-d,wskorea26-priv-subnet-c,wskorea26-priv-subnet-d" \
  --query "sort_by(Subnets,&Tags[?Key=='Name']|[0].Value)[].[Tags[?Key=='Name']|[0].Value,CidrBlock]" \
  --output table
```

## 4. S3 정적 페이지

### 4.1 버킷 생성과 Public 차단

```bash
aws s3api create-bucket \
  --bucket ${BUCKET} \
  --region ${AWS_REGION} \
  --create-bucket-configuration LocationConstraint=${AWS_REGION}

aws s3api put-public-access-block \
  --bucket ${BUCKET} \
  --public-access-block-configuration BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true
```

### 4.2 객체 업로드

문제지의 Object Path가 `/web/main/`이므로 정확히 이 prefix로 업로드합니다.

```bash
aws s3 cp index.html s3://${BUCKET}/web/main/index.html \
  --sse aws:kms \
  --sse-kms-key-id alias/wskorea26-s3-key \
  --content-type text/html

aws s3 cp main.jpeg s3://${BUCKET}/web/main/main.jpeg \
  --sse aws:kms \
  --sse-kms-key-id alias/wskorea26-s3-key \
  --content-type image/jpeg
```

채점 스크립트는 `head-object`로 `SSEKMSKeyId`를 읽고 그 key id의 alias가 `alias/wskorea26-s3-key`인지 확인합니다.

```bash
for key in web/main/index.html web/main/main.jpeg; do
  aws s3api head-object --bucket ${BUCKET} --key $key \
    --query "[Key,SSEKMSKeyId,ContentType]" --output table
done
```

## 5. DynamoDB

문제지에는 PK가 `client_id(S)`라고 되어 있습니다. GET 조회는 `concert_name` 최신순 정렬이 필요하므로 실전 풀이에서는 GSI를 추가하는 편이 안정적입니다.

| 항목 | 값 |
|---|---|
| Table | `wskorea26-data-table` |
| PK | `client_id` String |
| 권장 GSI | `concert-created-index` |
| GSI PK | `concert_name` String |
| GSI SK | `created_at` String |
| 과금 | `PAY_PER_REQUEST` |
| 암호화 | KMS `alias/wskorea26-dynamodb-key` |
| 삭제 방지 | Enabled |

```bash
aws dynamodb create-table \
  --table-name wskorea26-data-table \
  --attribute-definitions \
    AttributeName=client_id,AttributeType=S \
    AttributeName=concert_name,AttributeType=S \
    AttributeName=created_at,AttributeType=S \
  --key-schema AttributeName=client_id,KeyType=HASH \
  --global-secondary-indexes '[
    {
      "IndexName": "concert-created-index",
      "KeySchema": [
        {"AttributeName": "concert_name", "KeyType": "HASH"},
        {"AttributeName": "created_at", "KeyType": "RANGE"}
      ],
      "Projection": {"ProjectionType": "ALL"}
    }
  ]' \
  --billing-mode PAY_PER_REQUEST \
  --sse-specification Enabled=true,SSEType=KMS,KMSMasterKeyId=alias/wskorea26-dynamodb-key \
  --deletion-protection-enabled
```

확인:

```bash
aws dynamodb describe-table --table-name wskorea26-data-table \
  --query "Table.[TableName,KeySchema[0].[AttributeName,KeyType],DeletionProtectionEnabled,SSEDescription.KMSMasterKeyArn]" \
  --output table
```

## 6. ECR와 book 이미지

### 6.1 Dockerfile

제공 파일은 수정하지 말고 컨테이너에 그대로 넣습니다.

```dockerfile
FROM public.ecr.aws/amazonlinux/amazonlinux:2023
WORKDIR /app
COPY book /app/book
RUN chmod +x /app/book
EXPOSE 8080
CMD ["/app/book"]
```

### 6.2 ECR 생성과 push

```bash
aws ecr create-repository \
  --repository-name wskorea26-book-repo \
  --image-scanning-configuration scanOnPush=true \
  --encryption-configuration encryptionType=KMS,kmsKey=alias/wskorea26-s3-key

aws ecr get-login-password --region ${AWS_REGION} \
  | docker login --username AWS --password-stdin ${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com

docker build -t wskorea26-book-repo:stable .
docker tag wskorea26-book-repo:stable ${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/wskorea26-book-repo:stable
docker push ${ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/wskorea26-book-repo:stable
```

주의: 채점 스크립트는 ECR 암호화 타입만 확인합니다. 그래도 문제 요구사항상 이미지는 KMS로 암호화하는 것이 좋습니다. 별도 ECR KMS 키를 만들면 더 깔끔하지만, 채점 스크립트는 alias까지 확인하지 않습니다.

확인:

```bash
aws ecr describe-repositories \
  --query "repositories[?repositoryName=='wskorea26-book-repo'].[repositoryName,imageScanningConfiguration.scanOnPush,encryptionConfiguration.encryptionType]" \
  --output table

aws ecr describe-images \
  --repository-name wskorea26-book-repo \
  --image-ids imageTag=stable \
  --query "imageDetails[0].imageTags" \
  --output text
```

## 7. EKS Cluster와 NodeGroup

문제 요구사항:

| 항목 | 값 |
|---|---|
| Cluster | `wskorea26-cluster` |
| Version | `1.35` |
| Subnet | `wskorea26-priv-subnet-c`, `wskorea26-priv-subnet-d` |
| Secret encryption | `alias/wskorea26-eks-key` |
| Addon NodeGroup | `wskorea26-addon-ng`, `t3.medium`, tag `Name=wskorea26-addon-node`, label `node-type=addon` |
| App NodeGroup | `wskorea26-app-ng`, `t3.medium`, tag `Name=wskorea26-app-node`, label `node-type=app` |

### 7.1 eksctl 예시

`cluster.yaml`:

```yaml
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: wskorea26-cluster
  region: ap-northeast-2
  version: "1.35"

vpc:
  id: "<VPC_ID>"
  subnets:
    private:
      ap-northeast-2c:
        id: "<PRIV_C_SUBNET_ID>"
      ap-northeast-2d:
        id: "<PRIV_D_SUBNET_ID>"

cloudWatch:
  clusterLogging:
    enableTypes:
      - api
      - audit
      - authenticator
      - controllerManager
      - scheduler

secretsEncryption:
  keyARN: "<EKS_KMS_KEY_ARN>"

iam:
  withOIDC: true

managedNodeGroups:
  - name: wskorea26-addon-ng
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
    privateNetworking: true
    labels:
      node-type: addon
    tags:
      Name: wskorea26-addon-node
    taints:
      - key: dedicated
        value: addon
        effect: NoSchedule

  - name: wskorea26-app-ng
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
    privateNetworking: true
    labels:
      node-type: app
    tags:
      Name: wskorea26-app-node
    taints:
      - key: dedicated
        value: app
        effect: NoSchedule
```

생성:

```bash
eksctl create cluster -f cluster.yaml
aws eks update-kubeconfig --region ap-northeast-2 --name wskorea26-cluster
kubectl create namespace wskorea26
kubectl create namespace monitoring
```

채점 스크립트는 kube-system에서 `aws-node`, `kube-proxy`를 제외한 Pod들이 addon node에 있는지 확인합니다. 따라서 CoreDNS, AWS Load Balancer Controller, Metrics Server, Grafana/Prometheus 계열은 addon node에 배치해야 합니다.

Addon에 배포할 컴포넌트에는 다음을 붙입니다.

```yaml
nodeSelector:
  node-type: addon
tolerations:
  - key: dedicated
    operator: Equal
    value: addon
    effect: NoSchedule
```

Book 앱에는 다음을 붙입니다.

```yaml
nodeSelector:
  node-type: app
tolerations:
  - key: dedicated
    operator: Equal
    value: app
    effect: NoSchedule
```

## 8. IAM 권한 구성

### 8.1 book Pod 권한

book 앱은 DynamoDB에 예약 정보를 저장해야 합니다.

필요 권한:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "dynamodb:PutItem",
        "dynamodb:UpdateItem",
        "dynamodb:GetItem",
        "dynamodb:Query",
        "dynamodb:Scan"
      ],
      "Resource": [
        "arn:aws:dynamodb:ap-northeast-2:<ACCOUNT_ID>:table/wskorea26-data-table",
        "arn:aws:dynamodb:ap-northeast-2:<ACCOUNT_ID>:table/wskorea26-data-table/index/*"
      ]
    }
  ]
}
```

IRSA 또는 EKS Pod Identity를 사용합니다. 대회 환경에서 설정 시간이 부족하면 node role에 임시로 권한을 붙일 수 있지만, 최소 권한 원칙 관점에서는 service account 전용 role이 더 좋습니다.

### 8.2 IRSA 연결 확인

정책을 만들었더라도 ServiceAccount와 IAM Role의 trust policy가 맞지 않으면 Pod 안에서 `AccessDeniedException`이 발생합니다.

```bash
aws eks describe-cluster --name wskorea26-cluster \
  --query 'cluster.identity.oidc.issuer' --output text
kubectl get serviceaccount book-sa -n wskorea26 -o yaml
kubectl logs -n wskorea26 deploy/book --tail=100
kubectl get events -n wskorea26 --sort-by=.lastTimestamp
```

| 오류 | 먼저 확인할 곳 |
|---|---|
| `AccessDeniedException` | IAM policy의 table ARN과 GSI ARN, role trust policy |
| `ResourceNotFoundException` | `TABLE_NAME`, 리전, 실제 테이블 이름 |
| 자격 증명 오류 | ServiceAccount annotation, OIDC provider, Pod Identity 연결 |
| `ImagePullBackOff` | ECR URI/tag `stable`, node role의 ECR pull 권한 |

## 9. Kubernetes book 배포

문제지의 앱 설명:

| 항목 | 값 |
|---|---|
| Env | `AWS_REGION`, `TABLE_NAME` |
| Port | `8080` |
| Health | `/health` |
| POST | `/v1/book` |

`book-deploy.yaml`:

```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: book-sa
  namespace: wskorea26
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/wskorea26-book-pod-role
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: book
  namespace: wskorea26
spec:
  replicas: 2
  selector:
    matchLabels:
      app: book
  template:
    metadata:
      labels:
        app: book
    spec:
      serviceAccountName: book-sa
      nodeSelector:
        node-type: app
      tolerations:
        - key: dedicated
          operator: Equal
          value: app
          effect: NoSchedule
      containers:
        - name: book
          image: <ACCOUNT_ID>.dkr.ecr.ap-northeast-2.amazonaws.com/wskorea26-book-repo:stable
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: AWS_REGION
              value: ap-northeast-2
            - name: TABLE_NAME
              value: wskorea26-data-table
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: book-svc
  namespace: wskorea26
spec:
  type: NodePort
  selector:
    app: book
  ports:
    - name: http
      port: 80
      targetPort: 8080
```

배포:

```bash
kubectl apply -f book-deploy.yaml
kubectl get pod -n wskorea26 -o wide
```

확인:

```bash
kubectl run curl -n wskorea26 --rm -it --image=curlimages/curl --restart=Never -- \
  curl -i http://book-svc/health
```

## 10. Lambda 조회 함수

문제지에서는 Python 3.14를 요구합니다. 계정에서 해당 런타임이 지원되면 `python3.14`를 사용하고, 지원되지 않는 환경에서는 심사 기준에 맞춰 제공 런타임 중 가장 가까운 Python 3.x를 사용해야 합니다.

채점 스크립트는 다음만 직접 확인합니다.

```bash
aws lambda get-function-configuration \
  --function-name wskorea26-book-lambda \
  --query "[FunctionName,Runtime,Environment.Variables.TABLE_NAME]" \
  --output text
```

Lambda는 ALB 이벤트를 받는다고 가정하고 작성합니다. 공개 요청은 `/book?concert_name=...`로 들어오지만 내부 요구사항은 `/reserv-query`입니다. 안전하게 두 경로 모두 처리하게 만들면 좋습니다.

`lambda_function.py`:

```python
import json
import os
from decimal import Decimal

import boto3
from boto3.dynamodb.conditions import Key

REGION = os.environ.get("AWS_REGION", "ap-northeast-2")
TABLE_NAME = os.environ["TABLE_NAME"]
INDEX_NAME = os.environ.get("INDEX_NAME", "concert-created-index")

dynamodb = boto3.resource("dynamodb", region_name=REGION)
table = dynamodb.Table(TABLE_NAME)


def _json_default(value):
    if isinstance(value, Decimal):
        return int(value) if value % 1 == 0 else float(value)
    raise TypeError(type(value).__name__)


def _alb_response(status_code, body):
    return {
        "statusCode": status_code,
        "statusDescription": f"{status_code}",
        "isBase64Encoded": False,
        "headers": {
            "content-type": "application/json; charset=utf-8"
        },
        "body": json.dumps(body, ensure_ascii=False, default=_json_default),
    }


def handler(event, context):
    params = event.get("queryStringParameters") or {}
    concert_name = params.get("concert_name")

    if not concert_name:
        return _alb_response(400, {"error": "concert_name is required"})

    try:
        response = table.query(
            IndexName=INDEX_NAME,
            KeyConditionExpression=Key("concert_name").eq(concert_name),
            ScanIndexForward=False,
        )
        items = response.get("Items", [])
    except Exception:
        # GSI가 없다면 마지막 방어선으로 scan을 사용한다.
        response = table.scan(
            FilterExpression=Key("concert_name").eq(concert_name)
        )
        items = sorted(
            response.get("Items", []),
            key=lambda item: item.get("created_at", ""),
            reverse=True,
        )

    return _alb_response(200, items)
```

배포:

```bash
zip function.zip lambda_function.py

aws lambda create-function \
  --function-name wskorea26-book-lambda \
  --runtime python3.14 \
  --handler lambda_function.handler \
  --zip-file fileb://function.zip \
  --role arn:aws:iam::<ACCOUNT_ID>:role/wskorea26-book-lambda-role \
  --timeout 10 \
  --environment Variables="{AWS_REGION=ap-northeast-2,TABLE_NAME=wskorea26-data-table,INDEX_NAME=concert-created-index}"
```

Lambda role에는 DynamoDB `Query`, `Scan`, CloudWatch Logs 권한을 부여합니다.

## 11. ALB 구성

이 과제의 가장 까다로운 부분입니다.

채점 요구:

1. ALB 이름은 `wskorea26-book-alb`
2. scheme은 `internet-facing`
3. listener는 HTTP 80
4. CloudFront를 통하지 않은 `http://<ALB_DNS>/book` 요청은 `403`
5. CloudFront로 들어온 `/book` POST는 예약 저장
6. CloudFront로 들어온 `/book?concert_name=...` GET은 예약 조회

### 11.0 ALB Controller 선행 확인

Ingress 방식으로 구성하려면 AWS Load Balancer Controller가 먼저 정상 실행되어야 합니다. ALB가 생기지 않는 경우의 대부분은 controller, subnet tag, IAM 권한 문제입니다.

```bash
kubectl get deployment -n kube-system aws-load-balancer-controller
kubectl get pods -n kube-system -l app.kubernetes.io/name=aws-load-balancer-controller -o wide
kubectl logs -n kube-system deploy/aws-load-balancer-controller --tail=100

aws ec2 describe-subnets --subnet-ids ${PUB_C} ${PUB_D} \
  --query 'Subnets[*].[SubnetId,Tags]' --output json
```

정상 기준은 controller Pod가 `Running`, `READY 1/1`이고 공개 subnet 두 개에 `kubernetes.io/role/elb=1` 태그가 있는 것입니다. controller도 addon node에 배치합니다.

Ingress 적용 후에는 ALB가 만들어질 때까지 상태와 이벤트를 확인합니다.

```bash
kubectl apply -f book-ingress.yaml
kubectl get ingress -n wskorea26 -w
kubectl describe ingress book-ingress -n wskorea26
```

ALB가 생기지 않으면 다음 절로 넘어가지 않습니다.

### 11.1 추천 경로 처리 방식

외부 공개 URL은 `/book`으로 유지하고, CloudFront Function에서 내부 경로를 바꿉니다.

| 사용자 요청 | CloudFront Function 내부 rewrite | ALB target |
|---|---|---|
| `POST /book` | `/v1/book` | EKS book service |
| `GET /book?concert_name=...` | `/reserv-query` | Lambda target group |

이렇게 하면 제공 앱의 `/v1/book`, Lambda 요구사항의 `/reserv-query`를 모두 살릴 수 있습니다.

### 11.2 ALB Listener Rule 설계

ALB default action은 403 fixed response로 둡니다.

| Priority | Conditions | Action |
|---|---|---|
| 10 | Header `X-Origin-Verify=wskorea26-cf`, Path `/v1/book`, Method `POST` | EKS target group |
| 20 | Header `X-Origin-Verify=wskorea26-cf`, Path `/reserv-query`, Method `GET` | Lambda target group |
| default | 나머지 | fixed response 403 |

이 구조에서는 CloudFront가 넣어주는 header가 없으면 무조건 403입니다.

### 11.3 Lambda target group

```bash
LAMBDA_ARN=$(aws lambda get-function --function-name wskorea26-book-lambda --query Configuration.FunctionArn --output text)

LAMBDA_TG_ARN=$(aws elbv2 create-target-group \
  --name wskorea26-lambda-tg \
  --target-type lambda \
  --query TargetGroups[0].TargetGroupArn \
  --output text)

aws lambda add-permission \
  --function-name wskorea26-book-lambda \
  --statement-id allow-alb \
  --principal elasticloadbalancing.amazonaws.com \
  --action lambda:InvokeFunction \
  --source-arn ${LAMBDA_TG_ARN}

aws elbv2 register-targets \
  --target-group-arn ${LAMBDA_TG_ARN} \
  --targets Id=${LAMBDA_ARN}
```

### 11.4 EKS target group

AWS Load Balancer Controller를 쓰면 Ingress로 ALB를 만들 수 있습니다. 다만 Lambda target group까지 한 Ingress에서 모두 구성하는 것은 복잡하므로, 시험에서는 다음 중 하나를 선택합니다.

- 방법 A: ALB Controller로 ALB와 앱 target group을 만들고, Lambda rule은 CLI로 추가
- 방법 B: ALB와 target group을 CLI로 직접 만들고, Kubernetes NodePort service를 target으로 등록

시간이 부족하면 방법 A가 더 쉽습니다.

Ingress 예시:

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: book-ingress
  namespace: wskorea26
  annotations:
    alb.ingress.kubernetes.io/load-balancer-name: wskorea26-book-alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/subnets: <PUB_C_SUBNET_ID>,<PUB_D_SUBNET_ID>
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP":80}]'
    alb.ingress.kubernetes.io/conditions.book-svc: >
      [{"field":"http-header","httpHeaderConfig":{"httpHeaderName":"X-Origin-Verify","values":["wskorea26-cf"]}},
       {"field":"http-request-method","httpRequestMethodConfig":{"values":["POST"]}}]
spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
          - path: /v1/book
            pathType: Exact
            backend:
              service:
                name: book-svc
                port:
                  number: 80
```

Ingress 적용 뒤 생성된 ALB ARN과 listener ARN을 확인하고 Lambda rule을 추가합니다.

```bash
ALB_ARN=$(aws elbv2 describe-load-balancers \
  --names wskorea26-book-alb \
  --query "LoadBalancers[0].LoadBalancerArn" \
  --output text)

LISTENER_ARN=$(aws elbv2 describe-listeners \
  --load-balancer-arn ${ALB_ARN} \
  --query "Listeners[0].ListenerArn" \
  --output text)

aws elbv2 create-rule \
  --listener-arn ${LISTENER_ARN} \
  --priority 20 \
  --conditions \
    Field=path-pattern,Values=/reserv-query \
    Field=http-header,HttpHeaderConfig="{HttpHeaderName=X-Origin-Verify,Values=[wskorea26-cf]}" \
    Field=http-request-method,HttpRequestMethodConfig="{Values=[GET]}" \
  --actions Type=forward,TargetGroupArn=${LAMBDA_TG_ARN}
```

Default 403 확인:

```bash
ALB_DNS=$(aws elbv2 describe-load-balancers --names wskorea26-book-alb --query "LoadBalancers[0].DNSName" --output text)
curl -i http://${ALB_DNS}/book
```

기대값은 `HTTP/1.1 403`입니다.

CloudFront를 만들기 전에는 검증 헤더를 넣어 두 target을 각각 검사할 수 있습니다.

```bash
curl -i -X POST \
  -H 'X-Origin-Verify: wskorea26-cf' \
  -H 'Content-Type: application/json' \
  -d '{"client_id":"TEST-001","username":"test","email":"test@example.com","concert_name":"TEST"}' \
  http://${ALB_DNS}/v1/book

curl -i \
  -H 'X-Origin-Verify: wskorea26-cf' \
  "http://${ALB_DNS}/reserv-query?concert_name=TEST"
```

이 테스트가 실패하면 CloudFront가 아니라 ALB rule, target health, 앱 또는 Lambda부터 수정해야 합니다.

## 12. CloudFront

### 12.1 Origin 구성

Origin은 2개를 둡니다.

| Origin ID | Domain | 용도 |
|---|---|---|
| `s3-origin` | S3 bucket regional domain | 정적 페이지 |
| `alb-origin` | ALB DNS | `/book*` API |

CloudFront Distribution의 `Comment`를 반드시 `wskorea26-concert-cf`로 설정합니다. 채점 스크립트는 Comment로 Distribution ID를 찾습니다.

### 12.2 S3 origin

S3는 직접 접근 금지입니다. OAC를 만들고 bucket policy에서 CloudFront distribution만 허용합니다.

Bucket policy 예시:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "AllowCloudFrontServicePrincipalReadOnly",
      "Effect": "Allow",
      "Principal": {
        "Service": "cloudfront.amazonaws.com"
      },
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::wskorea26-concert-bucket-<NO>/web/main/*",
      "Condition": {
        "StringEquals": {
          "AWS:SourceArn": "arn:aws:cloudfront::<ACCOUNT_ID>:distribution/<DISTRIBUTION_ID>"
        }
      }
    }
  ]
}
```

### 12.3 CloudFront Function으로 URI 보정

`/book`을 내부 경로로 바꾸는 viewer-request function 예시입니다.

```javascript
function handler(event) {
  var request = event.request;

  if (request.uri === "/book" || request.uri === "/book/") {
    if (request.method === "GET") {
      request.uri = "/reserv-query";
    } else if (request.method === "POST") {
      request.uri = "/v1/book";
    }
  }

  return request;
}
```

이 함수는 `/book*` cache behavior의 viewer-request 단계에 연결합니다.

### 12.4 Cache Behavior

| Behavior | Origin | 설정 |
|---|---|---|
| Default `*` | S3 | Viewer protocol policy: redirect-to-https, root object: `web/main/index.html` 또는 origin path `/web/main` + default root `index.html` |
| `/book*` | ALB | Viewer protocol policy: redirect-to-https, allowed methods: GET/HEAD/OPTIONS/POST/PUT/PATCH/DELETE |

API behavior는 이전 GET 결과가 재사용되지 않도록 AWS 관리형 `CachingDisabled` cache policy를 사용하는 편이 안전합니다. Query string `concert_name`과 `Content-Type` 헤더가 origin까지 전달되도록 origin request policy도 확인합니다. Query string이 전달되지 않으면 Lambda는 정상이어도 항상 `400`을 반환합니다.

```bash
aws cloudfront wait distribution-deployed --id ${CF_ID}
aws cloudfront get-distribution-config --id ${CF_ID} \
  --query "DistributionConfig.CacheBehaviors.Items[?PathPattern=='/book*'].[AllowedMethods.Items,CachePolicyId,OriginRequestPolicyId,FunctionAssociations]" \
  --output json
```

ALB origin custom header:

| Header | Value |
|---|---|
| `X-Origin-Verify` | `wskorea26-cf` |

S3 origin custom header:

| Header | Value |
|---|---|
| `wskorea26-s3-access` | 자유 값 또는 `true` |

채점 스크립트는 header name/value 목록에서 `X-Origin-Verify`, `wskorea26-cf`, `wskorea26-s3-access`가 보이는지 확인합니다.

### 12.5 정적 페이지 경로 주의

제공 `index.html` 안의 이미지 경로는 `main.jpeg`입니다. CloudFront 루트에서 `index.html`이 보이면 브라우저는 `/main.jpeg`를 요청합니다. 따라서 다음 중 하나로 구성해야 합니다.

- 방법 A: S3 origin path를 `/web/main`으로 두고 default root object를 `index.html`로 설정
- 방법 B: CloudFront Function에서 `/main.jpeg`를 `/web/main/main.jpeg`로 rewrite

채점 스크립트는 `https://$CF_DOMAIN/main.jpeg`를 직접 호출하므로 `/main.jpeg`가 200을 반환해야 합니다.

## 13. Application 동작 테스트

채점 스크립트가 실제로 호출하는 요청입니다.

```bash
CF_ID=$(aws cloudfront list-distributions \
  --query "DistributionList.Items[?Comment=='wskorea26-concert-cf'].Id | [0]" \
  --output text)

CF_DOMAIN=$(aws cloudfront get-distribution \
  --id ${CF_ID} \
  --query "Distribution.DomainName" \
  --output text)

curl -i https://${CF_DOMAIN}/
curl -i http://${CF_DOMAIN}/
curl -i https://${CF_DOMAIN}/main.jpeg
```

POST:

```bash
curl -s -X POST \
  -H 'Content-Type: application/json' \
  -d '{"client_id":"D1114","username":"akaね","email":"akane@ztmy.com","concert_name":"ZUTOMAYO_INTENSE_II"}' \
  https://${CF_DOMAIN}/book
```

기대 응답:

```json
{"booking_id":"..."}
```

GET:

```bash
curl -s -X GET \
  -H 'Content-Type: application/json' \
  "https://${CF_DOMAIN}/book?concert_name=ZUTOMAYO_INTENSE_II"
```

기대 응답:

```json
[
  {
    "client_id": "D1114",
    "username": "akaね",
    "email": "akane@ztmy.com",
    "concert_name": "ZUTOMAYO_INTENSE_II",
    "booking_id": "...",
    "created_at": "..."
  }
]
```

`concert_name`이 없을 때:

```bash
curl -s -o /dev/null -w "%{http_code}\n" \
  -X GET \
  -H 'Content-Type: application/json' \
  "https://${CF_DOMAIN}/book"
```

기대값은 `400`입니다.

## 14. Monitoring

문제 요구사항:

- 클러스터 메트릭 수집
- Pod 로그 CloudWatch Logs 전송
- Grafana 대시보드에서 시각화
- 모든 모니터링 컴포넌트는 addon nodegroup에서 실행

권장 구성:

1. `monitoring` namespace 생성
2. Prometheus 설치
3. Grafana 설치
4. Fluent Bit 또는 CloudWatch Agent 설치
5. Grafana 앞에 internet-facing ALB `wskorea26-grafana-alb` 구성
6. admin user를 `skills-<비번호>-admin`으로 설정
7. admin password를 `$korea26!!`로 설정
8. dashboard 이름을 `wskorea26-monitoring`으로 맞춤

Grafana 설치 예시:

```bash
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm repo add grafana https://grafana.github.io/helm-charts
helm repo update

helm install prometheus prometheus-community/kube-prometheus-stack \
  -n monitoring --create-namespace \
  --set grafana.adminUser='skills-<비번호>-admin' \
  --set grafana.adminPassword='$korea26!!' \
  --set grafana.service.type=ClusterIP \
  --set grafana.nodeSelector.node-type=addon \
  --set prometheus.prometheusSpec.nodeSelector.node-type=addon
```

Addon taint를 줬다면 Helm values에 toleration도 넣어야 합니다.

대시보드 패널:

| 패널 | PromQL 예시 |
|---|---|
| CPU 사용량 | `sum(rate(container_cpu_usage_seconds_total{namespace="wskorea26"}[5m])) by (pod)` |
| Memory 사용량 | `sum(container_memory_working_set_bytes{namespace="wskorea26"}) by (pod)` |
| Running Pod 개수 | `count(kube_pod_status_phase{namespace="wskorea26",phase="Running"})` |
| Restart 횟수 | `sum(kube_pod_container_status_restarts_total{namespace="wskorea26"}) by (pod)` |
| Network RX | `sum(rate(container_network_receive_bytes_total{namespace="wskorea26"}[5m])) by (pod)` |

채점 스크립트가 출력하는 URL:

```bash
GRAFANA_ALB_DNS=$(aws elbv2 describe-load-balancers --names wskorea26-grafana-alb --query "LoadBalancers[0].DNSName" --output text)
echo "URL: http://${GRAFANA_ALB_DNS}"
echo 'Login: skills-<비번호>-admin / $korea26!!'
```

Grafana service는 ClusterIP로 유지하고 AWS Load Balancer Controller Ingress로 `wskorea26-grafana-alb`를 생성합니다. HTTP 80으로 Grafana 로그인 화면이 열려야 하며, dashboard title은 `wskorea26-monitoring`이어야 합니다.

## 15. 채점 스크립트 기준 최종 점검

아래 순서대로 확인하면 `mark.sh`가 확인하는 대부분의 항목을 사전에 잡을 수 있습니다.

### 15.1 Network

```bash
aws ec2 describe-vpcs \
  --filter Name=tag:Name,Values=wskorea26-vpc \
  --query "Vpcs[0].CidrBlock" \
  --output text

aws ec2 describe-subnets \
  --filters "Name=tag:Name,Values=wskorea26-pub-subnet-c,wskorea26-pub-subnet-d,wskorea26-priv-subnet-c,wskorea26-priv-subnet-d" \
  --query "sort_by(Subnets,&Tags[?Key=='Name']|[0].Value)[].[Tags[?Key=='Name']|[0].Value,CidrBlock]" \
  --output table
```

### 15.2 S3

```bash
aws s3api list-objects-v2 \
  --bucket ${BUCKET} \
  --prefix "web/main/" \
  --query "sort(Contents[].Key)" \
  --output text

aws s3api get-public-access-block \
  --bucket ${BUCKET} \
  --query "PublicAccessBlockConfiguration.[BlockPublicAcls,IgnorePublicAcls,BlockPublicPolicy,RestrictPublicBuckets]" \
  --output text

aws s3api get-bucket-policy-status \
  --bucket ${BUCKET} \
  --query "PolicyStatus.IsPublic" \
  --output text
```

### 15.3 EKS

```bash
aws eks describe-cluster --name wskorea26-cluster \
  --query "cluster.[name,version]" \
  --output text

kubectl get namespace wskorea26

kubectl get pod -n kube-system -o wide
kubectl get pod -n wskorea26 -o wide
kubectl get nodes --show-labels | grep node-type
```

### 15.4 ALB와 CloudFront

```bash
ALB_DNS=$(aws elbv2 describe-load-balancers \
  --names wskorea26-book-alb \
  --query "LoadBalancers[0].DNSName" \
  --output text)

curl -o /dev/null -s -w "%{http_code}\n" http://${ALB_DNS}/book
```

기대값은 `403`입니다.

```bash
CF_ID=$(aws cloudfront list-distributions \
  --query "DistributionList.Items[?Comment=='wskorea26-concert-cf'].Id | [0]" \
  --output text)

CF_DOMAIN=$(aws cloudfront get-distribution \
  --id ${CF_ID} \
  --query "Distribution.DomainName" \
  --output text)

curl -o /dev/null -s -w "%{http_code}\n" https://${CF_DOMAIN}
curl -o /dev/null -s -w "%{http_code}\n" http://${CF_DOMAIN}/
curl -o /dev/null -s -w "status: %{http_code}, size: %{size_download} bytes\n" https://${CF_DOMAIN}/main.jpeg
```

기대값:

- HTTPS root: `200`
- HTTP root: `301` 또는 `302`
- main.jpeg: `200`, size가 0보다 큼

## 16. 자주 틀리는 부분

| 실수 | 결과 | 해결 |
|---|---|---|
| CloudFront Comment를 안 넣음 | `CF_ID`가 `None` | Distribution comment를 `wskorea26-concert-cf`로 설정 |
| S3 객체를 루트에 업로드 | `web/main/` 조회 실패 | `web/main/index.html`, `web/main/main.jpeg`로 업로드 |
| `/main.jpeg`가 404 | 정적 페이지 이미지 채점 실패 | origin path 또는 Function rewrite로 `/main.jpeg` 처리 |
| ALB default가 앱 forward | 직접 ALB 접근이 200 | default action을 403으로 변경 |
| CloudFront custom header 누락 | ALB rule 미매칭 | ALB origin에 `X-Origin-Verify: wskorea26-cf` 추가 |
| Lambda가 `/book` GET을 처리 못함 | 조회 API 실패 | CloudFront Function rewrite 또는 Lambda에서 `/book`도 허용 |
| Pod가 addon node에 올라감 | pod 분리 채점 실패 | book deployment에 `nodeSelector: node-type: app` |
| CoreDNS가 app node에 올라감 | addon 분리 채점 실패 | CoreDNS deployment에 addon nodeSelector/toleration 적용 |
| DynamoDB KMS alias 불일치 | KMS 채점 실패 | 테이블 생성 시 `alias/wskorea26-dynamodb-key` 사용 |
| Grafana ALB 이름 또는 계정이 다름 | 수동 채점 접속 실패 | ALB `wskorea26-grafana-alb`, `skills-<비번호>-admin` / `$korea26!!` 확인 |

## 17. 최종 제출 전 순서

1. `mark.sh`를 실행하기 전에 CloudFront 배포 상태가 `Deployed`인지 확인합니다.
2. ECR image scan이 완료되었는지 확인합니다.
3. `kubectl get pod -A`에서 CrashLoopBackOff가 없어야 합니다.
4. `curl http://<ALB_DNS>/book`이 403인지 확인합니다.
5. `curl https://<CF_DOMAIN>/book` POST와 GET을 직접 실행합니다.
6. Grafana URL이 열리는지 확인합니다.
7. 테스트 부하나 임시 Pod를 지웁니다.
8. 과제 종료 후에는 리소스를 수정하지 않습니다.

## 18. 증상별 빠른 복구표

| 증상 | 원인 분리용 명령 | 자주 발생하는 원인 |
|---|---|---|
| Pod가 `Pending` | `kubectl describe pod -n wskorea26 <POD>` | taint와 toleration 불일치, nodeSelector 오타 |
| Pod가 `ImagePullBackOff` | `kubectl describe pod -n wskorea26 <POD>` | ECR URI 또는 `stable` tag 누락, node IAM 권한 |
| ALB가 생성되지 않음 | `kubectl describe ingress -n wskorea26 book-ingress` | controller 오류, subnet tag 누락 |
| Target이 unhealthy | `aws elbv2 describe-target-health --target-group-arn <ARN>` | health path/port 오류, security group, readiness 실패 |
| ALB 직접 접속이 200 | `aws elbv2 describe-rules --listener-arn ${LISTENER_ARN}` | default action이 forward로 설정됨 |
| CloudFront root가 403 | `aws s3api get-bucket-policy --bucket ${BUCKET}` | OAC policy의 distribution ARN 또는 object ARN 오류 |
| `/main.jpeg`가 404 | `curl -I https://${CF_DOMAIN}/main.jpeg` | origin path 또는 URI rewrite 누락 |
| POST가 403 | ALB listener rule 확인 | custom header 누락, path rewrite 미적용 |
| GET이 400 | Lambda log와 CloudFront policy 확인 | query string `concert_name` 미전달 |
| GET이 502 | Lambda log 확인 | Lambda 응답 형식 또는 권한 오류 |
| 최신 예약순이 아님 | GSI와 `ScanIndexForward` 확인 | GSI 누락, `created_at` 정렬키 미사용 |
| Grafana URL이 비어 있음 | `kubectl describe svc grafana -n monitoring` | LoadBalancer provisioning 대기 또는 service type 오류 |

### 계층별 진단 순서

1. `kubectl logs`와 `book-svc/health`로 애플리케이션 확인
2. DynamoDB 직접 조회로 데이터 저장 확인
3. ALB에 검증 헤더를 넣어 POST/GET target 확인
4. CloudFront domain에서 `/`, `/main.jpeg`, `/book` 확인
5. 마지막에 `mark.sh` 전체 실행

이 순서를 지키면 “CloudFront 문제처럼 보였지만 실제로는 Pod 권한 문제”인 상황을 빠르게 구분할 수 있습니다.

## 19. 제출 직전 한 화면 점검 명령

```bash
echo '[identity]'
aws sts get-caller-identity --query '[Account,Arn]' --output text
echo '[eks nodes]'
kubectl get nodes -L node-type
echo '[pods]'
kubectl get pods -A -o wide
echo '[alb]'
curl -s -o /dev/null -w 'direct ALB: %{http_code}\n' "http://${ALB_DNS}/book"
echo '[cloudfront]'
curl -s -o /dev/null -w 'root: %{http_code}\n' "https://${CF_DOMAIN}/"
curl -s -o /dev/null -w 'image: %{http_code}, %{size_download} bytes\n' "https://${CF_DOMAIN}/main.jpeg"
echo '[resource status]'
aws eks describe-cluster --name wskorea26-cluster --query 'cluster.status' --output text
aws cloudfront get-distribution --id ${CF_ID} --query 'Distribution.Status' --output text
aws dynamodb describe-table --table-name wskorea26-data-table --query 'Table.TableStatus' --output text
```

기대값은 node `Ready`, 모든 핵심 Pod `Running`, ALB 직접 접근 `403`, CloudFront root/image `200`, EKS `ACTIVE`, CloudFront `Deployed`, DynamoDB `ACTIVE`입니다.
