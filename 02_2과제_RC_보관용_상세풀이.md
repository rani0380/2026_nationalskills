# 제2과제 Release Candidate 콘솔 + CLI 상세 풀이

기준: `day2-release-candidate-tp.pdf` + `day2-release-candidate-marking.pdf` (22.5점, 4시간)

최신 Release Candidate 제2과제는 3개의 독립 모듈로 구성됩니다. 모듈마다 리전이 다르므로, 작업 전마다 `aws configure set region ...`을 다시 설정해야 합니다.

| 모듈 | 주제 | 리전 | 핵심 서비스 |
|---|---|---|---|
| 1 | Workflow | `ap-southeast-1` | S3, Lambda, DynamoDB, Step Functions |
| 2 | Real-time data analytics | `ap-northeast-2` | VPC, EC2, ALB, Kinesis Data Streams, Managed Flink |
| 3 | MSK | `ap-northeast-1` | VPC, MSK, EC2 Producer, Lambda Consumer, DynamoDB, S3, SNS |

## 0. 공통 채점 대비

채점 스크립트는 대부분 리소스 이름으로 조회합니다. 이름이 조금이라도 다르면 실제 구성이 맞아도 실패합니다.

```bash
aws sts get-caller-identity
export ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
export NUM=<비번호>
```

주의할 점:

- Bastion 또는 채점용 접근 경로가 끊기지 않게 합니다.
- 각 모듈의 리전을 섞지 않습니다.
- 테스트용 부하, 임시 파일, 임시 데이터는 채점 전 정리합니다.
- Lambda Runtime은 Workflow가 Python 3.12, MSK의 두 Consumer가 Python 3.14입니다.

---


### 최신 RC 적용 원칙

- Cloud Event Handling은 문제 오류로 삭제되었습니다. 구축·채점하지 않습니다.
- 채점표 기준 총점은 Workflow 7.5점, Analytics 7.5점, MSK 7.5점으로 22.5점입니다.
- 아래의 1-1~1-6, 2-1~2-7, 3-1~3-6 검증 명령을 최종 기준으로 사용합니다.

## 콘솔 중심 전체 구축 순서

이 절은 AWS Management Console을 기준으로 위에서 아래 순서대로 진행합니다. 명령이 꼭 필요한 단계만 **필수 터미널 작업**으로 표시했습니다. 각 Module은 Region이 다르므로 서비스 화면을 열기 전에 우측 상단 Region부터 확인합니다.

### 시작 전 공통 준비

1. CloudShell을 열고 `aws sts get-caller-identity --query Account --output text`로 본인 계정을 확인합니다.
2. 비번호를 기록하고 Module별 Bucket 이름에 정확히 사용합니다.
3. IAM Role은 먼저 만든 뒤 서비스 리소스를 생성합니다. Trust Entity와 최소 권한을 혼동하지 않습니다.
4. Module을 바꿀 때마다 Console Region과 CloudShell Region을 함께 바꿉니다.
5. 리소스 이름은 대소문자까지 표와 동일하게 입력합니다.

| 순서 | Module | Region |
|---:|---|---|
| 1 | Workflow | ap-southeast-1 |
| 2 | Real-time Data Analytics | ap-northeast-2 |
| 3 | MSK | ap-northeast-1 |

### 콘솔 순서 1: Workflow

#### 1-A. S3와 DynamoDB

1. Region을 Singapore(`ap-southeast-1`)로 변경합니다.
2. S3 -> Create bucket에서 `wsc2026-student-score-bucket-<비번호>`를 만듭니다.
3. 버킷에서 Create folder를 눌러 `input`, `processed`, `error`를 각각 만듭니다.
4. DynamoDB -> Tables -> Create table로 이동합니다.
5. Table name은 `wsc2026-student-score`, Partition key는 `studentId` String, Sort key는 `examDate` String으로 지정합니다.
6. Table settings는 Customize settings, Capacity mode는 On-demand를 선택합니다.

정상 확인: S3에는 세 Prefix만 있고, DynamoDB Key schema에는 HASH `studentId`와 RANGE `examDate`가 표시되어야 합니다.

#### 1-B. IAM Role 두 개

1. IAM -> Roles -> Create role -> AWS service -> Lambda를 선택하고 `wsc2026-lambda-student-role`을 만듭니다.
2. 이 Role에는 대상 버킷의 GetObject/PutObject/DeleteObject, 테이블 PutItem, 로그 기록, Step Functions 실행에 실제 필요한 권한만 부여합니다.
3. 다시 Create role -> AWS service -> Step Functions를 선택하고 `wsc2026-stepfunction-student-role`을 만듭니다.
4. State Machine Role에는 지정 Lambda InvokeFunction과 필요한 S3 작업만 허용합니다.
5. Resource를 가능하면 정확한 Bucket, Prefix, Table, Function ARN으로 제한합니다.

#### 1-C. Processing Lambda

1. Lambda -> Create function -> Author from scratch를 선택합니다.
2. Function name은 `wsc2026-student-score-function`, Runtime은 Python 3.12, Execution role은 기존 `wsc2026-lambda-student-role`을 선택합니다.
3. Configuration -> Environment variables에 `S3_BUCKET`과 `DDB_TABLE=wsc2026-student-score`를 입력합니다.
4. Code 탭에 아래 Module 1의 완성 코드를 붙여넣고 Deploy를 누릅니다.
5. Test Event로 Bucket과 `input/test.csv` Key를 전달해 문법·권한 오류를 먼저 확인합니다.

#### 1-D. Step Functions와 S3 Trigger

1. Step Functions -> Create state machine -> Write your workflow in code를 선택합니다.
2. Type은 반드시 Standard, Name은 `wsc2026-student-score-workflow`로 지정합니다.
3. 아래 1.7의 ASL Definition을 붙여넣고 기존 Role `wsc2026-stepfunction-student-role`을 선택합니다.
4. 아래 1.8의 Trigger Lambda를 별도로 만들고 State Machine ARN 환경변수를 넣습니다.
5. S3 버킷 -> Properties -> Event notifications -> Create event notification으로 이동합니다.
6. Prefix `input/`, Suffix `.csv`, Event type All object create events, Destination은 Trigger Lambda로 설정합니다.

#### 1-E. Workflow 채점 준비

1. `processed/`, `error/`의 이전 결과와 DynamoDB Item을 모두 정리합니다.
2. 지급된 `test.csv`를 정확히 `input/test.csv`로 업로드합니다.
3. Step Functions Execution이 Succeeded인지 확인합니다.
4. DynamoDB에서 STU1020의 average `96.6`, grade `A`를 확인합니다.
5. S3에서 `processed/test.csv`와 오류 JSON 4개만 존재하는지 확인합니다.

### 콘솔 순서 2: Real-time Data Analytics

#### 2-A. VPC

1. Region을 Seoul(`ap-northeast-2`)로 변경합니다.
2. VPC -> Create VPC -> VPC and more 또는 수동 생성을 선택합니다.
3. `analytics-vpc`(`10.20.0.0/16`)와 Public a/b, Private a/b를 과제 CIDR대로 만듭니다.
4. Public Route Table은 `analytics-pub-rtb`로 통합하고 IGW `analytics-igw`를 기본 경로로 지정합니다.
5. NAT Gateway `analytics-ngw`를 Public Subnet에 만들고 두 Private Route Table의 기본 경로로 연결합니다.

#### 2-B. IAM과 Private EC2

1. IAM에서 EC2 Trust Role `wsc2026-analytics-ec2-role`을 만듭니다. RC 문제지의 정확한 이름 `analytics`를 사용합니다.
2. SSM 접속 권한과 `wsc2026-order-stream` PutRecord 권한을 부여합니다.
3. EC2 -> Launch instance에서 Name `wsc2026-analytics-ec2`, Type `t3.small`, Private Subnet `analytics-priv-a`, Public IP Disabled로 생성합니다.
4. Security Group은 ALB Security Group에서 들어오는 Application Port 5000만 허용합니다.
5. Systems Manager -> Fleet Manager 또는 Session Manager에서 Managed node와 연결 성공 여부를 확인합니다.

#### 2-C. Kinesis

1. Kinesis -> Data streams -> Create data stream으로 이동합니다.
2. Name은 `wsc2026-order-stream`, Capacity mode는 On-demand로 지정합니다.
3. Status가 Active가 될 때까지 기다립니다.

#### 2-D. 애플리케이션과 systemd

1. Session Manager로 EC2에 접속합니다.
2. 지급 Application을 설치하고 Stream 이름과 Region을 설정합니다.
3. systemd Unit 이름을 정확히 `app`으로 저장합니다.
4. **필수 터미널 작업:** `sudo systemctl enable --now app`을 실행합니다.
5. `systemctl is-active app`과 `systemctl is-enabled app`이 각각 `active`, `enabled`인지 확인합니다.

#### 2-E. Target Group과 ALB

1. EC2 -> Target Groups -> Create target group에서 Instance 유형, Name `wsc2026-analytics-tg`, Port 5000을 지정합니다.
2. Health check path는 `/health`로 설정하고 Analytics EC2를 등록합니다.
3. Load Balancers -> Create Application Load Balancer로 `wsc2026-analytics-alb`를 Internet-facing, Public Subnet a/b에 만듭니다.
4. Listener HTTP:80을 `wsc2026-analytics-tg`로 전달합니다.
5. Target Health가 Healthy인지 확인한 뒤 `/health`와 POST `/order`를 시험합니다.

#### 2-F. Managed Apache Flink Studio

1. Managed Service for Apache Flink -> Studio notebooks -> Create Studio notebook으로 이동합니다.
2. Name은 `wsc2026-analytics-flink`, Runtime은 과제의 Apache Flink 1.19에 대응하는 `ZEPPELIN-FLINK-3_0`을 선택합니다.
3. IAM Role은 `wsc2026-analytics-flink-role`을 사용하고 Stream Describe/GetRecords/GetShardIterator/ListShards 권한만 부여합니다.
4. Notebook을 Run한 뒤 아래 2.7의 Source Table SQL을 먼저 실행합니다.
5. `SELECT * FROM order_stream`으로 데이터 유입을 확인한 다음 1분 주문 수와 상품별 매출 SQL을 각각 실행합니다.
6. 채점 전 Application status가 READY인지 확인합니다. Flink 애플리케이션 코드를 별도로 작성하면 안 됩니다.

### 콘솔 순서 3: MSK

#### 3-A. VPC와 저장소

1. Region을 Tokyo(`ap-northeast-1`)로 변경합니다.
2. `msk-vpc`와 Public a/d, Private a/d Subnet을 문제지 CIDR대로 만듭니다.
3. `msk-igw`, `msk-pub-rtb`, `msk-ngw`, Private Route Table a/d를 연결합니다.
4. DynamoDB `wsc2026-sensor-data`를 PK `sensorId` String, SK `timestamp` String으로 만듭니다.
5. S3 `wsc2026-sensor-alert-bucket-<비번호>`와 SNS Standard Topic `wsc2026-sensor-alert`을 만듭니다.

#### 3-B. IAM과 MSK Cluster

1. EC2 Trust Role `wsc2026-msk-ec2-role`을 만들고 필요한 Kafka Topic Write 권한과 SSM 권한만 부여합니다.
2. Lambda Trust Role `wsc2026-msk-lambda-role`을 만들고 Kafka Read/Write, DynamoDB PutItem, SNS Publish, S3 PutObject, VPC ENI, Logs 권한을 필요한 리소스로 제한합니다.
3. Amazon MSK -> Create cluster에서 Provisioned Cluster `wsc2026-msk-cluster`를 만듭니다.
4. Kafka version `3.6.0`, Broker `kafka.t3.small`, Private Subnet a/d를 선택합니다.
5. Client authentication은 IAM만 활성화하고 Public access는 비활성화합니다.
6. Cluster가 Active가 될 때까지 기다린 뒤 View client information에서 IAM Bootstrap server를 복사합니다.

#### 3-C. Producer와 Kafka Topic

1. EC2 `wsc2026-sensor-producer`, `t3.small`, Private Subnet, Public IP Disabled, Role `wsc2026-msk-ec2-role`로 생성합니다.
2. MSK SG는 Producer와 Lambda SG에서 들어오는 IAM SASL Broker Port를 허용합니다.
3. Session Manager로 Producer에 접속하고 Kafka CLI와 AWS MSK IAM Auth Library를 준비합니다.
4. **필수 터미널 작업:** Console에서는 Kafka Topic을 만들 수 없으므로 아래 3.6 명령으로 raw Topic(3/2)과 alert Topic(1/2)을 생성합니다.
5. 지급 Producer를 설치하고 systemd로 활성화한 뒤 매 메시지마다 실제 Timestamp가 생성되는지 로그에서 확인합니다.

#### 3-D. Lambda Consumer와 Trigger

1. Lambda -> Create function에서 `wsc2026-sensor-consumer`와 `wsc2026-sensor-alert-consumer`를 Python 3.14로 만듭니다.
2. 두 함수 모두 기존 `wsc2026-msk-lambda-role`을 연결합니다.
3. Sensor Consumer에는 DynamoDB Table, Alert Topic, Bootstrap Server 환경변수를 넣습니다.
4. Alert Consumer에는 SNS Topic ARN과 S3 Bucket 환경변수를 넣습니다.
5. Add trigger -> MSK에서 Cluster와 Topic을 각각 raw/alert로 선택하고 Private Subnet a/d, Lambda SG, Starting position LATEST를 지정합니다.
6. Event source mapping 두 개가 Enabled가 될 때까지 기다립니다.

#### 3-E. MSK 채점 확인

1. Producer 로그에서 실제 현재 시각 형식 `YYYY-MM-DDTHH:mm:ss±HH:mm`을 확인합니다.
2. DynamoDB Explore items에서 `sensorId`, `timestamp`, `temperature`, `status`가 채워졌는지 확인합니다.
3. 3-6은 샘플 문자열이 아니라 실제 Timestamp가 저장된 Item 최대 3개를 확인합니다.
4. 이상 데이터가 발생하면 SNS Publish와 S3 `alert/{sensorId}/{date}/{timestamp}.json` 저장을 확인합니다.
5. 마지막으로 아래 Module 3 채점 명령을 실행해 Runtime, MSK IAM, Trigger Enabled, Producer Data를 확인합니다.

## Module 1. Workflow

### 1.1 목표

학생 성적 CSV가 S3 `input/` 경로에 업로드되면 Step Functions가 실행되고, Lambda가 CSV를 검증하여 정상 데이터는 DynamoDB에 저장하고 오류 데이터는 S3 `error/`에 JSON으로 저장합니다. 처리 후 원본 CSV는 `processed/`로 이동합니다.

### 1.2 필수 리소스

| 항목 | 값 |
|---|---|
| Region | `ap-southeast-1` |
| S3 Bucket | `wsc2026-student-score-bucket-<비번호>` |
| Prefix | `input/`, `processed/`, `error/` |
| DynamoDB Table | `wsc2026-student-score` |
| Key | PK `studentId`, SK `examDate` |
| Processing Lambda | `wsc2026-student-score-function` |
| Runtime | `python3.12` |
| Lambda Env | `S3_BUCKET`, `DDB_TABLE` |
| Trigger Lambda | 선수 직접 작성 |
| Step Functions | `wsc2026-student-score-workflow`, `STANDARD` |
| Lambda Role | `wsc2026-lambda-student-role` |
| Step Functions Role | `wsc2026-stepfunction-student-role` |

### 1.3 S3 버킷과 폴더

```bash
aws configure set region ap-southeast-1
export BUCKET_NAME=wsc2026-student-score-bucket-${NUM}

aws s3api create-bucket \
  --bucket ${BUCKET_NAME} \
  --region ap-southeast-1 \
  --create-bucket-configuration LocationConstraint=ap-southeast-1

aws s3api put-object --bucket ${BUCKET_NAME} --key input/
aws s3api put-object --bucket ${BUCKET_NAME} --key processed/
aws s3api put-object --bucket ${BUCKET_NAME} --key error/
```

채점 전에는 `processed/`, `error/`가 비어 있어야 하며, 제공된 `test.csv`를 `input/test.csv`에 업로드합니다.

```bash
aws s3 rm s3://${BUCKET_NAME}/processed/ --recursive
aws s3 rm s3://${BUCKET_NAME}/error/ --recursive
aws s3 cp test.csv s3://${BUCKET_NAME}/input/test.csv
```

### 1.4 DynamoDB

```bash
aws dynamodb create-table \
  --table-name wsc2026-student-score \
  --attribute-definitions AttributeName=studentId,AttributeType=S AttributeName=examDate,AttributeType=S \
  --key-schema AttributeName=studentId,KeyType=HASH AttributeName=examDate,KeyType=RANGE \
  --billing-mode PAY_PER_REQUEST
```

채점 전 데이터 정리:

```bash
aws dynamodb scan --table-name wsc2026-student-score \
  --projection-expression "studentId, examDate" \
  --query "Items[]" --output json
```

데이터가 남아 있으면 키를 기준으로 삭제합니다.

### 1.5 성적 처리 Lambda TODO 완성

제공 `lambda-function.py`의 `calculate_grade`, `save_student`를 채워야 합니다.

핵심 구현:

```python
def calculate_grade(average):
    if average >= 90:
        return "A"
    if average >= 80:
        return "B"
    if average >= 70:
        return "C"
    if average >= 60:
        return "D"
    return "F"


def save_student(table, row):
    scores = [int(row[field].strip()) for field in SCORE_FIELDS]
    average = Decimal(str(round(sum(scores) / len(scores), 1)))
    grade = calculate_grade(float(average))
    now = datetime.now(timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ")

    table.put_item(
        Item={
            "studentId": row["studentId"].strip(),
            "examDate": row["examDate"].strip(),
            "name": row["name"].strip(),
            "className": row["className"].strip(),
            "korean": int(row["korean"]),
            "english": int(row["english"]),
            "math": int(row["math"]),
            "science": int(row["science"]),
            "history": int(row["history"]),
            "average": average,
            "grade": grade,
            "createdAt": now,
        }
    )
```

`STU1020`은 `100, 98, 92, 97, 96`이므로 평균 `96.6`, 등급 `A`가 나와야 합니다. 채점 스크립트가 이 값을 직접 조회합니다.

### 1.6 Lambda 배포

```bash
zip function.zip lambda-function.py

aws lambda create-function \
  --function-name wsc2026-student-score-function \
  --runtime python3.12 \
  --handler lambda-function.handler \
  --zip-file fileb://function.zip \
  --role arn:aws:iam::${ACCOUNT_ID}:role/wsc2026-lambda-student-role \
  --timeout 30 \
  --environment Variables="{S3_BUCKET=${BUCKET_NAME},DDB_TABLE=wsc2026-student-score}"
```

함수명 파일에 하이픈이 있으면 Python import가 불편하므로 실제 배포 시에는 `index.py`로 이름을 바꾸고 `--handler index.handler`를 쓰는 편이 안전합니다.

### 1.7 Step Functions 정의

State Machine 이름은 `wsc2026-student-score-workflow`, 타입은 `STANDARD`입니다.

흐름:

1. `CheckS3File`: S3 HeadObject
2. `ProcessStudentData`: Lambda invoke
3. `CheckResult`: `statusCode == 200`이면 processed 이동
4. `MoveToProcessed`: `input/test.csv`를 `processed/test.csv`로 복사 후 원본 삭제
5. 실패 시 `MoveToError`

State Machine 정의의 핵심 형태:

```json
{
  "StartAt": "CheckS3File",
  "States": {
    "CheckS3File": {
      "Type": "Task",
      "Resource": "arn:aws:states:::aws-sdk:s3:headObject",
      "Parameters": {
        "Bucket": "wsc2026-student-score-bucket-<비번호>",
        "Key.$": "$.key"
      },
      "Next": "ProcessStudentData"
    },
    "ProcessStudentData": {
      "Type": "Task",
      "Resource": "arn:aws:states:::lambda:invoke",
      "Parameters": {
        "FunctionName": "wsc2026-student-score-function",
        "Payload.$": "$"
      },
      "OutputPath": "$.Payload",
      "Retry": [
        {
          "ErrorEquals": ["Lambda.ServiceException", "Lambda.AWSLambdaException"],
          "IntervalSeconds": 2,
          "MaxAttempts": 3,
          "BackoffRate": 2
        }
      ],
      "Next": "CheckResult"
    },
    "CheckResult": {
      "Type": "Choice",
      "Choices": [
        {
          "Variable": "$.statusCode",
          "NumericEquals": 200,
          "Next": "MoveToProcessed"
        }
      ],
      "Default": "MoveToError"
    }
  }
}
```

Copy/Delete 작업은 `arn:aws:states:::aws-sdk:s3:copyObject`, `deleteObject`를 사용합니다. Step Functions role에는 S3와 Lambda invoke 권한이 필요합니다.

### 1.8 S3 Trigger Lambda

S3 `input/*.csv` 업로드 이벤트를 받아 Step Functions를 시작합니다.

```python
import json
import os
import urllib.parse
import boto3

sf = boto3.client("stepfunctions")
STATE_MACHINE_ARN = os.environ["STATE_MACHINE_ARN"]


def handler(event, context):
    for record in event.get("Records", []):
        key = urllib.parse.unquote_plus(record["s3"]["object"]["key"])
        if not key.startswith("input/") or not key.endswith(".csv"):
            continue
        sf.start_execution(
            stateMachineArn=STATE_MACHINE_ARN,
            input=json.dumps({"key": key}),
        )
    return {"started": True}
```

S3 notification:

- Event: `s3:ObjectCreated:*`
- Prefix: `input/`
- Suffix: `.csv`

### 1.9 채점 확인

```bash
aws dynamodb get-item \
  --table-name wsc2026-student-score \
  --key '{"studentId":{"S":"STU1020"},"examDate":{"S":"2026-05-30"}}' \
  --query "Item.[studentId.S,average.N,grade.S]" \
  --output text

aws s3 ls s3://${BUCKET_NAME}/processed/test.csv
aws s3 ls s3://${BUCKET_NAME}/error/ | grep "error_"
```

기대값:

- `STU1020 96.6 A`
- `processed/test.csv` 존재
- `error/error_...json` 존재

---

## Module 2. Real-time Data Analytics

### 2.1 목표

Private subnet의 EC2에서 Flask 앱을 systemd 서비스로 실행합니다. ALB가 `/order`, `/health` 요청을 EC2로 전달하고, 앱은 주문 로그를 Kinesis Data Stream에 기록합니다. Managed Apache Flink Studio Notebook에서 SQL로 실시간 분석합니다.

### 2.2 필수 리소스

| 항목 | 값 |
|---|---|
| Region | `ap-northeast-2` |
| VPC | `analytics-vpc`, `10.20.0.0/16` |
| EC2 | `wsc2026-analytics-ec2`, `t3.small`, private subnet |
| EC2 Role | `wsc2026-analytics-ec2-role` |
| ALB | `wsc2026-analytics-alb`, HTTP 80 |
| Target Group | `wsc2026-analytics-tg`, port `5000` |
| Kinesis Stream | `wsc2026-order-stream`, ON_DEMAND |
| Flink App | `wsc2026-analytics-flink`, Apache Flink 1.19 |
| Flink Role | `wsc2026-analytics-flink-role` |

RC 문제지 기준 EC2 Role 이름은 `wsc2026-analytics-ec2-role`입니다.

### 2.3 네트워크

| Subnet | CIDR | Route |
|---|---|---|
| `analytics-pub-a` | `10.20.0.0/24` | IGW |
| `analytics-pub-b` | `10.20.1.0/24` | IGW |
| `analytics-priv-a` | `10.20.100.0/24` | NAT |
| `analytics-priv-b` | `10.20.101.0/24` | NAT |

EC2는 private subnet에 배치해야 합니다. 채점 스크립트는 EC2가 속한 subnet의 Name 태그를 확인합니다.

### 2.4 Kinesis Data Stream

```bash
aws configure set region ap-northeast-2

aws kinesis create-stream \
  --stream-name wsc2026-order-stream \
  --stream-mode-details StreamMode=ON_DEMAND

aws kinesis wait stream-exists --stream-name wsc2026-order-stream
```

확인:

```bash
aws kinesis describe-stream-summary \
  --stream-name wsc2026-order-stream \
  --query "StreamDescriptionSummary.[StreamName,StreamStatus,StreamModeDetails.StreamMode]" \
  --output text
```

기대값: `wsc2026-order-stream ACTIVE ON_DEMAND`

### 2.5 EC2 앱 배포

제공 앱 환경변수:

| 변수 | 값 |
|---|---|
| `STREAM_NAME` | `wsc2026-order-stream` |
| `AWS_REGION` | `ap-northeast-2` |

설치 예시:

```bash
sudo mkdir -p /opt/app
sudo cp app.py requirements.txt /opt/app/
cd /opt/app
sudo python3 -m venv venv
sudo /opt/app/venv/bin/pip install -r requirements.txt
```

systemd 서비스:

```ini
[Unit]
Description=WSC2026 Analytics App
After=network-online.target

[Service]
WorkingDirectory=/opt/app
Environment=STREAM_NAME=wsc2026-order-stream
Environment=AWS_REGION=ap-northeast-2
ExecStart=/opt/app/venv/bin/gunicorn -b 0.0.0.0:5000 app:app
Restart=always
User=ec2-user

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now app
sudo systemctl status app
```

EC2 role에는 Kinesis `PutRecord`, `PutRecords` 권한이 필요합니다.

### 2.6 ALB

ALB는 public subnet, EC2 target은 private subnet입니다.

| 항목 | 값 |
|---|---|
| ALB | `wsc2026-analytics-alb` |
| Listener | HTTP 80 |
| Target Group | `wsc2026-analytics-tg` |
| Target Group Port | `5000` |
| Health Check | `/health` |

채점 스크립트는 target group 이름과 port를 직접 확인합니다.

### 2.7 Managed Apache Flink

Flink Studio Notebook:

| 항목 | 값 |
|---|---|
| Application Name | `wsc2026-analytics-flink` |
| Runtime | `ZEPPELIN-FLINK-3_0` 또는 Flink 1.19 계열 |
| Status | `READY` |

Notebook에서 실행해야 하는 SQL:

```sql
SELECT COUNT(*) as order_count
FROM order_stream
WHERE event_time > CURRENT_TIMESTAMP - INTERVAL '1' MINUTE;
```

```sql
SELECT product_name, SUM(price * quantity) as total_revenue
FROM order_stream
GROUP BY product_name;
```

채점은 애플리케이션 상태와 런타임을 봅니다.

```bash
aws kinesisanalyticsv2 describe-application \
  --application-name wsc2026-analytics-flink \
  --query "ApplicationDetail.[ApplicationName,ApplicationStatus,RuntimeEnvironment]" \
  --output text
```

### 2.8 채점 확인

```bash
ALB_DNS=$(aws elbv2 describe-load-balancers \
  --names wsc2026-analytics-alb \
  --query "LoadBalancers[0].DNSName" \
  --output text)

curl -s http://${ALB_DNS}/health
curl -s -X POST http://${ALB_DNS}/order
```

Systemd 확인은 SSM으로 수행됩니다.

```bash
systemctl is-active app
systemctl is-enabled app
```

---

## Module 3. MSK


### 3.0 최신 공식 질의 답변 적용

- **채점기준 3-6:** `wsc2026-sensor-data`에서 최대 3개 Item의 `sensorId`와 `timestamp`를 조회합니다. `(timestamp)`라는 문자가 아니라 각 센서 메시지의 실제 생성 시각을 저장합니다.
- **Timestamp:** 문서의 `(timestamp)`는 고정 문자열이나 형식 예시가 아니라 메시지가 만들어진 **실제 시각**이어야 합니다.
- 출력 형식은 `YYYY-MM-DDTHH:mm:ss±HH:mm`입니다. 예: `2026-08-11T02:17:05+09:00`.
- Producer가 메시지를 만들 때마다 현재 시각을 새로 계산해야 하며 모든 레코드에 같은 샘플 Timestamp를 하드코딩하면 안 됩니다.



### 3.1 목표

Private MSK 클러스터를 IAM 인증으로 구성하고, EC2 Producer가 센서 데이터를 `wsc2026-sensor-raw` 토픽으로 발행합니다. Lambda consumer가 데이터를 처리해 DynamoDB에 저장하고, 이상 데이터는 `wsc2026-sensor-alert` 토픽으로 넘깁니다. Alert consumer는 SNS 알림과 S3 로그 저장을 수행합니다.

### 3.2 필수 리소스

| 항목 | 값 |
|---|---|
| Region | `ap-northeast-1` |
| VPC | `msk-vpc`, `192.168.0.0/16` |
| MSK Cluster | `wsc2026-msk-cluster` |
| Kafka Version | `3.6.0` |
| Broker Type | `kafka.t3.small` |
| Auth | IAM enabled |
| Topic 1 | `wsc2026-sensor-raw`, partitions 3, replication 2 |
| Topic 2 | `wsc2026-sensor-alert`, partitions 1, replication 2 |
| Producer EC2 | `wsc2026-sensor-producer`, `t3.small` |
| EC2 Role | `wsc2026-msk-ec2-role` |
| Lambda Role | `wsc2026-msk-lambda-role` |
| Lambda Runtime | `python3.14` |
| DynamoDB | `wsc2026-sensor-data`, PK `sensorId`, SK `timestamp` |
| S3 Bucket | `wsc2026-sensor-alert-bucket-<비번호>` |
| SNS Topic | `wsc2026-sensor-alert` |

### 3.3 네트워크

| Subnet | CIDR | Route |
|---|---|---|
| `msk-pub-a` | `192.168.0.0/24` | IGW |
| `msk-pub-d` | `192.168.1.0/24` | IGW |
| `msk-priv-a` | `192.168.10.0/24` | NAT |
| `msk-priv-d` | `192.168.11.0/24` | NAT |

MSK와 Producer EC2는 private 환경에 둡니다. Producer가 MSK broker에 접근할 수 있도록 보안 그룹에서 broker port를 허용합니다.

### 3.4 DynamoDB, S3, SNS

```bash
aws configure set region ap-northeast-1
export ALERT_BUCKET=wsc2026-sensor-alert-bucket-${NUM}

aws dynamodb create-table \
  --table-name wsc2026-sensor-data \
  --attribute-definitions AttributeName=sensorId,AttributeType=S AttributeName=timestamp,AttributeType=S \
  --key-schema AttributeName=sensorId,KeyType=HASH AttributeName=timestamp,KeyType=RANGE \
  --billing-mode PAY_PER_REQUEST

aws s3api create-bucket \
  --bucket ${ALERT_BUCKET} \
  --region ap-northeast-1 \
  --create-bucket-configuration LocationConstraint=ap-northeast-1

aws sns create-topic --name wsc2026-sensor-alert
```

### 3.5 MSK Cluster

MSK 설정:

- Cluster name: `wsc2026-msk-cluster`
- Kafka version: `3.6.0`
- Instance type: `kafka.t3.small`
- Broker subnets: private a/d
- SASL IAM authentication: enabled
- Public access: disabled

채점 확인:

```bash
CLUSTER_ARN=$(aws kafka list-clusters \
  --cluster-name-filter wsc2026-msk-cluster \
  --query "ClusterInfoList[0].ClusterArn" \
  --output text)

aws kafka describe-cluster \
  --cluster-arn ${CLUSTER_ARN} \
  --query "ClusterInfo.[ClusterName,State,CurrentBrokerSoftwareInfo.KafkaVersion,BrokerNodeGroupInfo.InstanceType,ClientAuthentication.Sasl.Iam.Enabled]" \
  --output text
```

기대값: `wsc2026-msk-cluster ACTIVE 3.6.0 kafka.t3.small True`

### 3.6 Topic 생성

MSK client EC2나 Producer EC2에서 Kafka CLI를 사용합니다.

```bash
BOOTSTRAP=$(aws kafka get-bootstrap-brokers \
  --cluster-arn ${CLUSTER_ARN} \
  --query BootstrapBrokerStringSaslIam \
  --output text)

kafka-topics.sh --bootstrap-server ${BOOTSTRAP} \
  --command-config client.properties \
  --create --topic wsc2026-sensor-raw \
  --partitions 3 --replication-factor 2

kafka-topics.sh --bootstrap-server ${BOOTSTRAP} \
  --command-config client.properties \
  --create --topic wsc2026-sensor-alert \
  --partitions 1 --replication-factor 2
```

`client.properties`에는 IAM auth 설정을 넣습니다.

```properties
security.protocol=SASL_SSL
sasl.mechanism=AWS_MSK_IAM
sasl.jaas.config=software.amazon.msk.auth.iam.IAMLoginModule required;
sasl.client.callback.handler.class=software.amazon.msk.auth.iam.IAMClientCallbackHandler
```

### 3.7 Producer EC2

제공 `Application.md` 기준 환경변수:

| 변수 | 값 |
|---|---|
| `BOOTSTRAP_SERVERS` | MSK IAM bootstrap broker endpoint |
| `TOPIC_RAW` | `wsc2026-sensor-raw` |

systemd 예시:

```ini
[Unit]
Description=WSC2026 Sensor Producer
After=network-online.target

[Service]
WorkingDirectory=/opt/sensor
Environment=BOOTSTRAP_SERVERS=<MSK_BOOTSTRAP_SERVERS>
Environment=TOPIC_RAW=wsc2026-sensor-raw
ExecStart=/opt/sensor/app
Restart=always
User=ec2-user

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now sensor-producer
sudo journalctl -u sensor-producer -f
```

로그에 `SENSOR-001`, `SENSOR-002` 같은 출력이 계속 나오면 producer가 실행 중입니다.

### 3.8 Lambda Consumer

채점 스크립트는 다음 두 Lambda의 런타임과 event source mapping 상태를 확인합니다.

| Function | Trigger topic |
|---|---|
| `wsc2026-sensor-consumer` | `wsc2026-sensor-raw` |
| `wsc2026-sensor-alert-consumer` | `wsc2026-sensor-alert` |

Sensor consumer 처리:

1. MSK event에서 메시지를 읽습니다.
2. temperature/humidity 임계치를 확인합니다.
3. 정상 데이터는 DynamoDB에 `status=NORMAL`로 저장합니다.
4. 이상 데이터는 `status=ALERT`, `alert_reason`을 붙이고 alert topic으로 발행합니다.

이상 기준:

| 조건 | status |
|---|---|
| `temperature > 80` | ALERT |
| `temperature < 10` | ALERT |
| `humidity > 90` | ALERT |
| `humidity < 20` | ALERT |
| otherwise | NORMAL |

Alert consumer 처리:

1. `wsc2026-sensor-alert` topic 메시지 수신
2. SNS `wsc2026-sensor-alert`로 알림 발송
3. S3 `alert/{sensorId}/{date}/{timestamp}.json` 경로에 로그 저장

환경변수:

| Function | Env |
|---|---|
| sensor consumer | `DDB_TABLE=wsc2026-sensor-data`, `ALERT_TOPIC=wsc2026-sensor-alert`, `BOOTSTRAP_SERVER=<broker>` |
| alert consumer | `SNS_TOPIC_ARN=<SNS ARN>`, `S3_BUCKET=wsc2026-sensor-alert-bucket-<비번호>` |

### 3.9 Event Source Mapping

MSK Lambda trigger를 두 함수에 연결합니다.

```bash
aws lambda create-event-source-mapping \
  --function-name wsc2026-sensor-consumer \
  --event-source-arn ${CLUSTER_ARN} \
  --topics wsc2026-sensor-raw \
  --starting-position LATEST \
  --source-access-configurations Type=VPC_SUBNET,URI=subnet:<PRIVATE_SUBNET_ID> Type=VPC_SECURITY_GROUP,URI=security_group:<LAMBDA_SG_ID>

aws lambda create-event-source-mapping \
  --function-name wsc2026-sensor-alert-consumer \
  --event-source-arn ${CLUSTER_ARN} \
  --topics wsc2026-sensor-alert \
  --starting-position LATEST \
  --source-access-configurations Type=VPC_SUBNET,URI=subnet:<PRIVATE_SUBNET_ID> Type=VPC_SECURITY_GROUP,URI=security_group:<LAMBDA_SG_ID>
```

채점 확인:

```bash
for fn in wsc2026-sensor-consumer wsc2026-sensor-alert-consumer; do
  aws lambda list-event-source-mappings \
    --function-name $fn \
    --query "EventSourceMappings[0].[State]" \
    --output text
done
```

기대값:

```text
Enabled
Enabled
```

### 3.10 Data Processing 확인

```bash
aws dynamodb scan \
  --table-name wsc2026-sensor-data \
  --max-items 1 \
  --query "Items[0].{sensorId:sensorId.S,temperature:temperature.S,status:status.S}" \
  --output table
```

```bash
aws dynamodb scan \
  --table-name wsc2026-sensor-data \
  --max-items 3 \
  --query "Items[*].{sensorId:sensorId.S,timestamp:timestamp.S}" \
  --output table
```

Producer가 계속 실행 중이면 데이터가 쌓이고, 위 명령에서 sensorId와 timestamp가 표시됩니다.


### 3.11 Timestamp 구현과 검증

Python Producer에서는 timezone이 포함된 실제 현재 시각을 다음처럼 생성합니다.

```python
from datetime import datetime, timezone

timestamp = datetime.now().astimezone().isoformat(timespec="seconds")
# 예: 2026-08-11T02:17:05+09:00

message = {
    "sensorId": sensor_id,
    "temperature": temperature,
    "humidity": humidity,
    "timestamp": timestamp,
}
```

채점표는 KST Offset `+09:00`을 명시적으로 요구합니다. 코드에서 KST를 지정해야 하며 `+00:00`이면 3-6에서 오답 처리될 수 있습니다.

```bash
aws dynamodb scan \
  --table-name wsc2026-sensor-data \
  --projection-expression "sensorId,#ts" \
  --expression-attribute-names '{"#ts":"timestamp"}' \
  --query "Items[*].[sensorId.S,timestamp.S]" \
  --output table
```

확인할 사항:

1. `timestamp`가 빈 값 또는 문자 `(timestamp)`가 아닌 실제 시각인지 확인합니다.
2. 메시지를 여러 번 생성했을 때 Timestamp가 계속 갱신되는지 확인합니다.
3. 날짜와 시각 사이에 `T`, 끝부분에 KST Offset `+09:00`이 있는지 확인합니다.
4. S3 Key의 `alert/{sensorId}/{date}/{timestamp}.json`에도 실제 Timestamp를 사용합니다. 파일명에 사용할 수 있도록 필요하면 `:`만 안전한 문자로 치환하되 JSON 본문은 원래 Timestamp를 보존합니다.

---

## 공식 채점표 19/19 만점 검증

이 절은 공식 채점표 19개 항목을 실행 순서대로 정리한 최종 검증판입니다. 채점 직전에 모두 실행하고 아래 기대값과 비교합니다.

| 모듈 | 항목 | 배점 |
|---|---:|---:|
| Workflow | 1-1~1-6 | 7.5 |
| Analytics | 2-1~2-7 | 7.5 |
| MSK | 3-1~3-6 | 7.5 |
| 합계 | 19개 | 22.5 |

### Workflow 사전 준비

채점 전 연습 데이터가 남으면 1-1, 1-5, 1-6이 오답 처리될 수 있습니다. 채점자가 파일을 올리기 전에 S3와 DynamoDB Item을 비우고, 빈 Prefix marker만 다시 만듭니다.

```bash
aws configure set region ap-southeast-1
export BUCKET_NAME=wsc2026-student-score-bucket-${NUM}
aws s3 rm "s3://${BUCKET_NAME}" --recursive
for p in input error processed; do aws s3api put-object --bucket "$BUCKET_NAME" --key "${p}/" >/dev/null; done
aws dynamodb scan --table-name wsc2026-student-score --select COUNT --query Count --output text
```

DynamoDB 출력은 `0`이어야 합니다. 이후 지급 `test.csv`를 올리고 60초 기다립니다.

```bash
aws s3 cp test.csv "s3://${BUCKET_NAME}/input/test.csv"
sleep 60
```

### 1-1 S3 Bucket + Folder Structure - 1점

```bash
aws s3api head-bucket --bucket "$BUCKET_NAME" >/dev/null && aws s3 ls "s3://${BUCKET_NAME}/"
```

기대값은 `PRE error/`, `PRE input/`, `PRE processed/`입니다.

### 1-2 DynamoDB Table + Key Schema - 1점

```bash
aws dynamodb describe-table --table-name wsc2026-student-score --query "Table.[TableName,KeySchema]" --output json
```

`studentId=HASH`, `examDate=RANGE`이며 다른 KeySchema가 없어야 합니다.

### 1-3 Lambda Function + Runtime + Env - 1.5점

```bash
aws lambda get-function-configuration --function-name wsc2026-student-score-function --query "[FunctionName,Runtime,Environment.Variables]" --output json
```

기대값은 함수명, `python3.12`, `S3_BUCKET=wsc2026-student-score-bucket-<비번호>`, `DDB_TABLE=wsc2026-student-score`입니다.

### 1-4 Step Functions State Machine - 1점

```bash
SM_ARN=$(aws stepfunctions list-state-machines --query "stateMachines[?name=='wsc2026-student-score-workflow'].stateMachineArn|[0]" --output text)
aws stepfunctions describe-state-machine --state-machine-arn "$SM_ARN" --query "[name,type]" --output text
```

기대값은 `wsc2026-student-score-workflow STANDARD`입니다.

### 1-5 Workflow Result Normal - 1.5점

```bash
aws dynamodb get-item --table-name wsc2026-student-score --key '{"studentId":{"S":"STU1020"},"examDate":{"S":"2026-05-30"}}' --query "Item.[studentId.S,average.N,grade.S]" --output text
aws s3 ls "s3://${BUCKET_NAME}/processed/"
```

기대값은 `STU1020 96.6 A`와 `test.csv` 하나입니다.

### 1-6 Workflow Result Error - 1.5점

```bash
aws s3 ls "s3://${BUCKET_NAME}/error/"
ERROR_KEYS=$(aws s3api list-objects-v2 --bucket "$BUCKET_NAME" --prefix error/ --query "Contents[?Size>`0`].Key" --output text)
printf '%s\n' "$ERROR_KEYS" | tr '\t' '\n' | sort
printf 'error-file-count='; printf '%s\n' "$ERROR_KEYS" | tr '\t' '\n' | sed '/^$/d' | wc -l
```

정확히 4개여야 하며 파일명 끝은 `STU2001.json`, `STU2002.json`, `STU2004.json`, `unknown.json`입니다. 다른 오류 파일이 하나라도 있으면 안 됩니다.

### Analytics 사전 준비

```bash
aws configure set region ap-northeast-2
export ALB_DNS=$(aws elbv2 describe-load-balancers --names wsc2026-analytics-alb --query "LoadBalancers[0].DNSName" --output text)
export EC2_ID=$(aws ec2 describe-instances --filters "Name=tag:Name,Values=wsc2026-analytics-ec2" "Name=instance-state-name,Values=running" --query "Reservations[0].Instances[0].InstanceId" --output text)
```

### 2-1 EC2 Instance - 1점

```bash
aws ec2 describe-instances --instance-ids "$EC2_ID" --query "Reservations[0].Instances[0].InstanceType" --output text
SUBNET_ID=$(aws ec2 describe-instances --instance-ids "$EC2_ID" --query "Reservations[0].Instances[0].SubnetId" --output text)
aws ec2 describe-subnets --subnet-ids "$SUBNET_ID" --query "Subnets[0].Tags[?Key=='Name'].Value|[0]" --output text
```

기대값은 `t3.small`, `analytics-priv-a`입니다.

### 2-2 ALB Resources - 1점

```bash
LB_ARN=$(aws elbv2 describe-load-balancers --names wsc2026-analytics-alb --query "LoadBalancers[0].LoadBalancerArn" --output text)
aws elbv2 describe-listeners --load-balancer-arn "$LB_ARN" --query "Listeners[].[Port,Protocol]" --output text
aws elbv2 describe-target-groups --names wsc2026-analytics-tg --query "TargetGroups[].[TargetGroupName,Port]" --output text
```

기대값은 `80 HTTP`, `wsc2026-analytics-tg 5000`입니다.

### 2-3 Kinesis Stream - 1점

```bash
aws kinesis describe-stream-summary --stream-name wsc2026-order-stream --query "StreamDescriptionSummary.[StreamName,StreamStatus,StreamModeDetails.StreamMode]" --output text
```

기대값은 `wsc2026-order-stream ACTIVE ON_DEMAND`입니다.

### 2-4 Kinesis Data - 1점

```bash
curl -s -X POST "http://${ALB_DNS}/order" | jq .
curl -s -X POST "http://${ALB_DNS}/order" | jq -e 'has("event_time") and has("order_id") and has("price") and has("product_name") and has("quantity")'
```

`event_time`, `order_id`, `price`, `product_name`, `quantity`에 값이 있어야 하며 두 번째 출력은 `true`여야 합니다.

### 2-5 Flink Application - 1점

```bash
aws kinesisanalyticsv2 describe-application --application-name wsc2026-analytics-flink --query "ApplicationDetail.[ApplicationName,ApplicationStatus,RuntimeEnvironment]" --output text
```

기대값은 `wsc2026-analytics-flink READY ZEPPELIN-FLINK-3_0`입니다.

### 2-6 Application Health - 1점

```bash
curl -s "http://${ALB_DNS}/health"
```

기대값은 `{"status":"healthy"}`입니다.

### 2-7 Systemd Service - 1.5점

```bash
CMD_ID=$(aws ssm send-command --instance-ids "$EC2_ID" --document-name "AWS-RunShellScript" --parameters '{"commands":["systemctl is-active app && systemctl is-enabled app"]}' --query "Command.CommandId" --output text)
sleep 3
aws ssm get-command-invocation --command-id "$CMD_ID" --instance-id "$EC2_ID" --query "StandardOutputContent" --output text
```

SSM 출력이 `active`, `enabled` 두 줄이어야 합니다.

### MSK 사전 준비

채점표의 사전준비 Bucket 변수에는 student-score Bucket이 적혀 있지만 3-1 기대 출력과 문제지는 sensor-alert Bucket을 요구합니다. 기대값에 맞춰 Alert Bucket을 사용합니다.

```bash
aws configure set region ap-northeast-1
export CLUSTER_ARN=$(aws kafka list-clusters --cluster-name-filter wsc2026-msk-cluster --query "ClusterInfoList[0].ClusterArn" --output text)
export ALERT_BUCKET=wsc2026-sensor-alert-bucket-${NUM}
```

### 3-1 Resources - 1점

```bash
aws dynamodb describe-table --table-name wsc2026-sensor-data --query "Table.[TableName,KeySchema[*].AttributeName]" --output text
aws s3api head-bucket --bucket "$ALERT_BUCKET" --output json
```

기대값은 Table `wsc2026-sensor-data`, Key `sensorId timestamp`, Bucket Region `ap-northeast-1`, `AccessPointAlias=false`입니다.

### 3-2 Lambda Functions - 1.5점

```bash
for fn in wsc2026-sensor-consumer wsc2026-sensor-alert-consumer; do aws lambda get-function --function-name "$fn" --query "Configuration.[FunctionName,Runtime]" --output text; done
```

두 함수 모두 `python3.14`여야 합니다.

### 3-3 MSK Cluster Configuration - 1.5점

```bash
aws kafka describe-cluster --cluster-arn "$CLUSTER_ARN" --query "ClusterInfo.[ClusterName,State,CurrentBrokerSoftwareInfo.KafkaVersion,BrokerNodeGroupInfo.InstanceType,ClientAuthentication.Sasl.Iam.Enabled]" --output text
aws kafka list-topics --cluster-arn "$CLUSTER_ARN" --query "Topics[?starts_with(TopicName,'wsc2026')].[TopicName,ReplicationFactor,PartitionCount]" --output json
```

기대값은 Cluster `ACTIVE 3.6.0 kafka.t3.small True`, Alert Topic `RF=2/Partition=1`, Raw Topic `RF=2/Partition=3`입니다.

### 3-4 MSK Trigger Mapping - 1.5점

```bash
for fn in wsc2026-sensor-consumer wsc2026-sensor-alert-consumer; do aws lambda list-event-source-mappings --function-name "$fn" --query "EventSourceMappings[0].[State]" --output text; done
```

두 줄 모두 `Enabled`여야 합니다.

### 3-5 Data Processing Result - 1점

```bash
aws dynamodb scan --table-name wsc2026-sensor-data --max-items 1 --query "Items[0].{sensorId:sensorId.S,temperature:temperature.S,status:status.S}" --output json
```

값은 달라도 되지만 Key 3개가 모두 있어야 하고 `temperature`는 DynamoDB String이어야 합니다.

### 3-6 Producer Running - 1점

```bash
aws dynamodb scan --table-name wsc2026-sensor-data --max-items 3 --query "Items[*].{sensorId:sensorId.S,timestamp:timestamp.S}" --output json
aws dynamodb scan --table-name wsc2026-sensor-data --max-items 3 --query "Items[*].timestamp.S" --output json | jq -e 'length > 0 and all(test("^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\+09:00$"))'
```

Timestamp는 실제 생성 시각의 `YYYY-MM-DDTHH:mm:ss+09:00` 형식이어야 하며 검사 결과는 `true`여야 합니다. `+00:00`, 고정 샘플, `(timestamp)`, 빈 값은 오답 위험입니다.

### 19개 최종 체크표

| 번호 | 만점 확인값 | 완료 |
|---|---|:---:|
| 1-1 | S3 세 Prefix | □ |
| 1-2 | studentId HASH / examDate RANGE | □ |
| 1-3 | Python 3.12 / Env 2개 | □ |
| 1-4 | Workflow STANDARD | □ |
| 1-5 | STU1020 / 96.6 / A / test.csv | □ |
| 1-6 | 오류 JSON 정확히 4개 | □ |
| 2-1 | t3.small / analytics-priv-a | □ |
| 2-2 | 80 HTTP / TG 5000 | □ |
| 2-3 | ACTIVE ON_DEMAND | □ |
| 2-4 | 주문 JSON 5개 Key | □ |
| 2-5 | READY / ZEPPELIN-FLINK-3_0 | □ |
| 2-6 | healthy | □ |
| 2-7 | SSM active / enabled | □ |
| 3-1 | DDB Keys / Alert Bucket / Alias false | □ |
| 3-2 | Lambda 두 개 Python 3.14 | □ |
| 3-3 | Cluster / Topic / RF / Partition | □ |
| 3-4 | Mapping 두 개 Enabled | □ |
| 3-5 | sensorId / temperature / status | □ |
| 3-6 | 실제 KST Timestamp +09:00 | □ |

---
## 2과제 이론 설명

이 2과제는 “독립 모듈 3개”처럼 보이지만, 실제로는 클라우드에서 자주 쓰는 세 가지 아키텍처 패턴을 묻습니다.

| 모듈 | 아키텍처 패턴 | 핵심 질문 |
|---|---|---|
| Workflow | 서버리스 배치 처리 | 파일이 들어왔을 때 검증, 처리, 저장, 이동을 어떻게 자동화하는가 |
| Real-time Analytics | 실시간 스트림 분석 | 애플리케이션 이벤트를 어떻게 수집하고 SQL로 즉시 분석하는가 |
| MSK | Kafka 기반 이벤트 스트리밍 | 대량 이벤트를 토픽으로 분리하고 consumer가 어떻게 후처리하는가 |

### 1. Workflow 이론

Workflow 모듈은 S3에 파일이 들어오는 순간부터 처리 완료까지를 서버리스로 자동화하는 문제입니다.

#### S3의 역할

S3는 원본 파일과 처리 결과를 저장하는 객체 저장소입니다. 파일 시스템의 폴더처럼 보이는 `input/`, `processed/`, `error/`는 실제 폴더가 아니라 key prefix입니다.

| Prefix | 의미 |
|---|---|
| `input/` | 처리할 원본 CSV |
| `processed/` | 정상 처리 후 이동된 CSV |
| `error/` | 검증 실패 데이터 또는 오류 로그 |

시험에서 `processed/`, `error/`가 비어 있어야 한다고 하는 이유는 채점자가 `test.csv`를 기준으로 새 실행 결과만 확인하기 위해서입니다. 이전 실행 결과가 남아 있으면 채점 결과가 섞일 수 있습니다.

#### Lambda의 역할

Lambda는 짧게 실행되는 이벤트 처리 함수입니다. 이 모듈에서는 CSV를 읽고, 행 단위로 검증하고, 정상 데이터만 DynamoDB에 저장합니다.

검증 로직은 데이터 품질을 보장하는 단계입니다.

| 검증 | 이유 |
|---|---|
| 필수 필드 확인 | DynamoDB key나 평균 계산에 필요한 값이 빠지면 저장이 불가능 |
| 점수 범위 확인 | 0~100 범위를 벗어난 점수는 성적 데이터로 신뢰할 수 없음 |
| 정수 형식 확인 | `eighty`, `A` 같은 값은 평균 계산 불가 |
| 날짜 형식 확인 | 정렬, 조회, key 구성에 날짜 형식이 필요 |

#### DynamoDB의 역할

DynamoDB는 서버리스 NoSQL 데이터베이스입니다. 이 모듈에서는 학생별 시험 결과를 저장합니다.

PK와 SK의 의미:

| Key | 의미 |
|---|---|
| `studentId` | 학생을 구분하는 파티션 키 |
| `examDate` | 같은 학생의 여러 시험일을 구분하는 정렬 키 |

`studentId + examDate` 조합이 하나의 성적 결과를 유일하게 식별합니다.

#### Step Functions의 역할

Step Functions는 여러 작업을 순서대로 연결하고, 성공/실패에 따라 흐름을 분기하는 오케스트레이션 서비스입니다.

Lambda 하나만으로도 CSV 처리는 가능하지만, Step Functions를 쓰면 다음 장점이 있습니다.

- S3 파일 존재 확인, Lambda 호출, 결과 분기, 파일 이동을 한 흐름으로 관리할 수 있습니다.
- 실패 지점을 시각적으로 추적할 수 있습니다.
- 재시도와 backoff 정책을 선언적으로 넣을 수 있습니다.
- 처리 완료 파일과 오류 파일 이동을 명확하게 분리할 수 있습니다.

시험에서 `STANDARD` 타입을 요구하는 이유는 실행 이력과 상태 추적이 중요하기 때문입니다. `EXPRESS`는 대량 고빈도 이벤트에는 좋지만, 채점자가 실행 흐름을 추적하기에는 Standard가 더 적합합니다.

#### Trigger Lambda가 필요한 이유

S3 이벤트가 바로 Step Functions를 실행할 수도 있지만, 과제에서는 Trigger Lambda를 통해 실행하도록 요구합니다. Trigger Lambda는 S3 이벤트 형식을 Step Functions 입력 형식인 `{"key":"input/test.csv"}`로 바꿔주는 어댑터 역할을 합니다.

### 2. Real-time Data Analytics 이론

이 모듈은 주문 이벤트를 생성하고, Kinesis Data Stream에 쌓고, Managed Apache Flink에서 SQL로 실시간 분석하는 구조입니다.

#### EC2와 ALB의 역할

EC2는 주문 이벤트를 생성하는 Flask 애플리케이션을 실행합니다. EC2를 private subnet에 두는 이유는 애플리케이션 서버에 직접 접근하지 못하게 하고, ALB를 통해서만 들어오게 하기 위해서입니다.

ALB는 외부 HTTP 요청을 받아 private EC2로 전달합니다.

| 구성 | 이유 |
|---|---|
| ALB public subnet | 사용자가 인터넷에서 접근해야 함 |
| EC2 private subnet | 서버 직접 노출 방지 |
| Target Group port 5000 | Flask/Gunicorn 앱이 5000 포트에서 실행 |
| `/health` health check | ALB가 정상 target인지 판단 |

systemd를 요구하는 이유는 재부팅 후에도 앱이 자동 실행되어야 하기 때문입니다. 채점 스크립트가 `systemctl is-active app && systemctl is-enabled app`을 확인하는 것도 이 때문입니다.

#### Kinesis Data Stream의 역할

Kinesis Data Stream은 실시간 이벤트를 순서 있게 수집하는 스트림 서비스입니다. 앱이 `/order` 요청을 받을 때마다 주문 JSON을 Kinesis에 넣습니다.

Kinesis의 핵심 개념:

| 개념 | 설명 |
|---|---|
| Stream | 이벤트가 들어가는 논리적 파이프 |
| Record | 하나의 이벤트 JSON |
| Partition Key | 이벤트가 어느 shard로 갈지 결정하는 값 |
| Retention | consumer가 나중에 읽을 수 있도록 일정 시간 보관 |
| On-demand mode | shard 용량을 직접 설계하지 않아도 트래픽에 맞춰 자동 조절 |

시험에서 On-demand를 요구하는 이유는 선수들이 shard 수 계산보다 아키텍처 구성에 집중하도록 하기 위한 것입니다.

#### Managed Apache Flink의 역할

Flink는 스트림 데이터를 실시간으로 분석하는 엔진입니다. 이 문제에서는 Flink 애플리케이션 코딩을 금지하고, Studio Notebook에서 SQL로 분석하도록 합니다.

두 SQL의 의미:

```sql
SELECT COUNT(*) as order_count
FROM order_stream
WHERE event_time > CURRENT_TIMESTAMP - INTERVAL '1' MINUTE;
```

최근 1분간 들어온 주문 수를 계산합니다. 실시간 트래픽 모니터링에 해당합니다.

```sql
SELECT product_name, SUM(price * quantity) as total_revenue
FROM order_stream
GROUP BY product_name;
```

상품별 누적 매출을 계산합니다. 이벤트가 계속 들어오면 결과도 계속 갱신됩니다.

즉 이 모듈은 “애플리케이션 로그/이벤트 → 스트림 수집 → SQL 분석” 흐름을 이해하는지 확인합니다.

### 3. MSK 이론

MSK 모듈은 Kafka 기반 이벤트 스트리밍 파이프라인입니다. Kinesis와 비슷하게 이벤트를 처리하지만, Kafka 토픽과 consumer group 개념을 중심으로 동작합니다.

#### Kafka/MSK의 역할

Amazon MSK는 AWS가 관리하는 Apache Kafka입니다. 직접 Kafka broker를 설치하고 패치하지 않아도 됩니다.

핵심 개념:

| 개념 | 설명 |
|---|---|
| Cluster | Kafka broker들의 집합 |
| Broker | 메시지를 저장하고 전달하는 서버 |
| Topic | 메시지 종류별 논리적 채널 |
| Partition | topic을 병렬 처리하기 위한 단위 |
| Replication Factor | broker 장애에 대비한 복제 수 |
| Producer | topic에 메시지를 쓰는 애플리케이션 |
| Consumer | topic에서 메시지를 읽는 애플리케이션 |

이 문제에서 raw topic과 alert topic을 나누는 이유는 정상 처리 흐름과 이상 데이터 후처리 흐름을 분리하기 위해서입니다.

| Topic | 목적 |
|---|---|
| `wsc2026-sensor-raw` | Producer가 생성한 원본 센서 데이터 |
| `wsc2026-sensor-alert` | 이상 탐지된 센서 데이터 |

#### Partition과 Replication Factor

`wsc2026-sensor-raw`는 partitions 3입니다. 원본 센서 데이터는 많이 들어올 수 있으므로 병렬 처리를 고려한 설정입니다.

`wsc2026-sensor-alert`는 partitions 1입니다. 이상 이벤트는 상대적으로 적고, 순서가 더 중요할 수 있습니다.

Replication factor 2는 broker 하나가 장애 나도 메시지를 유지하기 위한 복제 설정입니다. 그래서 MSK는 최소 2개 이상의 broker/subnet 구성이 필요합니다.

#### IAM 인증

문제에서 MSK 클러스터는 IAM 인증으로만 접근 가능해야 합니다. 이는 Kafka 자체 사용자/비밀번호 대신 AWS IAM principal에 권한을 부여하는 방식입니다.

장점:

- EC2 role, Lambda role에 최소 권한을 줄 수 있습니다.
- 키 파일이나 패스워드를 서버에 직접 저장하지 않아도 됩니다.
- CloudTrail/IAM 기반 운영 통제가 가능합니다.

#### Lambda MSK Trigger

Lambda는 MSK topic을 event source로 사용할 수 있습니다. 이때 Lambda가 private MSK broker에 접근해야 하므로 VPC subnet과 security group 설정이 필요합니다.

흐름:

1. Producer EC2가 raw topic에 센서 JSON 발행
2. `wsc2026-sensor-consumer` Lambda가 raw topic 소비
3. 정상/이상 여부 판단
4. 정상 데이터는 DynamoDB 저장
5. 이상 데이터는 alert topic으로 발행
6. `wsc2026-sensor-alert-consumer` Lambda가 alert topic 소비
7. SNS 알림 발송 및 S3 로그 저장

#### DynamoDB, S3, SNS의 역할

| 서비스 | 역할 |
|---|---|
| DynamoDB | 센서 데이터의 최신/이력 조회를 위한 저장소 |
| S3 | 이상 데이터 JSON 로그를 장기 보관 |
| SNS | 운영자에게 이상 데이터 발생 알림 |

이 구조는 “실시간 감지 + 저장 + 알림 + 감사 로그”를 모두 포함합니다.

### 4. 세 모듈을 관통하는 핵심 개념

#### 이벤트 기반 아키텍처

2과제의 세 모듈은 모두 이벤트 기반입니다.

| 모듈 | 이벤트 |
|---|---|
| Workflow | S3에 CSV 업로드 |
| Analytics | `/order` 요청으로 주문 이벤트 생성 |
| MSK | 센서 데이터 Kafka 메시지 |

이벤트 기반 아키텍처에서는 요청자가 후속 처리 전체를 직접 기다리지 않습니다. 이벤트를 남기면 Lambda, Step Functions, Flink, Kafka consumer가 뒤에서 처리합니다.

#### 최소 권한 원칙

모든 모듈에서 IAM Role이 등장합니다. 이론적으로 가장 중요한 기준은 “필요한 서비스의 필요한 action만 허용”입니다.

예:

- Workflow Lambda: 해당 S3 bucket read/write, 해당 DynamoDB table write
- Analytics EC2: `kinesis:PutRecord`, `kinesis:PutRecords`
- MSK Lambda: 특정 topic read/write, DynamoDB write, SNS publish, S3 put

#### 채점 관점

채점 스크립트는 모든 이론을 직접 확인하지는 않습니다. 대신 다음처럼 이론이 반영된 “증거”를 확인합니다.

| 이론 | 채점 증거 |
|---|---|
| 서버리스 워크플로우 | Step Functions `STANDARD`, Lambda 결과, S3 processed/error |
| 스트림 수집 | Kinesis stream `ACTIVE ON_DEMAND` |
| 운영 서비스화 | systemd `active/enabled` |
| Kafka 보안 | MSK IAM auth `True` |
| Consumer 연결 | Lambda event source mapping `Enabled` |

---

## 최종 점검표

| 모듈 | 확인 명령 |
|---|---|
| Workflow S3 | `aws s3 ls s3://wsc2026-student-score-bucket-<비번호>/` |
| Workflow DDB | `aws dynamodb describe-table --table-name wsc2026-student-score` |
| Workflow Lambda | `aws lambda get-function-configuration --function-name wsc2026-student-score-function` |
| Workflow SFN | `aws stepfunctions describe-state-machine --state-machine-arn <ARN>` |
| Analytics ALB | `curl http://<ALB_DNS>/health` |
| Analytics Kinesis | `aws kinesis describe-stream-summary --stream-name wsc2026-order-stream` |
| Analytics systemd | `systemctl is-active app && systemctl is-enabled app` |
| MSK Cluster | `aws kafka describe-cluster --cluster-arn <ARN>` |
| MSK Lambda | `aws lambda list-event-source-mappings --function-name wsc2026-sensor-consumer` |
| MSK Data | `aws dynamodb scan --table-name wsc2026-sensor-data --max-items 1` |

## 자주 틀리는 부분

| 실수 | 결과 | 해결 |
|---|---|---|
| 모듈별 리전을 섞음 | 리소스 조회 실패 | 모듈 시작마다 `aws configure set region` 실행 |
| `processed/`, `error/`에 이전 파일이 남음 | Workflow 채점 혼동 | 채점 전 S3 prefix 정리 |
| Lambda handler 이름 불일치 | Lambda invoke 실패 | 파일명과 handler를 `index.handler`처럼 맞춤 |
| `STU1020` 평균이 `96.6`이 아님 | Workflow 결과 실패 | Decimal/round로 소수 1자리 저장 |
| EC2 앱을 foreground로만 실행 | systemd 채점 실패 | `app.service` 등록 후 enable |
| Target group port가 80 | ALB 채점 실패 | `wsc2026-analytics-tg` port 5000 |
| MSK IAM auth 누락 | MSK 채점 실패 | `ClientAuthentication.Sasl.Iam.Enabled=True` |
| Lambda MSK trigger disabled | MSK trigger 채점 실패 | event source mapping 상태 `Enabled` 확인 |
