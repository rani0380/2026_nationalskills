# 2026 전국대회 클라우드컴퓨팅 1과제 풀이해설서

## 1. 과제 목표 요약

이 과제는 지급된 `book-linux-amd64_v1.0.1`, `index.html`, `main.jpeg`를 이용해 AWS 위에 콘서트 예약 서비스를 구축하는 문제이다.

최종 사용 흐름은 다음과 같다.

1. 사용자는 CloudFront 단일 도메인으로 접속한다.
2. `/` 경로는 S3의 정적 페이지 `static/index.html`을 보여준다.
3. `/booking` 경로는 CloudFront -> ALB -> EKS Pod의 `POST /v1/book`으로 전달된다.
4. `book` 애플리케이션은 DynamoDB에 예약 데이터를 저장한다.
5. `/v1/book?booking_id=...` 경로는 CloudFront -> Lambda Function URL로 전달되어 DynamoDB 저장 데이터를 조회한다.
6. 애플리케이션 로그는 Fluent Bit을 통해 CloudWatch Logs로 수집되고, Prometheus/Grafana에서 메트릭과 로그를 확인한다.

## 2. 전체 구축 순서

권장 구축 순서는 의존성이 적은 것부터 만드는 것이다.

1. 공통 변수 정리
2. KMS CMK 생성
3. VPC, Subnet, IGW, NAT Gateway, Route Table 구성
4. DynamoDB 생성
5. ECR 생성 및 이미지 빌드/푸시
6. EKS Private Cluster와 NodeGroup 생성
7. Kubernetes 기본 리소스 배포
8. AWS Load Balancer Controller 설치 및 ALB Ingress 구성
9. S3 정적 파일 업로드
10. Lambda GET API 구현
11. CloudFront, WAF 연결
12. Observability 구성
13. 최종 API와 로그 검증

## 3. 공통 변수

예시는 다음 값을 기준으로 설명한다. 실제 작업에서는 비번호와 임의 영문 4자리를 반드시 본인 값으로 바꾼다.

```bash
export AWS_REGION=ap-northeast-2
export PREFIX=wsc2026
export RANDOM4=abcd
export PLAYER_NO=01

export VPC_NAME=wsc2026-skills-vpc
export BUCKET_NAME=wsc2026-static-${RANDOM4}-${PLAYER_NO}-bucket
export ECR_NAME=wsc2026-book-ecr
export TABLE_NAME=wsc2026-book-table
export CLUSTER_NAME=wsc2026-eks-cluster
export NAMESPACE=wsc2026
export OBS_NS=observability
```

주의할 점은 문제에서 지정한 고정 리소스명은 임의 변경하지 않는 것이다. 특히 DynamoDB, ECR, EKS Cluster, Deployment, Service, Ingress, Lambda, Grafana Dashboard 이름은 채점 대상일 가능성이 높다.

## 4. KMS CMK 생성

문제는 KMS CMK를 여러 리소스에 요구한다.

필요한 CMK는 다음과 같다.

| 용도 | CMK Name |
|---|---|
| DynamoDB | `wsc2026-db-kms` |
| ECR | `wsc2026-ecr-kms` |
| EKS Secret 암호화 | `wsc2026-eks-kms` |
| S3 | `wsc2026-bucket-kms` |
| Lambda | `wsc2026-function-kms` |

중요 조건은 Key Policy에 root와 `kms:*`를 넓게 허용하지 않는 것이다. 관리자는 필요한 키 관리 권한만, 서비스 역할은 필요한 암복호화 권한만 부여한다.

서비스별로 필요한 대표 권한은 다음과 같다.

- DynamoDB: `kms:Encrypt`, `kms:Decrypt`, `kms:GenerateDataKey`, `kms:DescribeKey`
- ECR: `kms:Encrypt`, `kms:Decrypt`, `kms:GenerateDataKey`, `kms:DescribeKey`
- S3/CloudFront OAC: S3가 객체 암복호화에 사용할 수 있도록 허용
- Lambda: 함수 코드와 환경변수 암복호화에 사용할 수 있도록 허용
- EKS: Kubernetes Secret 암호화 provider에 사용할 수 있도록 허용

## 5. Networking 풀이

Reference01 기준으로 VPC를 만든다.

| 리소스 | 이름 | CIDR |
|---|---|---|
| VPC | `wsc2026-skills-vpc` | `192.168.0.0/16` |
| Public Subnet A | `wsc2026-skills-hub-sub-a` | `192.168.1.0/24` |
| Public Subnet B | `wsc2026-skills-hub-sub-b` | `192.168.10.0/24` |
| Private Subnet A | `wsc2026-skills-app-sub-a` | `192.168.2.0/24` |
| Private Subnet B | `wsc2026-skills-app-sub-b` | `192.168.20.0/24` |

구성 원칙은 다음과 같다.

- Public Subnet은 IGW로 `0.0.0.0/0` 라우팅
- Private Subnet A는 NAT Gateway A로 `0.0.0.0/0` 라우팅
- Private Subnet B는 NAT Gateway B로 `0.0.0.0/0` 라우팅
- ALB는 Public Subnet에 배치
- EKS NodeGroup은 Private Subnet에 배치
- EKS Endpoint는 Private Access만 활성화

채점에서 자주 틀리는 부분은 Private Subnet Route Table을 하나만 만들고 NAT 하나로 몰아넣는 것이다. 문제는 고가용성을 요구하므로 AZ별 NAT Gateway와 Route Table을 분리하는 편이 안전하다.

## 6. DynamoDB 풀이

테이블은 다음 조건으로 생성한다.

| 항목 | 값 |
|---|---|
| Table Name | `wsc2026-book-table` |
| Partition Key | `client_id` |
| Billing Mode | `PAY_PER_REQUEST` |
| Encryption | `wsc2026-db-kms` CMK |
| Deletion Protection | Enabled |
| PITR | Enabled |
| GSI | `booking_id` 기반 조회용 |

GSI는 Lambda가 `booking_id`로 조회하기 위해 필요하다.

권장 GSI 예시는 다음과 같다.

| 항목 | 값 |
|---|---|
| Index Name | `booking_id-index` |
| Partition Key | `booking_id` |
| Projection | ALL |

IAM 권한은 분리한다.

- EKS Pod Role: `dynamodb:PutItem` 중심
- Lambda Role: `dynamodb:Query`, `dynamodb:GetItem` 중심

Pod와 Lambda에 `dynamodb:*`를 부여하면 최소 권한 조건에서 감점될 수 있다.

## 7. ECR 풀이

ECR은 다음 조건으로 만든다.

| 항목 | 값 |
|---|---|
| Repository Name | `wsc2026-book-ecr` |
| Type | Private |
| Encryption | KMS, `wsc2026-ecr-kms` |
| Scan on push | Enabled |
| Tag mutability | Mutable, 단 v1 태그는 예외 보호 |

문제의 핵심 문구는 "같은 이름의 이미지 태그가 있어도 업로드가 되도록 구성하되, v1.0.0, v1.0.1 등 v1 버전을 명시하는 이미지 태그는 예외"이다. 따라서 일반 태그는 overwrite 가능하게 두고, immutable tag exclusion 또는 정책으로 `v1.*` 계열 태그를 보호하는 방향이 맞다.

ECR에는 최종적으로 `v1.0.0` 이미지 외 다른 이미지가 남아 있으면 안 된다. 빌드 중 `latest`나 테스트 태그를 썼다면 마지막에 정리한다.

Dockerfile 예시:

```dockerfile
FROM public.ecr.aws/amazonlinux/amazonlinux:2023
WORKDIR /app
COPY book-linux-amd64_v1.0.1 /app/book
RUN chmod +x /app/book
EXPOSE 8080
ENTRYPOINT ["/app/book"]
```

이미지 빌드 시 지급 바이너리는 amd64이므로 ARM 환경에서 작업한다면 반드시 `--platform linux/amd64`를 지정한다.

```bash
docker build --platform linux/amd64 -t wsc2026-book-ecr:v1.0.0 .
```

## 8. EKS 풀이

EKS 조건은 다음과 같다.

| 항목 | 값 |
|---|---|
| Cluster Name | `wsc2026-eks-cluster` |
| Version | `1.35` |
| Endpoint | Private only |
| Secret Encryption | `wsc2026-eks-kms` |
| Control Plane Logs | 전체 활성화 |
| Cluster Domain | `wsc2026.skills.local` |

주의: 실제 AWS에서 문제에 적힌 Kubernetes `1.35`가 아직 제공되지 않는 환경이면, 시험 환경에서 선택 가능한 최신 버전을 사용하되 문제지와 채점 스크립트 기준을 확인해야 한다. 문서상 풀이에서는 요구 버전 `1.35`를 기준으로 적는다.

NodeGroup은 2개로 분리한다.

| 용도 | NodeGroup | Label | Instance |
|---|---|---|---|
| Addon/System | `wsc2026-addon-nodegroup` | `wsc2026/node=addon` | `t3.medium` |
| Application | `wsc2026-workload-ng` | `wsc2026/node=application` | `t3.medium` |

Addon NodeGroup에는 CoreDNS, AWS Load Balancer Controller, Prometheus, Grafana, Alertmanager 같은 시스템성 컴포넌트를 배치한다.

Application NodeGroup에는 `book` 애플리케이션 Pod만 배치한다. DaemonSet은 예외적으로 모든 노드에 뜰 수 있다.

## 9. Kubernetes Deployment 풀이

Namespace:

```yaml
apiVersion: v1
kind: Namespace
metadata:
  name: wsc2026
```

ConfigMap:

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: book-config
  namespace: wsc2026
data:
  AWS_REGION: ap-northeast-2
  TABLE_NAME: wsc2026-book-table
```

ServiceAccount는 Pod Identity Role과 연결한다.

```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: wsc2026-book-sa
  namespace: wsc2026
```

Deployment 핵심 조건:

- 이름: `wsc2026-book-deploy`
- replicas: 최소 2
- Application NodeGroup에만 배치
- `topologySpreadConstraints`로 AZ 분산
- `readinessProbe`, `livenessProbe`, `startupProbe` 모두 `/health`, port `8080`
- CPU/Memory request와 limit: `250m`, `512Mi`
- 환경변수는 `book-config`에서 주입

예시:

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wsc2026-book-deploy
  namespace: wsc2026
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wsc2026-book
  template:
    metadata:
      labels:
        app: wsc2026-book
    spec:
      serviceAccountName: wsc2026-book-sa
      nodeSelector:
        wsc2026/node: application
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: wsc2026-book
      containers:
        - name: book
          image: <account-id>.dkr.ecr.ap-northeast-2.amazonaws.com/wsc2026-book-ecr:v1.0.0
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: book-config
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "250m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
          startupProbe:
            httpGet:
              path: /health
              port: 8080
```

PDB:

```yaml
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: wsc2026-book-pdb
  namespace: wsc2026
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: wsc2026-book
```

Service:

```yaml
apiVersion: v1
kind: Service
metadata:
  name: wsc2026-book-svc
  namespace: wsc2026
  annotations:
    service.kubernetes.io/topology-mode: Auto
spec:
  selector:
    app: wsc2026-book
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
```

## 10. ALB Ingress 풀이

AWS Load Balancer Controller를 Addon NodeGroup에 설치한 뒤 Ingress를 만든다.

Ingress 조건:

- 이름: `wsc2026-book-ingress`
- ALB 이름: `wsc2026-app-alb`
- Scheme: `internet-facing`
- Security Group: `wsc2026-app-alb-sg`
- 잘못된 경로는 403
- CloudFront에서만 접근 가능하도록 제한

CloudFront에서만 접근시키는 방법은 두 가지가 있다.

1. ALB Security Group 인바운드를 CloudFront managed prefix list로 제한
2. CloudFront가 보내는 Custom Header를 ALB Listener Rule 조건으로 검사

시험에서는 둘을 같이 쓰면 가장 안전하다.

Ingress 경로 예시:

- `/booking` -> `wsc2026-book-svc:80`
- 기타 경로 -> fixed response `403`

단, `book` 애플리케이션 실제 API는 `/v1/book`이므로 `/booking`을 그대로 넘기면 404가 날 수 있다. 해결책은 다음 중 하나이다.

- ALB Listener Rule에서 `/booking`을 `/v1/book`으로 rewrite할 수 있는 구성을 사용한다.
- CloudFront Function으로 viewer request URI를 `/booking`에서 `/v1/book`으로 변경한 뒤 ALB Origin으로 전달한다.

구현 난이도는 CloudFront Function rewrite가 더 단순하다.

## 11. S3 풀이

S3 조건:

| 항목 | 값 |
|---|---|
| Bucket | `wsc2026-static-<임의영문4자리>-<비번호>-bucket` |
| Upload Prefix | `static/` |
| Public Access | Block |
| Encryption | SSE-KMS, `wsc2026-bucket-kms` |
| Bucket Key | Enabled |
| Access | CloudFront OAC만 허용 |

업로드 구조:

```text
s3://wsc2026-static-abcd-01-bucket/static/index.html
s3://wsc2026-static-abcd-01-bucket/static/main.jpeg
s3://wsc2026-static-abcd-01-bucket/static/book-linux-amd64_v1.0.1
```

정적 웹에 필요한 것은 `index.html`, `main.jpeg`지만 문제는 지급받은 배포파일들을 `static/`에 전부 업로드하라고 하므로 바이너리도 함께 올리는 편이 안전하다.

## 12. Lambda GET API 풀이

Lambda 조건:

| 항목 | 값 |
|---|---|
| Function Name | `wsc2026-book-get-function` |
| Runtime | Python 3.12 |
| Role | `wsc2026-book-function-role` |
| Policy | `wsc2026-book-function-policy` |
| CMK | `wsc2026-function-kms` |
| Env | `TABLE_NAME=wsc2026-book-table` |

Lambda는 `booking_id` query string을 받아 DynamoDB GSI에서 조회한다.

응답 컬럼 순서는 문제의 Response와 같아야 한다.

1. `client_id`
2. `username`
3. `email`
4. `concert_name`
5. `created_at`

Python 예시:

```python
import json
import os
import boto3
from boto3.dynamodb.conditions import Key

dynamodb = boto3.resource("dynamodb")
table = dynamodb.Table(os.environ["TABLE_NAME"])

def lambda_handler(event, context):
    params = event.get("queryStringParameters") or {}
    booking_id = params.get("booking_id")
    if not booking_id:
        return {"statusCode": 400, "body": json.dumps({"message": "booking_id is required"})}

    result = table.query(
        IndexName="booking_id-index",
        KeyConditionExpression=Key("booking_id").eq(booking_id),
        Limit=1,
    )
    items = result.get("Items", [])
    if not items:
        return {"statusCode": 404, "body": json.dumps({"message": "not found"})}

    item = items[0]
    body = {
        "client_id": item.get("client_id", ""),
        "username": item.get("username", ""),
        "email": item.get("email", ""),
        "concert_name": item.get("concert_name", ""),
        "created_at": format_created_at(item.get("created_at", "")),
    }
    return {
        "statusCode": 200,
        "headers": {"Content-Type": "application/json"},
        "body": json.dumps(body, ensure_ascii=False),
    }

def format_created_at(value):
    return value
```

`created_at`의 실제 저장 형식은 지급 애플리케이션이 만든 값을 확인한 뒤, 문제 예시처럼 `YYYY-MM-DD HH:MM:SS KS` 형태로 맞춘다.

## 13. CloudFront와 WAF 풀이

CloudFront는 단일 진입점이다.

Origin은 3개가 필요하다.

| Origin | 연결 대상 | 용도 |
|---|---|---|
| S3 Origin | S3 Bucket + OAC | 정적 페이지 |
| ALB Origin | `wsc2026-app-alb` | POST 예약 API |
| Lambda Origin | Lambda Function URL | GET 조회 API |

Behavior는 다음처럼 둔다.

| Path Pattern | Origin | Cache |
|---|---|---|
| Default `*` | S3 | Enabled |
| `/booking*` | ALB | Disabled |
| `/v1/book*` | Lambda Function URL | Disabled |

Default Root Object는 `static/index.html` 또는 Function으로 `/`를 `/static/index.html`로 rewrite하는 방식 중 하나를 선택한다. 가장 단순한 방법은 CloudFront Function을 사용해 `/` 요청 URI를 `/static/index.html`로 바꾸는 것이다.

WAF 조건:

- WebACL 이름: `wsc2026-waf`
- SQL Injection 관리형 룰 적용
- XSS 관리형 룰 적용
- Rate Limit: 특정 IP가 1분간 200개 이상 요청하면 차단
- CloudFront Distribution에 연결

AWS WAF rate-based rule의 기본 집계 창 설정이 문제의 "1분"과 맞는지 확인한다. 콘솔에서 Evaluation window를 선택할 수 있으면 1 minute로 둔다.

## 14. Observability 풀이

Namespace:

```bash
kubectl create namespace observability
```

구성 요소:

- Prometheus
- Alertmanager
- Node Exporter
- Grafana
- Fluent Bit

배치 원칙:

- Prometheus, Alertmanager, Grafana는 Addon NodeGroup에 배치
- Node Exporter와 Fluent Bit은 DaemonSet
- Prometheus retention은 7일
- Grafana Service는 LoadBalancer
- Grafana Admin Password는 `Skills$#$@!`

Prometheus Alert는 Reference02 기준으로 만든다.

| Alert | 조건 |
|---|---|
| PodHighCPU | Pod CPU 사용률 80% 초과 3분 이상 |
| PodHighMemory | Pod Memory 사용률 90% 초과 3분 이상 |
| PodNotReady | CrashLoopBackOff 또는 3분 이상 Not Ready |
| HighErrorRate | 4xx/5xx 비율 5% 초과 1분 이상 |
| HighLatency | 평균 응답 3초 초과 1분 이상 |
| PodCrashLooping | wsc2026 NS Pod 재시작 3회 초과가 3분 이상 지속 |

Fluent Bit 조건:

- 애플리케이션 액세스 로그를 CloudWatch Logs로 전송
- `/health` 로그 제외
- 실제 API 요청 로그만 수집
- JSON 필드를 파싱해 Grafana CloudWatch Logs 데이터소스에서 조회 가능하게 구성

로그 형식:

```text
INFO {"level":"INFO","path":"/v1/book","status":"200","duration":"75.959276ms","method":"POST"}
WARN {"level":"WARN","path":"/v1/book/999","status":"404","duration":"12.345678ms","method":"GET"}
ERROR {"level":"ERROR","path":"/v1/book","status":"500","duration":"231.876543ms","method":"POST"}
```

Grafana Dashboard 이름은 `wsc2026-grafana-dashboard`로 한다.

필수 패널:

- All Node CPU
- All Node Memory
- All Available Nodes
- All Pod CPU
- All Pod Memory
- All Pending Pods
- All Pod restarts
- App Pod CPU
- App Pod Memory
- App Running
- App restarts
- App Pending
- App Request Count
- App Response Time
- App Status Code
- App Application Logs
- Alertmanager 알림 현황

상단 변수:

- NodeGroup
- Namespace

색상 조건:

- CPU 80% 이상: 빨간색
- CPU 60% 이상 80% 미만: 노란색
- CPU 60% 미만: 초록색
- Pod 재시작 1회 이상: 경고 색상

## 15. 최종 검증 순서

### 15.1 VPC 검증

```bash
aws ec2 describe-vpcs --filters Name=tag:Name,Values=wsc2026-skills-vpc
aws ec2 describe-subnets --filters Name=vpc-id,Values=<vpc-id>
aws ec2 describe-route-tables --filters Name=vpc-id,Values=<vpc-id>
```

확인할 것:

- VPC CIDR이 `192.168.0.0/16`
- Public Subnet 2개, Private Subnet 2개
- Public은 IGW, Private은 NAT Gateway로 라우팅

### 15.2 DynamoDB 검증

```bash
aws dynamodb describe-table --table-name wsc2026-book-table
```

확인할 것:

- PK가 `client_id`
- Billing Mode가 `PAY_PER_REQUEST`
- SSE KMS 사용
- PITR 활성화
- Deletion Protection 활성화
- `booking_id` GSI 존재

### 15.3 ECR 검증

```bash
aws ecr describe-repositories --repository-names wsc2026-book-ecr
aws ecr describe-images --repository-name wsc2026-book-ecr
```

확인할 것:

- KMS 암호화
- scan on push
- 최종 이미지 태그 `v1.0.0`
- 불필요한 이미지 없음

### 15.4 EKS 검증

```bash
aws eks describe-cluster --name wsc2026-eks-cluster
kubectl get nodes --show-labels
kubectl get pods -A -o wide
```

확인할 것:

- Cluster endpoint가 private only
- Control plane logs 활성화
- Node label이 addon/application으로 분리
- 앱 Pod가 application node에 배치
- addon 컴포넌트가 addon node에 배치

### 15.5 Application 검증

```bash
kubectl -n wsc2026 get deploy,svc,ingress,pdb
kubectl -n wsc2026 get pods -o wide
kubectl -n wsc2026 describe deploy wsc2026-book-deploy
```

확인할 것:

- replicas 2
- probe 3종 설정
- `book-config`에서 환경변수 주입
- ServiceAccount가 Pod Identity와 연결
- PDB minAvailable 1

### 15.6 API 검증

ALB 또는 CloudFront 경유로 POST를 테스트한다.

```bash
curl -i -X POST "https://<cloudfront-domain>/booking" \
  -H "Content-Type: application/json" \
  -d '{"client_id":"C001","username":"Alice","email":"kim@example.com","concert_name":"Seoul2025"}'
```

응답에서 `booking_id`가 나와야 한다.

GET 테스트:

```bash
curl -i "https://<cloudfront-domain>/v1/book?booking_id=<booking_id>"
```

응답 필드 순서가 문제의 Response와 같아야 한다.

### 15.7 CloudFront/WAF 검증

```bash
curl -I "https://<cloudfront-domain>/"
curl -I "https://<cloudfront-domain>/static/index.html"
```

확인할 것:

- 정적 페이지 접근 가능
- `/booking`은 ALB Origin으로 전달
- `/v1/book`은 Lambda Origin으로 전달
- S3는 캐싱 활성화
- ALB/Lambda는 캐싱 비활성화
- WAF WebACL 연결
- SQLi/XSS managed rule과 rate limit rule 존재

### 15.8 Observability 검증

```bash
kubectl -n observability get pods,svc
kubectl -n observability get svc grafana
```

확인할 것:

- Prometheus, Alertmanager, Grafana 정상 실행
- Node Exporter, Fluent Bit DaemonSet 실행
- Grafana LoadBalancer 주소 접속 가능
- Dashboard 이름 `wsc2026-grafana-dashboard`
- CloudWatch Logs 데이터소스에서 앱 로그 조회 가능
- `/health` 로그 제외

## 16. 실수 방지 체크리스트

- 모든 리소스 리전이 `ap-northeast-2`인지 확인
- `<비번호>`, `<임의 영문 4자리>`를 그대로 두지 않았는지 확인
- KMS Key Policy에 root, `kms:*` 과다 권한이 없는지 확인
- EKS Cluster가 Public Endpoint로 열려 있지 않은지 확인
- EKS/Node IAM Role에 Administrator 권한을 붙이지 않았는지 확인
- Application Pod가 addon node에 뜨지 않는지 확인
- ConfigMap이 아니라 Deployment에 환경변수를 직접 적지 않았는지 확인
- `book` 이미지가 amd64로 빌드됐는지 확인
- `/booking`이 실제 `/v1/book`으로 전달되는지 확인
- Lambda GET 응답 컬럼 순서가 맞는지 확인
- CloudFront에서 S3는 캐싱, ALB/Lambda는 캐싱 비활성화인지 확인
- Fluent Bit이 `/health` 로그를 제외하는지 확인
- Grafana 비밀번호가 `Skills$#$@!`인지 확인

