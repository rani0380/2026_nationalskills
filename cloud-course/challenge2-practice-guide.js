(() => {
  const grading = document.getElementById('grading');
  const modulesSection = document.querySelector('.modules');
  if (!grading || !modulesSection) return;
  const guide = document.createElement('section');
  guide.className = 'walkthrough';
  guide.innerHTML = `
  <style>
    .walkthrough{margin-top:18px}.walkthrough>h2{margin:0 0 6px}.guide-nav{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:16px 0}.guide-nav a{padding:12px;border:1px solid var(--line);border-radius:12px;background:#102540;color:var(--text);text-decoration:none;font-weight:800;text-align:center}.guide{margin-top:16px;border:1px solid var(--line);border-radius:20px;background:rgba(16,28,48,.95);overflow:hidden}.guide>summary{list-style:none;cursor:pointer;padding:22px 24px;font-size:22px;font-weight:900;color:var(--text)}.guide>summary::-webkit-details-marker{display:none}.guide>summary::after{content:'＋';float:right;color:var(--blue)}.guide[open]>summary::after{content:'−'}.guide-body{padding:0 24px 26px}.phase{margin:18px 0;padding:18px;border:1px solid var(--line);border-radius:15px;background:#0b1729}.phase h3{margin:0 0 10px}.flow{padding:14px;border-radius:12px;background:#071220;color:#cde7ff;font-family:ui-monospace,Consolas,monospace;white-space:pre-wrap}.todo{counter-reset:item;list-style:none;padding:0}.todo>li{counter-increment:item;margin:10px 0;padding-left:38px;position:relative}.todo>li::before{content:counter(item);position:absolute;left:0;top:0;width:27px;height:27px;display:grid;place-items:center;border-radius:9px;background:#21466f;color:#fff;font-weight:900}.warn{border-left:4px solid var(--red);background:#29151d;padding:12px 14px;border-radius:8px}.success{border-left:4px solid var(--green);background:#10271f;padding:12px 14px;border-radius:8px}.guide table{width:100%;border-collapse:collapse}.guide th,.guide td{padding:9px;border-bottom:1px solid var(--line);text-align:left;vertical-align:top}.guide th{color:var(--blue)}@media(max-width:800px){.guide-nav{grid-template-columns:repeat(2,1fr)}.guide>summary{font-size:18px;padding:18px}.guide-body{padding:0 16px 20px}}
  </style>
  <h2>학생용 단계별 풀이 가이드</h2><p class="muted">문제를 먼저 읽고 직접 설계한 뒤 막히는 단계만 펼쳐 보세요. 명령을 복사하기 전에 현재 리전과 리소스 이름을 반드시 확인합니다.</p>
  <nav class="guide-nav"><a href="#guide-m1">모듈 1 풀이</a><a href="#guide-m2">모듈 2 풀이</a><a href="#guide-m3">모듈 3 풀이</a><a href="#guide-m4">모듈 4 풀이</a></nav>

  <details class="guide" id="guide-m1" open><summary>모듈 1 · DynamoDB 주문 애플리케이션 풀이 흐름</summary><div class="guide-body">
    <div class="flow">Internet → IGW → Public Subnet → EC2:8080 → Instance Role → DynamoDB<br>                                                     ├─ practice-orders<br>                                                     ├─ practice-products<br>                                                     └─ practice-sessions</div>
    <div class="phase"><h3>1단계 — 네트워크와 EC2부터 준비</h3><ol class="todo"><li><code>10.81.0.0/16</code> VPC와 <code>10.81.1.0/24</code> Public Subnet을 생성합니다.</li><li>Internet Gateway를 VPC에 연결하고 Public Route Table에 <code>0.0.0.0/0 → IGW</code>를 추가합니다.</li><li>EC2 Security Group은 TCP/8080을 허용합니다. SSH가 필요하면 자신의 IP로만 제한합니다.</li><li>Amazon Linux 2023, <code>t3.micro</code>, Public IP Enabled로 Client를 생성합니다.</li></ol><p class="success">확인: EC2가 running이고 Public IP가 있으며 Subnet Route Table에 IGW 기본 경로가 있어야 합니다.</p></div>
    <div class="phase"><h3>2단계 — 어떤 번호로 찾고, 어떤 순서로 보여줄지 정하기</h3><table><tr><th>테이블</th><th>한 건을 찾는 기본 번호</th><th>추가 검색표 (AWS 화면 이름: GSI)</th></tr><tr><td><code>practice-orders</code><br>주문 테이블</td><td><code>orderId</code> · 글자(String)<br>예: <code>O-2001</code></td><td>AWS에 입력할 이름: <code>CustomerCreatedAtIndex</code><br>먼저 <code>customerId</code>로 같은 고객의 주문을 모으고, <code>createdAt</code>으로 주문 날짜순 정렬합니다. 예: C101의 최신 주문부터 조회</td></tr><tr><td><code>practice-products</code><br>상품 테이블</td><td><code>productId</code> · 글자(String)<br>예: <code>P-100</code></td><td>AWS에 입력할 이름: <code>WarehouseStockIndex</code><br>먼저 <code>warehouseId</code>로 같은 창고의 상품을 모으고, 숫자 <code>stock</code>으로 재고 수량순 정렬합니다. 예: WH-A의 재고가 적은 상품부터 조회</td></tr><tr><td><code>practice-sessions</code><br>세션 테이블</td><td><code>sessionId</code> · 글자(String)<br>예: <code>S-001</code></td><td><code>expiresAt</code> TTL<br>세션이 끝나는 시각을 초 단위 숫자로 저장합니다. 시간이 지나면 DynamoDB가 만료된 세션을 자동 삭제합니다. 예: <code>1787212800</code></td></tr></table><p class="success"><strong>용어를 쉽게:</strong> GSI는 기본 번호 말고 고객이나 창고를 기준으로 찾기 위해 만드는 <strong>추가 검색표</strong>입니다. PK는 어떤 데이터를 한 묶음으로 모을지 정하고, SK는 그 묶음을 어떤 순서로 보여줄지 정합니다. TTL은 유효기간이 지난 데이터를 자동으로 지우는 기능입니다.</p><h3>한글 콘솔에서 만드는 방법</h3><div class="success"><strong>인덱스 메뉴 위치:</strong> DynamoDB → 테이블 → 원하는 테이블 이름 클릭 → 화면 위쪽의 <strong>인덱스</strong> 탭 → <strong>인덱스 생성</strong></div><div class="warn"><strong>현재 한글 화면에서 버튼 찾기:</strong> 인덱스 탭에는 위쪽의 <strong>벡터 인덱스</strong>와 아래쪽의 <strong>글로벌 보조 인덱스</strong>가 함께 표시됩니다. 이 문제에서는 벡터 인덱스를 사용하지 않습니다. 화면을 아래로 내려 <strong>글로벌 보조 인덱스 (0)</strong> 영역에 있는 <strong>인덱스 생성</strong> 버튼을 누르세요.</div><div class="notice"><strong>테이블 이름 확인:</strong> 화면에 보이는 <code>wskorea26-data-table</code>은 현재 계정에 이미 있는 다른 테이블입니다. 이 모의문제는 <code>practice-orders</code>, <code>practice-products</code>, <code>practice-sessions</code>를 각각 새로 만든 뒤 해당 테이블의 인덱스 탭에서 작업합니다.</div><ol class="todo"><li><strong>주문 테이블 만들기:</strong> AWS 콘솔에서 <strong>DynamoDB → 테이블 → 테이블 생성</strong>을 선택합니다. 테이블 이름은 <code>practice-orders</code>, 파티션 키는 <code>orderId</code>, 데이터 유형은 <strong>문자열</strong>을 선택합니다.</li><li><strong>용량과 암호화 설정:</strong> <strong>설정 사용자 지정</strong>을 선택하고 용량 모드는 <strong>온디맨드</strong>로 설정합니다. 암호화에서는 <strong>고객 관리형 키</strong>를 선택한 뒤 <code>alias/practice-orders-key</code>를 고릅니다. <strong>테이블 생성</strong>을 누르고 테이블 상태가 <strong>활성</strong>이 될 때까지 기다립니다.</li><li><strong>주문 인덱스 생성:</strong> <strong>테이블 → practice-orders → 인덱스 → 인덱스 생성</strong>을 선택합니다. 파티션 키는 <code>customerId</code> / <strong>문자열</strong>, 정렬 키는 <code>createdAt</code> / <strong>문자열</strong>, 인덱스 이름은 <code>CustomerCreatedAtIndex</code>, 속성 프로젝션은 <strong>모든 속성</strong>으로 설정한 뒤 <strong>인덱스 생성</strong>을 누릅니다.</li><li><strong>상품 테이블 만들기:</strong> <strong>DynamoDB → 테이블 → 테이블 생성</strong>에서 테이블 이름 <code>practice-products</code>, 파티션 키 <code>productId</code> / <strong>문자열</strong>로 생성합니다. 상태가 <strong>활성</strong>이 될 때까지 기다립니다.</li><li><strong>상품 인덱스 생성:</strong> <strong>테이블 → practice-products → 인덱스 → 인덱스 생성</strong>으로 이동합니다. 파티션 키는 <code>warehouseId</code> / <strong>문자열</strong>, 정렬 키는 <code>stock</code> / <strong>숫자</strong>, 인덱스 이름은 <code>WarehouseStockIndex</code>, 속성 프로젝션은 <strong>모든 속성</strong>으로 설정하고 생성합니다.</li><li><strong>세션 테이블과 TTL:</strong> <strong>테이블 생성</strong>에서 이름 <code>practice-sessions</code>, 파티션 키 <code>sessionId</code> / <strong>문자열</strong>로 생성합니다. 생성 후 <strong>테이블 → practice-sessions → 설정 → Time To Live(TTL) → 켜기</strong>를 선택하고 TTL 속성 이름에 <code>expiresAt</code>을 입력합니다.</li><li><strong>항목 입력:</strong> 각 테이블을 선택한 뒤 <strong>표 항목 탐색 → 항목 생성</strong>으로 이동합니다. <strong>새 속성 추가</strong>를 사용해 값을 넣습니다. <code>stock</code>과 <code>expiresAt</code>은 <strong>숫자</strong>, <code>createdAt</code>은 <strong>문자열</strong>을 선택합니다.</li><li><strong>생성 결과 확인:</strong> 각 테이블의 <strong>인덱스</strong> 탭에서 인덱스 상태가 <strong>활성</strong>인지 확인합니다. 이어서 <strong>표 항목 탐색 → 쿼리</strong>에서 테이블 또는 인덱스를 선택해 C101 주문과 WH-A 상품을 조회합니다.</li></ol><div class="phase" style="margin:12px 0"><h3>인덱스 입력값 한눈에 보기</h3><table><tr><th>대상</th><th>파티션 키</th><th>정렬 키</th><th>인덱스 이름</th><th>속성 프로젝션</th></tr><tr><td>주문</td><td>customerId · 문자열</td><td>createdAt · 문자열</td><td>CustomerCreatedAtIndex</td><td>모든 속성</td></tr><tr><td>상품</td><td>warehouseId · 문자열</td><td>stock · 숫자</td><td>WarehouseStockIndex</td><td>모든 속성</td></tr></table></div><p class="warn"><strong>인덱스 생성 버튼이 안 보이면:</strong> 테이블의 <strong>설정</strong> 화면이 아니라, 같은 줄에 있는 <strong>인덱스</strong> 탭을 눌렀는지 확인하세요. 인덱스를 만든 뒤 상태가 <strong>생성 중</strong>에서 <strong>활성</strong>으로 바뀌어야 사용할 수 있습니다.</p><p class="warn"><strong>입력할 때 꼭 확인:</strong> 세션의 <code>expiresAt</code>은 <code>"2026-08-20"</code> 같은 날짜 글자가 아니라 <code>1787212800</code>처럼 초로 계산한 숫자를 넣습니다. 고객별 주문을 찾으려면 각 주문에 <code>customerId</code>와 <code>createdAt</code>이 모두 있어야 하고, 창고별 상품을 찾으려면 각 상품에 <code>warehouseId</code>와 숫자 <code>stock</code>이 모두 있어야 합니다.</p></div>
    <div class="phase"><h3>3단계 — 암호화·백업·데이터</h3><h3>① 한글 콘솔에서 KMS 키와 별칭 만들기</h3><ol class="todo"><li>서울 리전에서 <strong>Key Management Service → 고객 관리형 키 → 키 생성</strong>을 선택합니다.</li><li>키 유형은 <strong>대칭</strong>, 키 사용은 <strong>암호화 및 해독</strong>을 선택하고 다음으로 이동합니다.</li><li>별칭 입력란에는 <code>practice-orders-key</code>만 입력합니다. 콘솔이 자동으로 앞에 <code>alias/</code>를 붙이므로 최종 별칭은 <code>alias/practice-orders-key</code>가 됩니다.</li><li>키 관리자와 키 사용 권한을 지정하고 키 생성을 완료합니다. <strong>고객 관리형 키</strong> 목록에서 상태가 <strong>활성화됨</strong>인지 확인합니다.</li></ol><p class="warn"><strong>주의:</strong> 별칭 입력란에 <code>alias/practice-orders-key</code> 전체를 넣지 마세요. 한글 콘솔에서는 <code>practice-orders-key</code>만 입력하면 됩니다.</p><h3>② Orders 테이블 암호화와 PITR 켜기</h3><ol class="todo"><li><strong>DynamoDB → 테이블 → practice-orders → 설정</strong>으로 이동합니다.</li><li><strong>암호화 정보 → 암호화 관리</strong>를 선택합니다. 키 관리에서 <strong>고객 관리형 키</strong>를 선택하고 <code>alias/practice-orders-key</code>를 지정한 뒤 변경 사항을 저장합니다. 테이블 생성할 때 이미 이 키를 선택했다면 다시 변경하지 않아도 됩니다.</li><li>같은 설정 화면의 <strong>특정 시점으로 복구(PITR) → PITR 편집</strong>을 누르고 <strong>특정 시점으로 복구 켜기</strong>를 선택해 저장합니다.</li><li>설정 화면에서 키 관리가 <strong>고객 관리형 키</strong>, PITR가 <strong>켜기</strong>로 표시되는지 확인합니다.</li></ol><p class="success"><strong>쉽게 이해하기:</strong> KMS 암호화는 저장된 주문 데이터를 보호하고, PITR은 실수로 데이터를 바꿨을 때 과거 시점의 테이블로 복원할 수 있게 합니다.</p><h3>③ 예제 데이터 넣기</h3><p>직접 입력해도 되지만, 실수를 줄이려면 제공된 예제 파일을 사용합니다. 파일에는 주문 8개, 상품 6개, 세션 3개가 들어 있습니다.</p><p><a class="btn alt" href="downloads/challenge2-practice/module1/dynamodb-seed.json" download>예제 데이터 dynamodb-seed.json 다운로드</a></p><pre>curl -fLO https://rani0380.github.io/2026_nationalskills/cloud-course/downloads/challenge2-practice/module1/dynamodb-seed.json<br>aws dynamodb batch-write-item --region ap-northeast-2 --request-items file://dynamodb-seed.json</pre><p>명령 결과의 <code>UnprocessedItems</code>가 빈 객체 <code>{}</code>이면 모두 입력된 것입니다. 값이 남아 있으면 같은 명령을 다시 실행해 미처리 항목을 넣습니다.</p><h3>④ 콘솔에서 직접 입력하는 경우</h3><ol class="todo"><li><strong>DynamoDB → 테이블 → 테이블 이름 → 표 항목 탐색 → 항목 생성</strong>으로 이동합니다.</li><li>Orders에는 <code>orderId</code>, <code>customerId</code>, <code>createdAt</code>, <code>status</code>, <code>total</code>을 넣습니다. <code>createdAt</code>은 문자열로 입력합니다.</li><li>C101 주문을 2개 이상 만들고 날짜를 다르게 합니다. 예: <code>O-2001 / C101 / 2026-08-01T09:00:00Z</code>, <code>O-2002 / C101 / 2026-08-02T09:00:00Z</code>.</li><li>Products의 <code>stock</code>은 문자열 <code>"10"</code>이 아니라 숫자 <code>10</code>으로 입력합니다. WH-A 상품을 3개 이상 넣고 재고값을 3, 10, 25처럼 다르게 합니다.</li><li>Sessions의 <code>expiresAt</code>은 숫자 타입으로 입력합니다. 예제의 <code>1798761600</code>은 2027년 이후 시각이므로 연습 중 바로 만료되지 않습니다.</li></ol><h3>⑤ 건수와 기능 확인</h3><pre># 주문 O-2001 한 건 확인<br>aws dynamodb get-item --region ap-northeast-2 --table-name practice-orders --key '{"orderId":{"S":"O-2001"}}'<br><br># 각 테이블의 항목 수 확인<br>aws dynamodb scan --region ap-northeast-2 --table-name practice-orders --select COUNT<br>aws dynamodb scan --region ap-northeast-2 --table-name practice-products --select COUNT<br>aws dynamodb scan --region ap-northeast-2 --table-name practice-sessions --select COUNT<br><br># C101 주문을 최신순으로 조회<br>aws dynamodb query --region ap-northeast-2 --table-name practice-orders --index-name CustomerCreatedAtIndex --key-condition-expression "customerId = :c" --expression-attribute-values '{":c":{"S":"C101"}}' --no-scan-index-forward<br><br># TTL 활성 상태 확인<br>aws dynamodb describe-time-to-live --region ap-northeast-2 --table-name practice-sessions<br><br># PITR 활성 상태 확인<br>aws dynamodb describe-continuous-backups --region ap-northeast-2 --table-name practice-orders</pre><table><tr><th>확인 대상</th><th>정상 결과</th></tr><tr><td>Orders 항목 수</td><td>Count 8 이상</td></tr><tr><td>Products 항목 수</td><td>Count 6 이상</td></tr><tr><td>Sessions 항목 수</td><td>Count 3 이상</td></tr><tr><td>C101 조회</td><td>O-2002가 O-2001보다 먼저 표시</td></tr><tr><td>TTL</td><td>TimeToLiveStatus가 ENABLED, AttributeName이 expiresAt</td></tr><tr><td>PITR</td><td>PointInTimeRecoveryStatus가 ENABLED</td></tr></table></div><div class="phase"><h3>4단계 — EC2 권한과 애플리케이션 배포</h3><h3>① EC2용 IAM 정책 만들기</h3><ol class="todo"><li>AWS 콘솔에서 <strong>IAM → 정책 → 정책 생성</strong>을 선택합니다.</li><li>정책 편집기에서 <strong>JSON</strong>을 선택하고 제공된 <code>iam-policy.json</code> 내용을 붙여 넣습니다. 이 정책은 세 테이블의 조회와 주문·상품 인덱스 조회만 허용합니다.</li><li><strong>다음</strong>을 누르고 정책 이름을 <code>practice-orders-read-policy</code>로 입력한 뒤 <strong>정책 생성</strong>을 누릅니다.</li></ol><p><a class="btn alt" href="downloads/challenge2-practice/module1/iam-policy.json" download>IAM 정책 iam-policy.json 다운로드</a></p><h3>② EC2용 IAM 역할 만들기</h3><ol class="todo"><li><strong>IAM → 역할 → 역할 생성</strong>을 선택합니다.</li><li>신뢰할 수 있는 엔터티 유형은 <strong>AWS 서비스</strong>, 사용 사례는 <strong>EC2</strong>를 선택합니다.</li><li>권한 추가 화면에서 <code>practice-orders-read-policy</code>를 검색해 선택합니다. Session Manager로 접속할 경우에만 <code>AmazonSSMManagedInstanceCore</code>도 추가합니다.</li><li>역할 이름을 <code>practice-orders-client-role</code>로 지정하고 역할을 생성합니다.</li></ol><h3>③ 역할을 Client EC2에 연결</h3><ol class="todo"><li><strong>EC2 → 인스턴스 → practice-orders-client</strong>를 선택합니다.</li><li><strong>작업 → 보안 → IAM 역할 수정</strong>을 선택합니다.</li><li><code>practice-orders-client-role</code>을 선택하고 <strong>IAM 역할 업데이트</strong>를 누릅니다.</li><li>EC2에 접속해 <code>aws sts get-caller-identity</code>를 실행합니다. 오류 없이 Role 정보가 나오면 Access Key를 직접 입력하지 않아도 AWS 권한을 사용할 수 있습니다.</li></ol><p class="warn"><strong>중요:</strong> 소스코드, 환경변수 또는 <code>aws configure</code>에 Access Key와 Secret Key를 저장하지 않습니다. EC2에 연결한 IAM 역할을 사용해야 합니다.</p><h3>④ Security Group과 네트워크 확인</h3><table><tr><th>확인 항목</th><th>정상 설정</th></tr><tr><td>EC2 Name 태그</td><td>practice-orders-client</td></tr><tr><td>Security Group Name 태그</td><td>practice-orders-client-sg</td></tr><tr><td>Inbound</td><td>SSH TCP/22 from <code>com.amazonaws.ap-northeast-2.ec2-instance-connect</code><br>Custom TCP/8080 from <code>0.0.0.0/0</code></td></tr><tr><td>Public IP</td><td>있음</td></tr><tr><td>Public Route</td><td>0.0.0.0/0 → practice-orders-igw</td></tr></table><h3>⑤ EC2에 애플리케이션 자동 배포</h3><p>이 실습은 <strong>Amazon Linux 2023</strong> 기준입니다. EC2 Instance Connect 또는 SSH로 <code>practice-orders-client</code>에 접속한 뒤 아래 순서대로 진행합니다. Session Manager는 사용하지 않아도 됩니다.</p><h4>1. 접속한 인스턴스와 IAM 역할 확인</h4><pre>whoami<br>hostname<br>aws sts get-caller-identity<br>curl -I https://rani0380.github.io</pre><table><tr><th>명령</th><th>정상 결과</th></tr><tr><td><code>whoami</code></td><td><code>ec2-user</code></td></tr><tr><td><code>aws sts get-caller-identity</code></td><td>응답의 ARN에 <code>practice-orders-client-role</code> 포함</td></tr><tr><td><code>curl -I</code></td><td><code>HTTP/2 200</code> 또는 정상 HTTP 응답</td></tr></table><p class="warn"><strong>curl에서 연결 오류가 나면:</strong> Public IP, Public Route <code>0.0.0.0/0 → practice-orders-igw</code>, 보안 그룹의 아웃바운드 HTTPS 허용을 먼저 확인합니다. <code>Unable to locate credentials</code>가 나오면 EC2에 IAM 역할이 연결되지 않은 것입니다.</p><h4>2. 배포 파일 내려받기와 실행</h4><pre>cd /home/ec2-user<br>curl -fLO https://rani0380.github.io/2026_nationalskills/cloud-course/downloads/challenge2-practice/module1/deploy.sh<br>ls -l deploy.sh<br>chmod +x deploy.sh<br>./deploy.sh</pre><p><code>curl -fLO</code>는 서버 오류가 있으면 실패로 표시하고 원래 파일 이름인 <code>deploy.sh</code>로 저장합니다. <code>chmod +x</code>는 파일을 실행 가능하게 만들며, <code>./deploy.sh</code>가 실제 설치를 시작합니다.</p><h4>3. 배포 스크립트가 자동으로 하는 작업</h4><ol class="todo"><li><code>dnf</code>로 Python 3와 pip를 설치합니다.</li><li><code>/opt/practice-orders</code> 폴더를 만들고 <code>app.py</code>와 <code>requirements.txt</code>를 내려받습니다.</li><li>Python 가상환경을 만들고 Flask와 boto3를 설치합니다.</li><li><code>practice-orders.service</code>를 systemd에 등록합니다.</li><li>서비스를 즉시 시작하고 부팅 시 자동 시작되도록 설정합니다.</li></ol><p class="success">마지막에 <code>Active: active (running)</code>이 나타나면 설치가 완료된 것입니다. 중간에 빨간 오류가 나타나면 마지막 오류 문장을 확인하고 아래 ⑥ 단계에서 로그를 조회합니다.</p><p class="notice"><strong>다시 실행해도 됨:</strong> 설치가 중간에 실패했다면 원인을 수정한 뒤 <code>./deploy.sh</code>를 다시 실행할 수 있습니다. 단, Amazon Linux 2023이 아닌 Ubuntu에서는 <code>dnf</code>와 <code>ec2-user</code>가 없어 이 스크립트를 그대로 사용할 수 없습니다.</p><div class="actions"><a class="btn alt" href="downloads/challenge2-practice/module1/deploy.sh" download>deploy.sh 다운로드</a><a class="btn alt" href="downloads/challenge2-practice/module1/practice-orders.service" download>systemd 서비스 파일</a></div><h3>⑥ EC2 내부에서 실행 상태 확인</h3><h4>1. 서비스가 실행 중인지 확인</h4><pre>sudo systemctl is-enabled practice-orders<br>sudo systemctl is-active practice-orders<br>sudo systemctl status practice-orders --no-pager</pre><table><tr><th>확인 결과</th><th>뜻</th></tr><tr><td><code>enabled</code></td><td>EC2를 재부팅해도 자동 실행됨</td></tr><tr><td><code>active</code></td><td>현재 애플리케이션이 실행 중임</td></tr><tr><td><code>failed</code> 또는 <code>inactive</code></td><td>아래 로그 확인 후 서비스 재시작 필요</td></tr></table><h4>2. 실패했다면 로그 확인</h4><pre>sudo journalctl -u practice-orders -n 50 --no-pager<br>sudo journalctl -u practice-orders --since "10 minutes ago" --no-pager<br>sudo systemctl restart practice-orders<br>sudo systemctl status practice-orders --no-pager</pre><p>로그는 아래에서 위로 읽기보다 <strong>가장 마지막 오류 문장</strong>부터 확인합니다. <code>ModuleNotFoundError</code>는 Python 패키지 설치, <code>Address already in use</code>는 8080 포트 중복, <code>AccessDeniedException</code>은 IAM 역할 또는 정책 문제입니다.</p><h4>3. 8080 포트와 Health API 확인</h4><pre>sudo ss -lntp | grep 8080<br>curl -i http://127.0.0.1:8080/health</pre><p><code>ss</code> 결과에 <code>0.0.0.0:8080</code>이 있고, curl 결과가 <code>HTTP/1.1 200 OK</code>이면서 본문에 <code>"status":"ok"</code>, <code>"database":"dynamodb"</code>, <code>"region":"ap-northeast-2"</code>가 나오면 <strong>웹 애플리케이션 자체는 정상</strong>입니다.</p><p class="warn"><strong>중요:</strong> <code>/health</code>는 DynamoDB를 실제로 조회하지 않습니다. 따라서 이 응답만으로 IAM 권한과 DynamoDB 연결까지 정상이라고 판단하면 안 됩니다.</p><h4>4. DynamoDB를 직접 조회해 권한 확인</h4><pre>aws dynamodb get-item --region ap-northeast-2 --table-name practice-orders --key '{"orderId":{"S":"O-2001"}}'<br><br>curl -i http://127.0.0.1:8080/v1/orders/O-2001<br>curl -i http://127.0.0.1:8080/v1/customers/C101/orders<br>curl -i "http://127.0.0.1:8080/v1/products/low-stock?warehouseId=WH-A"</pre><table><tr><th>확인 항목</th><th>정상 결과</th></tr><tr><td>DynamoDB CLI 조회</td><td><code>Item</code> 안에 <code>orderId: O-2001</code> 표시</td></tr><tr><td>주문 한 건 API</td><td>HTTP 200과 O-2001 주문 정보</td></tr><tr><td>C101 주문 API</td><td>주문 2개 이상, 최신 <code>createdAt</code>이 먼저 표시</td></tr><tr><td>WH-A 재고 API</td><td>WH-A 상품 목록이 숫자 <code>stock</code> 순서로 표시</td></tr></table><p class="success"><strong>최종 내부 정상 기준:</strong> 서비스가 <code>active</code>이고, <code>/health</code>와 DynamoDB CLI 조회 및 세 개의 데이터 API가 모두 정상이어야 앱·IAM 역할·DynamoDB 연결이 모두 정상입니다.</p><h4>5. 자주 나오는 오류</h4><table><tr><th>오류</th><th>원인과 해결</th></tr><tr><td><code>curl: command not found</code></td><td><code>sudo dnf install -y curl</code></td></tr><tr><td><code>Unable to locate credentials</code></td><td><code>practice-orders-client-role</code>을 EC2에 연결</td></tr><tr><td><code>AccessDeniedException</code></td><td><code>practice-orders-read-policy</code>와 <code>index/*</code> ARN 권한 확인</td></tr><tr><td><code>ResourceNotFoundException</code></td><td>서울 리전과 테이블 이름 확인</td></tr><tr><td><code>Connection refused</code></td><td>서비스 상태와 8080 Listen 여부 확인</td></tr><tr><td>O-2001이 나오지 않음</td><td>3단계에서 예제 데이터가 입력됐는지 확인</td></tr></table><h3>⑦ 외부에서 최종 확인</h3><pre>CLIENT_IP=$(aws ec2 describe-instances --region ap-northeast-2 --filters Name=tag:Name,Values=practice-orders-client Name=instance-state-name,Values=running --query 'Reservations[0].Instances[0].PublicIpAddress' --output text)<br>curl -s "http://\${CLIENT_IP}:8080/health"<br>curl -s "http://\${CLIENT_IP}:8080/v1/orders/O-2001"<br>curl -s "http://\${CLIENT_IP}:8080/v1/customers/C101/orders"<br>curl -s "http://\${CLIENT_IP}:8080/v1/products/low-stock?warehouseId=WH-A"</pre><table><tr><th>증상</th><th>확인할 곳</th></tr><tr><td>AccessDeniedException</td><td>EC2 IAM 역할 연결, practice-orders-read-policy, index/* ARN</td></tr><tr><td>내부 curl 실패</td><td>systemctl status와 journalctl 로그</td></tr><tr><td>내부 성공·외부 실패</td><td>Public IP, IGW Route, Security Group TCP/8080</td></tr><tr><td>고객·창고 조회 실패</td><td>인덱스 상태 활성, 이름·키 타입·데이터 속성</td></tr></table></div><div class="phase"><h3>5단계 — 제출 전 전체 검증과 오류 해결</h3><h3>① 자동 점검 스크립트부터 실행</h3><p>CloudShell에서 아래 명령을 실행합니다. 이 스크립트는 리소스를 생성·수정·삭제하지 않고 현재 상태만 조회합니다.</p><pre>curl -fLO https://rani0380.github.io/2026_nationalskills/cloud-course/downloads/challenge2-practice/module1/validate-module1.sh<br>chmod +x validate-module1.sh<br>./validate-module1.sh</pre><p><a class="btn alt" href="downloads/challenge2-practice/module1/validate-module1.sh" download>Module 1 자동 점검 스크립트 다운로드</a></p><table><tr><th>표시</th><th>뜻</th><th>다음 행동</th></tr><tr><td class="pass"><strong>PASS</strong></td><td>해당 조건을 만족함</td><td>다음 항목으로 이동</td></tr><tr><td class="fail"><strong>FAIL</strong></td><td>리소스를 찾지 못했거나 설정·기능이 다름</td><td>아래 수동 점검표에서 같은 항목 확인</td></tr></table><h3>② 한글 콘솔에서 이름과 상태 대조</h3><ol class="todo"><li><strong>VPC → 내 VPC</strong>에서 Name 태그 <code>practice-orders-vpc</code>, IPv4 CIDR <code>10.81.0.0/16</code>을 확인합니다.</li><li><strong>EC2 → 인스턴스</strong>에서 <code>practice-orders-client</code>가 <strong>실행 중</strong>, 인스턴스 유형 <code>t3.micro</code>, 퍼블릭 IPv4 주소 있음인지 확인합니다.</li><li><strong>DynamoDB → 테이블</strong>에서 <code>practice-orders</code>, <code>practice-products</code>, <code>practice-sessions</code> 세 테이블이 모두 <strong>활성</strong>인지 확인합니다.</li><li>Orders와 Products의 <strong>인덱스</strong> 탭에서 <code>CustomerCreatedAtIndex</code>, <code>WarehouseStockIndex</code> 상태가 <strong>활성</strong>인지 확인합니다.</li><li>Sessions의 <strong>설정 → Time To Live(TTL)</strong>에서 상태 <strong>켜기</strong>, 속성 <code>expiresAt</code>을 확인합니다.</li><li>Orders의 <strong>설정</strong>에서 암호화 키가 고객 관리형 <code>alias/practice-orders-key</code>, PITR가 <strong>켜기</strong>인지 확인합니다.</li></ol><h3>③ 데이터 건수와 타입 점검</h3><table><tr><th>테이블</th><th>최소 수량</th><th>반드시 확인할 데이터</th></tr><tr><td>practice-orders</td><td>8개</td><td>O-2001 존재, C101 주문 2개, createdAt 문자열 날짜</td></tr><tr><td>practice-products</td><td>6개</td><td>WH-A 상품 3개, stock은 숫자</td></tr><tr><td>practice-sessions</td><td>3개</td><td>expiresAt은 초 단위 숫자</td></tr></table><pre>aws dynamodb scan --region ap-northeast-2 --table-name practice-orders --select COUNT<br>aws dynamodb scan --region ap-northeast-2 --table-name practice-products --select COUNT<br>aws dynamodb scan --region ap-northeast-2 --table-name practice-sessions --select COUNT</pre><h3>④ API 네 개를 실제 채점 방식으로 확인</h3><pre>CLIENT_IP=$(aws ec2 describe-instances --region ap-northeast-2 --filters Name=tag:Name,Values=practice-orders-client Name=instance-state-name,Values=running --query 'Reservations[0].Instances[0].PublicIpAddress' --output text)<br>echo "CLIENT_IP=$CLIENT_IP"<br>curl -i -m 10 "http://\${CLIENT_IP}:8080/health"<br>curl -i -m 10 "http://\${CLIENT_IP}:8080/v1/orders/O-2001"<br>curl -i -m 10 "http://\${CLIENT_IP}:8080/v1/customers/C101/orders"<br>curl -i -m 10 "http://\${CLIENT_IP}:8080/v1/products/low-stock?warehouseId=WH-A"</pre><table><tr><th>요청</th><th>정상 기준</th></tr><tr><td>/health</td><td>HTTP 200, status=ok, database=dynamodb, region=ap-northeast-2</td></tr><tr><td>/v1/orders/O-2001</td><td>HTTP 200, 응답에 orderId O-2001 포함</td></tr><tr><td>/v1/customers/C101/orders</td><td>HTTP 200, C101 주문 2개 이상, 최신 주문이 먼저 표시</td></tr><tr><td>/v1/products/low-stock?warehouseId=WH-A</td><td>HTTP 200, WH-A 상품 목록 반환</td></tr></table><h3>⑤ 실패 위치를 빠르게 찾는 순서</h3><div class="flow">외부 curl 실패<br>├─ EC2 내부 curl도 실패 → 앱/systemd 문제<br>│  ├─ systemctl status → 실행 상태 확인<br>│  └─ journalctl → Python·DynamoDB 오류 확인<br>└─ EC2 내부 curl은 성공 → 네트워크 문제<br>   ├─ Public IP 확인<br>   ├─ Security Group TCP/8080 확인<br>   └─ Public Route 0.0.0.0/0 → IGW 확인</div><table><tr><th>오류·증상</th><th>주요 원인</th><th>확인 명령·메뉴</th></tr><tr><td>AccessDeniedException</td><td>IAM 역할 미연결 또는 인덱스 ARN 권한 누락</td><td>EC2 → 작업 → 보안 → IAM 역할 수정</td></tr><tr><td>ResourceNotFoundException</td><td>테이블 이름 또는 리전 오류</td><td><code>aws dynamodb list-tables --region ap-northeast-2</code></td></tr><tr><td>ValidationException</td><td>인덱스 키 타입·이름 또는 데이터 속성 오류</td><td>DynamoDB → 테이블 → 인덱스</td></tr><tr><td>Connection refused</td><td>앱이 8080에서 실행되지 않음</td><td><code>sudo systemctl status practice-orders</code></td></tr><tr><td>Connection timed out</td><td>SG·Route·Public IP 문제</td><td>EC2 네트워크 및 보안 탭</td></tr><tr><td>/health 500</td><td>앱은 실행됐지만 DynamoDB 접근 실패</td><td><code>sudo journalctl -u practice-orders -n 100</code></td></tr></table><h3>⑥ 최종 제출 체크리스트</h3><ul><li>모든 리소스가 서울 <code>ap-northeast-2</code>에 있습니다.</li><li>요구된 이름과 Name 태그의 철자·대소문자가 정확합니다.</li><li>세 테이블과 두 인덱스가 <strong>활성</strong>입니다.</li><li>Orders 암호화가 고객 관리형 KMS이고 PITR가 켜져 있습니다.</li><li>Sessions TTL가 <code>expiresAt</code>으로 켜져 있습니다.</li><li>요구 수량의 데이터와 필수 테스트 항목이 존재합니다.</li><li>EC2에 IAM 역할이 연결되어 있고 Access Key를 저장하지 않았습니다.</li><li>네 API가 Public IP에서 10초 안에 HTTP 200을 반환합니다.</li><li>자동 점검 결과의 FAIL 항목을 모두 해결했습니다.</li><li>채점이 끝나기 전에는 리소스를 중지·삭제·이름 변경하지 않습니다.</li></ul><p class="success"><strong>완료 기준:</strong> 자동 점검이 모두 PASS이고 네 API 응답이 정상일 때 Module 1 제출 준비가 완료된 것입니다.</p></div></div>
  </div></details>

  <details class="guide" id="guide-m2" open><summary>모듈 2 · VPC Lattice 서비스 연결 단계별 실습</summary><div class="guide-body">
    <div class="flow">Internet → practice-lattice-client:80 → Client VPC Association → practice-lattice-sn<br>                                                                                     └→ practice-lattice-inventory-service:80<br>                                                                                         └→ practice-lattice-inventory-tg:8080 → Private Service EC2</div>
    <div class="phase"><h3>시작 전 — 리소스 이름과 역할 이해</h3><table><tr><th>구분</th><th>정확한 이름</th><th>역할</th></tr><tr><td>Client VPC</td><td><code>practice-lattice-client-vpc</code></td><td>외부 요청을 받는 Client EC2가 위치</td></tr><tr><td>Service VPC</td><td><code>practice-lattice-service-vpc</code></td><td>Public IP 없는 Inventory EC2가 위치</td></tr><tr><td>서비스 네트워크</td><td><code>practice-lattice-sn</code></td><td>Client VPC와 Lattice 서비스를 연결</td></tr><tr><td>Lattice 서비스</td><td><code>practice-lattice-inventory-service</code></td><td>Inventory API의 Lattice 주소 제공</td></tr><tr><td>대상 그룹</td><td><code>practice-lattice-inventory-tg</code></td><td>요청을 Service EC2의 8080으로 전달</td></tr><tr><td>리스너</td><td><code>practice-lattice-http-listener</code></td><td>HTTP 80 요청을 대상 그룹으로 전달</td></tr></table><p class="notice"><strong>리전 주의:</strong> 모듈 2의 모든 리소스는 도쿄 <code>ap-northeast-1</code>에 만듭니다. 서울 리전에 만들면 이름이 맞아도 채점되지 않습니다.</p></div>
    <div class="phase"><h3>1단계 — 도쿄 리전에 두 VPC와 EC2 만들기</h3><h4>① Client VPC와 Public Subnet</h4><ol class="todo"><li>콘솔 우측 상단 리전을 <strong>아시아 태평양(도쿄) ap-northeast-1</strong>로 변경합니다.</li><li><strong>VPC → VPC 생성 → VPC만</strong>에서 이름 <code>practice-lattice-client-vpc</code>, IPv4 CIDR <code>10.82.0.0/16</code>으로 생성합니다.</li><li><strong>서브넷 → 서브넷 생성</strong>에서 이름 <code>practice-lattice-client-public-subnet</code>, CIDR <code>10.82.1.0/24</code>로 생성합니다.</li><li>Internet Gateway <code>practice-lattice-client-igw</code>를 만들고 Client VPC에 연결합니다.</li><li>Route Table <code>practice-lattice-client-public-rt</code>를 만들고 Public Subnet을 명시적으로 연결한 뒤 경로 <code>0.0.0.0/0 → practice-lattice-client-igw</code>를 추가합니다.</li><li><strong>VPC → 내 VPC → 작업 → VPC 설정 편집</strong>에서 DNS 확인과 DNS 호스트 이름을 모두 켭니다.</li></ol><h4>② Service VPC와 Private Subnet</h4><ol class="todo"><li>VPC <code>practice-lattice-service-vpc</code>, CIDR <code>10.83.0.0/16</code>을 생성합니다.</li><li>Private Subnet <code>practice-lattice-service-private-subnet</code>, CIDR <code>10.83.1.0/24</code>을 생성합니다.</li><li>Service VPC도 DNS 확인과 DNS 호스트 이름을 켭니다.</li><li>Service Private Subnet의 Route Table에는 IGW 경로를 직접 연결하지 않습니다.</li></ol><h4>③ EC2 두 대 생성</h4><table><tr><th>항목</th><th>Client EC2</th><th>Service EC2</th></tr><tr><td>Name</td><td><code>practice-lattice-client</code></td><td><code>practice-lattice-inventory</code></td></tr><tr><td>AMI / 유형</td><td>Amazon Linux 2023 / t3.micro</td><td>Amazon Linux 2023 / t3.micro</td></tr><tr><td>VPC / Subnet</td><td>Client VPC / Public Subnet</td><td>Service VPC / Private Subnet</td></tr><tr><td>Public IP</td><td>활성화</td><td><strong>비활성화</strong></td></tr></table><p class="warn"><strong>금지:</strong> 두 VPC 사이에 VPC Peering, Transit Gateway, VPN 또는 PrivateLink Endpoint Service를 만들지 않습니다. 두 VPC의 애플리케이션 통신은 VPC Lattice가 담당합니다.</p></div>
    <div class="phase"><h3>2단계 — Private Service EC2에 Inventory 앱 설치</h3><h4>① 먼저 설치용 외부 통신 경로 결정</h4><p>Service EC2에는 Public IP가 없으므로 GitHub와 패키지 저장소에서 파일을 받으려면 별도 아웃바운드 경로가 필요합니다.</p><ul><li><strong>연습에서 가장 쉬운 방법:</strong> Service VPC에 Public Subnet과 NAT Gateway를 만들고 Private Route Table의 <code>0.0.0.0/0</code>을 NAT Gateway로 보냅니다.</li><li><strong>폐쇄망 방식:</strong> 필요한 파일과 Python 패키지를 내부 S3 등에 미리 준비합니다. 초급 실습에서는 NAT 방식이 이해하기 쉽습니다.</li></ul><p class="notice">NAT Gateway는 VPC 간 직접 연결이 아니므로 Peering 금지 조건과 충돌하지 않습니다. 다만 비용이 발생하므로 연습이 끝난 뒤 불필요하면 AWS 콘솔에서 정리합니다.</p><h4>② EC2 생성 시 사용자 데이터로 자동 설치</h4><p>Service EC2를 만들 때 <strong>고급 세부 정보 → 사용자 데이터</strong>에 아래 내용을 입력하면 Private EC2에 직접 로그인하지 않아도 설치됩니다. 이때 NAT 경로가 먼저 준비되어 있어야 합니다.</p><pre>#!/bin/bash<br>curl -fsSL https://rani0380.github.io/2026_nationalskills/cloud-course/downloads/challenge2-practice/module2/deploy-service.sh -o /tmp/deploy-service.sh<br>chmod +x /tmp/deploy-service.sh<br>/tmp/deploy-service.sh</pre><p>이미 EC2를 만든 경우에는 Session Manager 등 Private EC2에 접속 가능한 방법이 있을 때 같은 명령을 실행할 수 있습니다. 설치 스크립트는 앱을 <code>/opt/practice-lattice-service</code>에 설치하고 부팅 시 자동 실행되는 <code>practice-lattice-service.service</code>를 등록합니다.</p><div class="actions"><a class="btn alt" href="downloads/challenge2-practice/module2/deploy-service.sh" download>Service 배포 스크립트</a><a class="btn alt" href="downloads/challenge2-practice/module2/service.py" download>service.py</a><a class="btn alt" href="downloads/challenge2-practice/module2/practice-lattice-service.service" download>Service systemd 파일</a></div><h4>③ 설치 상태 확인</h4><p>접속할 수 있다면 아래 명령으로 확인합니다. 접속하지 못해도 3단계에서 Target 상태가 Healthy가 되는지로 앱 상태를 판단할 수 있습니다.</p><pre>sudo systemctl status practice-lattice-service --no-pager<br>sudo journalctl -u practice-lattice-service -n 50 --no-pager<br>curl -i http://127.0.0.1:8080/health<br>curl -i "http://127.0.0.1:8080/v1/inventory?id=P-100"</pre><table><tr><th>요청</th><th>정상 결과</th></tr><tr><td><code>/health</code></td><td>HTTP 200, <code>status=ok</code>, <code>component=inventory</code></td></tr><tr><td><code>/v1/inventory?id=P-100</code></td><td><code>product_id=P-100</code>, <code>available=true</code>, <code>via=vpc-lattice</code></td></tr></table></div>
    <div class="phase"><h3>3단계 — 보안 그룹을 통신 방향에 맞게 구성</h3><table><tr><th>보안 그룹 이름</th><th>연결 대상</th><th>Inbound</th><th>목적</th></tr><tr><td><code>practice-lattice-client-sg</code></td><td>Client EC2</td><td>TCP/80 from <code>0.0.0.0/0</code><br>SSH/22 from 도쿄 EC2 Instance Connect 접두사 목록(접속 시)</td><td>외부 채점 요청과 Client 접속</td></tr><tr><td><code>practice-lattice-association-sg</code></td><td>Client VPC Association</td><td>TCP/80 from <code>10.82.0.0/16</code></td><td>Client VPC에서 서비스 네트워크 진입</td></tr><tr><td><code>practice-lattice-service-sg</code></td><td>Service EC2</td><td>TCP/8080 from <code>com.amazonaws.ap-northeast-1.vpc-lattice</code> 관리형 접두사 목록</td><td>Lattice에서 Inventory Target으로 전달</td></tr></table><ol class="todo"><li><strong>EC2 → 보안 그룹 → 보안 그룹 생성</strong>에서 각각 올바른 VPC를 선택합니다. Client SG와 Association SG는 Client VPC, Service SG는 Service VPC에 생성합니다.</li><li>Service SG의 소스는 <code>0.0.0.0/0</code>이나 Client CIDR가 아니라 소스 입력창에서 도쿄 리전의 <strong>VPC Lattice 관리형 접두사 목록</strong>을 검색해 선택합니다.</li><li>세 보안 그룹의 아웃바운드는 기본 허용 규칙을 유지합니다.</li></ol><p class="warn"><strong>자주 하는 실수:</strong> Association SG를 Service EC2에 연결하거나, Service SG를 VPC Association에 연결하면 통신되지 않습니다. 이름이 아니라 위 표의 <strong>연결 대상</strong>을 기준으로 확인하세요.</p></div>
    <div class="phase"><h3>4단계 — Lattice Target Group 만들기</h3><ol class="todo"><li><strong>VPC Lattice → 대상 그룹 → 대상 그룹 생성</strong>을 선택합니다.</li><li>대상 유형은 <strong>인스턴스</strong>, 이름은 <code>practice-lattice-inventory-tg</code>로 입력합니다.</li><li>프로토콜은 <strong>HTTP</strong>, 포트는 <code>8080</code>, VPC는 <code>practice-lattice-service-vpc</code>를 선택합니다.</li><li>상태 검사 프로토콜 HTTP, 상태 검사 경로 <code>/health</code>, 정상 성공 코드는 <code>200</code>으로 설정합니다.</li><li>대상 등록 화면에서 <code>practice-lattice-inventory</code>를 선택하고 포트 <code>8080</code>으로 등록합니다.</li><li>생성 후 대상 그룹의 <strong>대상</strong> 탭에서 상태를 확인합니다. 처음에는 Initial일 수 있으며 잠시 뒤 <strong>Healthy</strong>가 되어야 합니다.</li></ol><table><tr><th>Target 상태</th><th>확인할 곳</th></tr><tr><td>Initial이 오래 지속</td><td>EC2가 실행 중인지, 등록 포트가 8080인지 확인</td></tr><tr><td>Unhealthy</td><td>Service 앱 실행, <code>/health</code>, Service SG 관리형 접두사 목록 확인</td></tr><tr><td>Unused</td><td>대상 그룹이 Listener에 연결됐는지 확인</td></tr></table></div>
    <div class="phase"><h3>5단계 — Lattice Service와 Listener 만들기</h3><ol class="todo"><li><strong>VPC Lattice → 서비스 → 서비스 생성</strong>을 선택합니다.</li><li>이름은 <code>practice-lattice-inventory-service</code>, 사용자 지정 도메인은 입력하지 않고 인증 유형은 <strong>없음(None)</strong>으로 생성합니다.</li><li>서비스의 <strong>라우팅 → 리스너 추가</strong>를 선택합니다.</li><li>리스너 이름 <code>practice-lattice-http-listener</code>, 프로토콜 <strong>HTTP</strong>, 포트 <code>80</code>을 입력합니다.</li><li>기본 작업은 <strong>대상 그룹으로 전달</strong>, 대상은 <code>practice-lattice-inventory-tg</code>, 가중치는 100으로 지정합니다.</li><li>서비스 세부 정보에 자동 생성된 DNS 주소를 메모합니다. 예: <code>practice-lattice-inventory-service-....vpc-lattice-svcs.ap-northeast-1.on.aws</code>.</li></ol><p class="warn">DNS 주소를 복사할 때 <code>http://</code>를 두 번 붙이거나 뒤에 <code>/v1/inventory</code> 경로를 미리 붙이지 않습니다. Client 앱이 필요한 경로를 자동으로 추가합니다.</p></div>
    <div class="phase"><h3>6단계 — Service Network와 두 Association 만들기</h3><ol class="todo"><li><strong>VPC Lattice → 서비스 네트워크 → 서비스 네트워크 생성</strong>에서 이름 <code>practice-lattice-sn</code>, 인증 유형 <strong>없음(None)</strong>으로 생성합니다.</li><li>서비스 네트워크의 <strong>VPC 연결(연결된 VPC)</strong>에서 VPC 연결 추가를 선택하고 <code>practice-lattice-client-vpc</code>를 연결합니다.</li><li>VPC 연결 보안 그룹으로 <code>practice-lattice-association-sg</code>를 선택합니다. Service VPC는 연결하지 않습니다.</li><li><strong>서비스 연결</strong>에서 <code>practice-lattice-inventory-service</code>를 연결합니다.</li><li>VPC Association과 Service Association의 상태가 모두 <strong>Active</strong>가 될 때까지 기다립니다.</li></ol><p class="success"><strong>핵심:</strong> Client VPC Association은 Client가 서비스 네트워크로 들어오는 입구이고, Service Association은 Inventory Service를 그 네트워크에 공개하는 연결입니다. 둘 중 하나라도 없으면 요청이 전달되지 않습니다.</p></div>
    <div class="phase"><h3>7단계 — Client 앱 배포하고 종단 간 확인</h3><h4>① Client EC2에 접속해 앱 설치</h4><p>EC2 Instance Connect 또는 SSH로 <code>practice-lattice-client</code>에 접속합니다. 아래의 <code>SERVICE_DNS_NAME</code>을 5단계에서 복사한 실제 Lattice DNS 주소로 바꿉니다.</p><pre>curl -fLO https://rani0380.github.io/2026_nationalskills/cloud-course/downloads/challenge2-practice/module2/deploy-client.sh<br>chmod +x deploy-client.sh<br>./deploy-client.sh http://SERVICE_DNS_NAME</pre><p>예: <code>./deploy-client.sh http://practice-lattice-inventory-service-0123456789.vpc-lattice-svcs.ap-northeast-1.on.aws</code></p><div class="actions"><a class="btn alt" href="downloads/challenge2-practice/module2/deploy-client.sh" download>Client 배포 스크립트</a><a class="btn alt" href="downloads/challenge2-practice/module2/client.py" download>client.py</a><a class="btn alt" href="downloads/challenge2-practice/module2/practice-lattice-client.service" download>Client systemd 파일</a></div><h4>② Client 내부에서 구간별 확인</h4><pre>sudo systemctl status practice-lattice-client --no-pager<br>cat /etc/practice-lattice-client.env<br>curl -i http://127.0.0.1/health<br>curl -i "http://127.0.0.1/v1/client/inventory?id=P-100"</pre><table><tr><th>요청</th><th>정상 결과</th></tr><tr><td>Client <code>/health</code></td><td>HTTP 200, <code>component=client</code></td></tr><tr><td>Client Inventory API</td><td>HTTP 200, <code>product_id=P-100</code>, <code>available=true</code>, <code>via=vpc-lattice</code></td></tr></table><h4>③ 외부에서 최종 확인</h4><pre>CLIENT_IP=$(aws ec2 describe-instances --region ap-northeast-1 --filters Name=tag:Name,Values=practice-lattice-client Name=instance-state-name,Values=running --query 'Reservations[0].Instances[0].PublicIpAddress' --output text)<br>curl -i "http://\${CLIENT_IP}/health"<br>curl -i "http://\${CLIENT_IP}/v1/client/inventory?id=P-100"</pre><p class="success">외부 Client API가 P-100 응답을 반환하면 Internet → Client EC2 → VPC Association → Service Network → Listener → Target Group → Private Service EC2의 전체 경로가 정상입니다.</p></div>
    <div class="phase"><h3>8단계 — 오류를 구간별로 해결</h3><div class="flow">외부 Inventory API 실패<br>├─ 외부 /health도 실패 → Client Public IP·SG TCP/80·Client 서비스 확인<br>└─ 외부 /health 성공 → Client 이후 Lattice 구간 확인<br>   ├─ Target Unhealthy → Service 앱·8080·Service SG·/health<br>   ├─ DNS 해석 실패 → Client VPC Association·DNS 설정·서비스 DNS 오타<br>   ├─ HTTP 403 → Auth None과 Auth Policy 확인<br>   ├─ HTTP 503 → Listener Forward와 Healthy Target 확인<br>   └─ Read timeout → Association SG·Service Association·Target 경로 확인</div><table><tr><th>오류·증상</th><th>주요 원인</th><th>확인 방법</th></tr><tr><td><code>KeyError: INVENTORY_URL</code></td><td>환경변수 파일 누락</td><td><code>cat /etc/practice-lattice-client.env</code> 후 Client 재배포</td></tr><tr><td><code>Could not resolve host</code></td><td>DNS 주소 오타 또는 VPC Association/DNS 설정 문제</td><td>Lattice 서비스 DNS와 Client VPC DNS 설정 확인</td></tr><tr><td><code>Connection refused</code></td><td>Client 또는 Service 앱 미실행</td><td>각 EC2의 systemctl과 journalctl 확인</td></tr><tr><td>Target Unhealthy</td><td>포트·경로·Service SG 오류</td><td>HTTP/8080, <code>/health</code>, 관리형 접두사 목록 확인</td></tr><tr><td>HTTP 403</td><td>서비스 또는 서비스 네트워크 인증이 None이 아님</td><td>인증 유형과 Auth Policy 확인</td></tr><tr><td>HTTP 503</td><td>Listener에 정상 Target이 없음</td><td>Listener 기본 작업과 Target Healthy 상태 확인</td></tr><tr><td>내부 성공·외부 실패</td><td>Client SG TCP/80 또는 Public Route 문제</td><td>Public IP, IGW, Route Table, Client SG 확인</td></tr></table><h4>제출 전 체크리스트</h4><ul><li>모든 모듈 2 리소스가 도쿄 <code>ap-northeast-1</code>에 있습니다.</li><li>두 VPC CIDR, 두 EC2 이름과 Public IP 조건이 정확합니다.</li><li>금지된 VPC 직접 연결을 만들지 않았습니다.</li><li>Target Group은 Instance/HTTP/8080이고 상태가 Healthy입니다.</li><li>Listener는 HTTP/80이며 기본 작업이 Inventory Target Group 전달입니다.</li><li>Client VPC Association과 Inventory Service Association이 Active입니다.</li><li>Service SG는 VPC Lattice 관리형 접두사 목록만 TCP/8080으로 허용합니다.</li><li>외부 Client Inventory API가 P-100의 정상 JSON을 반환합니다.</li></ul></div>
  </div></details>
  <details class="guide" id="guide-m3" open><summary>모듈 3 · IAM 변경 이벤트 자동 복구 풀이 흐름</summary><div class="guide-body">
    <div class="flow">UpdateAssumeRolePolicy → CloudTrail Event → EventBridge Rule → Lambda<br>                                                                ├→ S3 정상 정책 읽기<br>                                                                ├→ IAM Trust Policy 복구<br>                                                                └→ SNS + CloudWatch Logs</div>
    <div class="phase"><h3>1단계 — 정상 정책을 먼저 백업</h3><ol class="todo"><li><code>practice-protected-role</code>을 생성하고 EC2 Service Principal만 신뢰하도록 설정합니다.</li><li>비번호를 붙인 S3 버킷을 생성하고 Versioning과 Public Access Block 네 항목을 활성화합니다.</li><li>정상 Trust Policy JSON을 <code>policies/practice-protected-role.json</code>에 업로드합니다.</li></ol><p class="warn">S3 백업 JSON은 <code>AssumeRolePolicyDocument</code> 내부 정책 자체여야 합니다. CLI 전체 출력 객체를 그대로 저장하면 Lambda 업데이트가 실패합니다.</p></div>
    <div class="phase"><h3>2단계 — Lambda 실행 Role 최소 권한</h3><ul><li><code>iam:GetRole</code>, <code>iam:UpdateAssumeRolePolicy</code>: 보호 Role ARN</li><li><code>s3:GetObject</code>: 백업 객체 ARN</li><li><code>sns:Publish</code>: Alert Topic ARN</li><li>CloudWatch Logs 기본 기록 권한</li></ul><p>Lambda 환경변수의 버킷 이름에는 <code>s3://</code>를 붙이지 않고, BACKUP_KEY는 정확한 Object Key만 사용합니다.</p></div>
    <div class="phase"><h3>3단계 — Lambda를 먼저 수동 시험</h3><ol class="todo"><li><code>remediate_iam.py</code>를 ZIP의 루트에 두고 압축해 배포합니다.</li><li>Runtime Python 3.12, Handler <code>remediate_iam.lambda_handler</code>, Timeout 30초 이상으로 지정합니다.</li><li>빈 테스트 이벤트로 Lambda를 직접 실행합니다.</li><li>Role Trust Policy가 정상이고 SNS Publish와 Lambda Log가 생성되는지 확인합니다.</li></ol><p class="success">EventBridge를 붙이기 전에 Lambda 단독 시험이 성공해야 문제 구간을 분리할 수 있습니다.</p></div>
    <div class="phase"><h3>4단계 — EventBridge Event Pattern</h3><pre>{<br>  "source": ["aws.iam"],<br>  "detail-type": ["AWS API Call via CloudTrail"],<br>  "detail": {<br>    "eventSource": ["iam.amazonaws.com"],<br>    "eventName": ["UpdateAssumeRolePolicy"],<br>    "requestParameters": {"roleName": ["practice-protected-role"]}<br>  }<br>}</pre><p>Rule Target으로 Lambda를 지정하고, Lambda Resource-based Policy에 EventBridge의 <code>lambda:InvokeFunction</code> 권한이 생성됐는지 확인합니다.</p></div>
    <div class="phase"><h3>5단계 — 실제 변조 시험</h3><ol class="todo"><li>현재 Trust Policy를 별도 파일로 보관합니다.</li><li>테스트 Principal을 추가해 <code>update-assume-role-policy</code>를 실행합니다.</li><li>EventBridge Metrics, Lambda Monitor, CloudWatch Logs 순서로 이벤트 전달을 확인합니다.</li><li>180초 이내 원래 EC2 Principal만 남는지 검사합니다.</li></ol><ul><li><strong>Rule이 실행되지 않음:</strong> CloudTrail API 이벤트 활성 여부와 Event Pattern의 대소문자를 확인합니다.</li><li><strong>Lambda AccessDenied:</strong> 오류 메시지의 Action과 Resource ARN을 실행 Role에 추가합니다.</li><li><strong>무한 반복 우려:</strong> Lambda의 복구 호출도 같은 API 이벤트를 만들 수 있습니다. 정상 정책과 현재 정책이 같으면 업데이트를 생략하도록 개선할 수 있습니다.</li></ul></div>
  </div></details>

  <details class="guide" id="guide-m4" open><summary>모듈 4 · SQS 기반 ECS Auto Scaling 풀이 흐름</summary><div class="guide-body">
    <div class="flow">SQS Visible Messages → CloudWatch Alarm → Application Auto Scaling → ECS Desired Count<br>                                                                     └→ Private Fargate Task → SQS receive/delete → Logs</div>
    <div class="phase"><h3>1단계 — Private Task의 외부 통신 결정</h3><p>두 Private Subnet을 서로 다른 AZ에 만듭니다. Task에 Public IP를 주지 않으므로 아래 중 하나를 선택합니다.</p><ul><li><strong>간단한 연습:</strong> 각 Private Route Table에서 NAT Gateway로 기본 경로를 구성합니다.</li><li><strong>Endpoint 방식:</strong> ECR API, ECR DKR, CloudWatch Logs, SQS, STS Interface Endpoint와 S3 Gateway Endpoint를 구성합니다.</li></ul><p class="warn">ECR Pull은 ECR Endpoint 두 개만으로 끝나지 않습니다. 이미지 Layer를 받기 위한 S3 접근도 필요합니다.</p></div>
    <div class="phase"><h3>2단계 — Queue와 DLQ</h3><ol class="todo"><li><code>practice-ecs-dlq</code>를 먼저 생성합니다.</li><li>Main Queue를 만들고 Visibility Timeout 60초, Receive Message Wait Time 10초 이상을 설정합니다.</li><li>Redrive Policy에 DLQ ARN과 <code>maxReceiveCount=3</code>을 설정합니다.</li></ol><p>Worker 처리시간 5초보다 Visibility Timeout이 충분히 길어야 동일 메시지의 중복 처리를 줄일 수 있습니다.</p></div>
    <div class="phase"><h3>3단계 — Image Build와 두 IAM Role</h3><pre>aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin &lt;ACCOUNT&gt;.dkr.ecr.ap-northeast-2.amazonaws.com<br>docker build -t practice-ecs-worker:latest module4<br>docker tag practice-ecs-worker:latest &lt;ACCOUNT&gt;.dkr.ecr.ap-northeast-2.amazonaws.com/practice-ecs-worker:latest<br>docker push &lt;ACCOUNT&gt;.dkr.ecr.ap-northeast-2.amazonaws.com/practice-ecs-worker:latest</pre><table><tr><th>Role</th><th>권한</th></tr><tr><td>Task Execution Role</td><td>ECR Image Pull, CloudWatch Logs</td></tr><tr><td>Task Role</td><td>대상 SQS의 ReceiveMessage, DeleteMessage, GetQueueAttributes</td></tr></table><p class="warn">SQS 권한을 Execution Role에만 넣으면 애플리케이션은 AccessDenied가 발생합니다.</p></div>
    <div class="phase"><h3>4단계 — Task Definition과 Service</h3><ol class="todo"><li>Fargate, awsvpc, Linux/x86_64 Task Definition을 등록합니다.</li><li>컨테이너 환경변수 세 개와 awslogs 설정을 구성합니다.</li><li>Private Subnet A/C, Public IP Disabled로 Service를 생성합니다.</li><li>초기 Desired Count를 0으로 지정합니다. 수동 시험 시 잠시 1로 올려 로그와 SQS 접근을 확인한 뒤 다시 0으로 내립니다.</li></ol></div>
    <div class="phase"><h3>5단계 — Scaling과 12개 메시지 시험</h3><p>ECS Service를 Scalable Target으로 등록해 Min 0, Max 6으로 설정합니다. Scale-out Alarm과 Step Scaling Policy, Scale-in Alarm과 Policy를 각각 연결합니다.</p><pre>QUEUE_URL=$(aws sqs get-queue-url --region ap-northeast-2 --queue-name practice-ecs-queue --query QueueUrl --output text)<br>for i in $(seq 1 12); do aws sqs send-message --region ap-northeast-2 --queue-url "$QUEUE_URL" --message-body "practice-$i"; done<br>aws ecs describe-services --region ap-northeast-2 --cluster practice-ecs-cluster --services practice-ecs-worker-service --query 'services[0].[desiredCount,runningCount]'</pre><ul><li><strong>Desired만 증가하고 Running=0:</strong> ECS Service Events에서 ECR Pull, ENI, IAM 오류를 확인합니다.</li><li><strong>Task는 실행되지만 Queue가 줄지 않음:</strong> Task Role과 Queue URL, Logs를 확인합니다.</li><li><strong>0에서 Scale-out 안 됨:</strong> Alarm Metric의 QueueName Dimension과 Scaling Policy 연결을 확인합니다.</li><li><strong>Scale-in 안 됨:</strong> Alarm datapoints, cooldown, Min Capacity를 확인합니다.</li></ul></div>
  </div></details>`;
  modulesSection.after(guide);
})();
