# AWS 전국과제 1과제·2과제 상세 풀이집

이 문서는 `07/2026_전국기능경기대회_과제`와 선별 과제 폴더의 문제지, 제공파일, 채점 스크립트를 기준으로 정리한 실습형 풀이집입니다. 화면에서 빠르게 확인할 수 있도록 핵심 흐름을 요약했고, 전체 Word 문서는 아래에서 내려받을 수 있습니다.

[상세 풀이집 DOCX 다운로드](assets/AWS_전국과제_1과제_2과제_상세풀이집.docx)

## 공통 준비

모든 과제는 리전, 리소스명, 태그 대소문자가 채점에 직접 영향을 줍니다.

| 점검 항목 | 기준 |
|---|---|
| 인증 | `aws sts get-caller-identity`로 계정 확인 |
| 리전 | 문제지의 모듈별 리전을 먼저 고정 |
| 태그 | `Name` 태그와 문제지 리소스명을 정확히 일치 |
| 보안 | `Principal: "*"` 또는 `Action: "*"` 남용 금지 |
| 검증 | 채점 스크립트가 조회하는 이름과 경로를 우선 확인 |

```bash
aws sts get-caller-identity
aws configure set region ap-northeast-2
ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
```

## 1과제: Web Service Provisioning

1과제는 VPC, S3, CloudFront, ECR, DynamoDB, EKS, Lambda, ALB, Monitoring을 한 번에 묶는 종합 구축 문제입니다.

| 영역 | 필수 리소스명/값 | 풀이 핵심 |
|---|---|---|
| VPC | `wskorea26-vpc`, `172.16.0.0/16` | Public 2개, Private 2개, NAT 2개 |
| S3 | `wskorea26-concert-bucket-<비번호>` | `web/main/` 경로 업로드, KMS, Public 차단 |
| ECR | `wskorea26-book-repo:stable` | scanOnPush, KMS 암호화 |
| DynamoDB | `wskorea26-data-table` | PK `client_id`, 삭제 방지, KMS |
| EKS | `wskorea26-cluster`, version `1.35` | Private subnet, Secret KMS, 로그 활성화 |
| NodeGroup | `wskorea26-addon-ng`, `wskorea26-app-ng` | `node-type=addon/app` 라벨로 워크로드 분리 |
| Lambda | `wskorea26-book-lambda` | ALB 이벤트 포맷, `concert_name` 검증 |
| ALB | `wskorea26-book-alb` | CloudFront 헤더 없으면 403 |
| CloudFront | `wskorea26-concert-cf` | `/`는 S3, `/book`은 ALB |
| Monitoring | `wskorea26-monitoring` | CPU, Memory, Pod 수, Restart, Network 패널 |

### 네트워크

1. VPC `172.16.0.0/16`을 만들고 `Name=wskorea26-vpc`를 붙입니다.
2. Public subnet은 `172.16.1.0/24`, `172.16.2.0/24`로 구성합니다.
3. Private subnet은 `172.16.201.0/24`, `172.16.202.0/24`로 구성합니다.
4. Public route table은 IGW, Private route table은 각 AZ의 NAT Gateway로 향하게 합니다.

```bash
aws ec2 create-vpc \
  --cidr-block 172.16.0.0/16 \
  --tag-specifications 'ResourceType=vpc,Tags=[{Key=Name,Value=wskorea26-vpc}]'
```

### S3와 CloudFront 정적 페이지

1. KMS alias `alias/wskorea26-s3-key`를 생성합니다.
2. 버킷은 `wskorea26-concert-bucket-<비번호>` 패턴으로 생성합니다.
3. Public Access Block 4개 항목을 모두 켭니다.
4. `index.html`, `main.jpeg`를 `web/main/` 아래에 업로드하며 SSE-KMS를 지정합니다.
5. CloudFront OAC 또는 버킷 정책으로 CloudFront에서만 읽히게 합니다.

```bash
aws s3api put-public-access-block \
  --bucket wskorea26-concert-bucket-<NO> \
  --public-access-block-configuration BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true

aws s3 cp index.html s3://wskorea26-concert-bucket-<NO>/web/main/index.html \
  --sse aws:kms --sse-kms-key-id alias/wskorea26-s3-key
```

### ECR과 애플리케이션 이미지

제공된 `book` 애플리케이션은 8080 포트에서 동작하며 `AWS_REGION`, `TABLE_NAME` 환경변수가 필요합니다.

```dockerfile
FROM public.ecr.aws/amazonlinux/amazonlinux:2023
WORKDIR /app
COPY book /app/book
RUN chmod +x /app/book
EXPOSE 8080
CMD ["/app/book"]
```

```bash
aws ecr create-repository \
  --repository-name wskorea26-book-repo \
  --image-scanning-configuration scanOnPush=true

docker build -t wskorea26-book-repo:stable .
docker tag wskorea26-book-repo:stable \
  $ACCOUNT_ID.dkr.ecr.ap-northeast-2.amazonaws.com/wskorea26-book-repo:stable
docker push $ACCOUNT_ID.dkr.ecr.ap-northeast-2.amazonaws.com/wskorea26-book-repo:stable
```

### DynamoDB와 Lambda

`wskorea26-data-table`은 `client_id` 문자열 파티션 키를 사용합니다. 삭제 방지와 KMS 암호화를 반드시 활성화합니다.

```bash
aws dynamodb create-table \
  --table-name wskorea26-data-table \
  --attribute-definitions AttributeName=client_id,AttributeType=S \
  --key-schema AttributeName=client_id,KeyType=HASH \
  --billing-mode PAY_PER_REQUEST \
  --sse-specification Enabled=true,SSEType=KMS,KMSMasterKeyId=alias/wskorea26-dynamodb-key \
  --deletion-protection-enabled
```

Lambda는 `concert_name` query parameter가 없으면 400을 반환하고, 조회 결과가 없으면 빈 배열 `[]`과 200을 반환해야 합니다.

### EKS와 워크로드 분리

1. EKS 클러스터는 private subnet c/d에 생성합니다.
2. Control plane log는 `api`, `audit`, `authenticator`, `controllerManager`, `scheduler`를 활성화합니다.
3. Secret 암호화용 KMS key를 지정합니다.
4. Addon nodegroup에는 `node-type=addon`, Application nodegroup에는 `node-type=app` 라벨을 부여합니다.
5. 모니터링 및 시스템 컴포넌트는 addon 노드, book 앱은 app 노드에만 배치합니다.

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: book
  namespace: wskorea26
spec:
  replicas: 2
  selector:
    matchLabels:
      app: book
  template:
    metadata:
      labels:
        app: book
    spec:
      nodeSelector:
        node-type: app
      containers:
        - name: book
          image: <ACCOUNT_ID>.dkr.ecr.ap-northeast-2.amazonaws.com/wskorea26-book-repo:stable
          ports:
            - containerPort: 8080
          env:
            - name: AWS_REGION
              value: ap-northeast-2
            - name: TABLE_NAME
              value: wskorea26-data-table
```

### ALB와 CloudFront 우회 차단

ALB는 internet-facing HTTP 80으로 만들되, CloudFront가 보내는 custom header가 없으면 접근을 거절해야 합니다.

| 요청 | 기대 결과 |
|---|---|
| `https://<CF_DOMAIN>/` | S3 `index.html` 표시 |
| `https://<CF_DOMAIN>/main.jpeg` | 이미지 200 |
| `https://<CF_DOMAIN>/book` POST | 예약 저장 |
| `https://<CF_DOMAIN>/book?concert_name=...` GET | 예약 조회 |
| `http://<ALB_DNS>/book` | 403 Forbidden |

CloudFront origin custom header는 다음처럼 맞춥니다.

| Origin | Header |
|---|---|
| ALB | `X-Origin-Verify: wskorea26-cf` |
| S3 | `wskorea26-s3-access` |

## 2과제 Module 1: NoSQL

Region은 `ap-southeast-1`입니다. DynamoDB 조건부 예약, GSI 희소 인덱스, Streams 감사 적재, EC2 Flask 앱 배포가 핵심입니다.

| 항목 | 값 |
|---|---|
| Reservation Table | `bigbae-nosql-reservation-table` |
| Key | PK `train_id`, SK `seat_id` |
| GSI | `gsi-user-reservations`, PK `user_id`, SK `reserved_at` |
| Stream | `NEW_AND_OLD_IMAGES` |
| Audit Table | `bigbae-nosql-audit-table`, PK `event_id` |
| Lambda | `bigbae-nosql-reservation-audit`, Python 3.13 |
| EC2 | `Name=bigbae-nosql-app-ec2`, TCP 8080 |

```bash
aws dynamodb create-table \
  --table-name bigbae-nosql-reservation-table \
  --attribute-definitions \
    AttributeName=train_id,AttributeType=S \
    AttributeName=seat_id,AttributeType=S \
    AttributeName=user_id,AttributeType=S \
    AttributeName=reserved_at,AttributeType=S \
  --key-schema AttributeName=train_id,KeyType=HASH AttributeName=seat_id,KeyType=RANGE \
  --global-secondary-indexes '[{"IndexName":"gsi-user-reservations","KeySchema":[{"AttributeName":"user_id","KeyType":"HASH"},{"AttributeName":"reserved_at","KeyType":"RANGE"}],"Projection":{"ProjectionType":"ALL"}}]' \
  --billing-mode PAY_PER_REQUEST \
  --stream-specification StreamEnabled=true,StreamViewType=NEW_AND_OLD_IMAGES
```

제공된 `app.py`는 `status`가 없거나 `available`일 때만 좌석을 `reserved`로 변경합니다. 이미 예약된 좌석은 `409 already reserved`가 되어야 합니다.

```bash
curl http://<EC2_PUBLIC_IP>:8080/healthcheck -i
curl -X POST http://<EC2_PUBLIC_IP>:8080/reserve \
  -H 'Content-Type: application/json' \
  -d '{"train_id":"T1","seat_id":"A1","user_id":"U1"}'
curl -X POST http://<EC2_PUBLIC_IP>:8080/reserve \
  -H 'Content-Type: application/json' \
  -d '{"train_id":"T1","seat_id":"A1","user_id":"U2"}'
```

## 2과제 Module 2: CDN Function

Region은 `us-east-1`입니다. CloudFront Functions와 KeyValueStore로 A/B 테스트를 구성합니다.

| 항목 | 값 |
|---|---|
| S3 Bucket | `skillsphone-landing-ab-<ACCOUNT_ID>` |
| Object A | `/version-a/index.html` |
| Object B | `/version-b/index.html` |
| KeyValueStore | `skillsphone-cdn-ab-config` |
| Request Function | `skillsphone-cdn-ab-req-fn` |
| Response Function | `skillsphone-cdn-ab-res-fn` |
| Cache Policy | `skillsphone-cdn-ab-cache-policy` |
| Distribution | `skillsphone-cdn-ab-distribution` |

KeyValueStore에는 다음 값을 정확히 넣습니다.

| Key | Value |
|---|---|
| `weight` | `0.3` |
| `version_a` | `/version-a/index.html` |
| `version_b` | `/version-b/index.html` |

viewer-request 함수는 `x-sp-ab` 쿠키가 있으면 같은 버전을 유지하고, 없으면 `weight` 기준으로 a/b를 배정한 뒤 URI를 재작성합니다. viewer-response 함수는 새 배정이 있을 때만 `Set-Cookie`를 추가합니다.

## 2과제 Module 3: EKS Scaling

Region은 `ap-northeast-2`입니다. SQS 메시지 수로 KEDA가 Pod를 늘리고, Pending Pod가 생기면 Karpenter가 앱 노드를 추가해야 합니다.

| 항목 | 값 |
|---|---|
| SQS | `skm-order-queue` |
| Cluster | `skm-eks-cluster`, version `1.35` |
| Addon NodeGroup | `skm-cluster-addon-ng`, `t3.medium`, desired 1 |
| Namespace | `skillsmkt` |
| Deployment | `order-processor` |
| KEDA | `order-scaler`, min 1, max 5 |
| Karpenter | `skm-app-nodepool`, `skm-app-nodeclass` |
| Instance Types | `t3.small`, `t3.medium` |

```yaml
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: order-scaler
  namespace: skillsmkt
spec:
  scaleTargetRef:
    name: order-processor
  minReplicaCount: 1
  maxReplicaCount: 5
  triggers:
    - type: aws-sqs-queue
      metadata:
        queueURL: <SQS_QUEUE_URL>
        queueLength: "5"
        awsRegion: ap-northeast-2
```

채점 직전에는 부하 주입을 중지하고 SQS queue를 비워 Pod 1개, 앱 노드 1대 상태로 안정화합니다.

## 2과제 Module 4: EKS O11y

Region은 `ap-northeast-1`입니다. 로그 생성 앱의 stdout JSON 로그를 OpenTelemetry Collector가 수집하여 Loki에 저장하고 Grafana에서 조회하는 구성이 목표입니다.

| 항목 | 값 |
|---|---|
| Cluster | `o11y-cluster`, version `1.35` |
| NodeGroup | `t3.medium`, min/desired/max 2 |
| App | `/healthz`, `/log?level=info&count=10`, port 8080 |
| Collector | OpenTelemetry Collector |
| Storage | Loki |
| Visualization | Grafana |

```bash
helm repo add grafana https://grafana.github.io/helm-charts
helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts
helm install loki grafana/loki -n logging --create-namespace
helm install grafana grafana/grafana -n logging
helm install otel open-telemetry/opentelemetry-collector -n logging --set mode=daemonset
```

## 제출 전 최종 점검표

| 점검 | 기준 |
|---|---|
| AWS 계정/리전 | `aws sts get-caller-identity`, `aws configure get region` |
| 1과제 CloudFront | HTTPS 루트 200, `/main.jpeg` 200, `/book` POST/GET 정상 |
| 1과제 ALB 우회 차단 | ALB DNS 직접 접근 시 403 |
| 1과제 EKS 분리 | 시스템/모니터링은 addon, 앱은 app 노드 |
| 1과제 DynamoDB | 삭제 방지 true, KMS alias 일치 |
| 2-1 NoSQL | 중복 예약 409, audit table 30초 내 기록 |
| 2-2 CDN | 첫 방문 쿠키 설정, 재방문 동일 버전 |
| 2-3 Scaling | 메시지 투입 후 2분 내 Pod scale-out |
| 2-4 O11y | 로그 생성 후 Loki/Grafana 조회 |

## 감점 방지 메모

- 문제지의 이름은 대소문자를 포함해 그대로 사용합니다.
- CloudFront Comment, Distribution Name처럼 스크립트가 조회하는 필드를 비워두지 않습니다.
- S3 정적 웹사이트 호스팅을 Public으로 여는 방식은 요구사항과 맞지 않습니다.
- KMS alias는 `alias/` 접두어까지 포함해 생성하고 실제 리소스 암호화에 사용되었는지 확인합니다.
- Kubernetes 노드 분리는 라벨만 붙이지 말고 `nodeSelector`, taint/toleration까지 함께 사용합니다.
