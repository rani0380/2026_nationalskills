# 2026 전국대회 클라우드컴퓨팅 1과제 이론 설명서

## 1. 아키텍처 핵심 개념

이 과제의 핵심은 단순히 AWS 리소스를 많이 만드는 것이 아니라, 정적 웹, 컨테이너 애플리케이션, 서버리스 조회 API, 데이터베이스, 모니터링을 하나의 운영 가능한 구조로 연결하는 것이다.

전체 구조는 다음 계층으로 나눌 수 있다.

| 계층 | AWS 서비스 | 역할 |
|---|---|---|
| 진입점 | CloudFront, WAF | 사용자 요청 수신, 캐싱, 보안 필터링 |
| 정적 웹 | S3 | `index.html`, `main.jpeg` 제공 |
| 동적 쓰기 API | ALB, EKS, Pod | `POST /v1/book` 처리 |
| 동적 조회 API | Lambda Function URL | `GET /v1/book` 처리 |
| 데이터 | DynamoDB | 예약 정보 저장 |
| 이미지 저장소 | ECR | 컨테이너 이미지 보관 |
| 보안 | KMS, IAM, Security Group | 암호화와 최소 권한 |
| 관측성 | Prometheus, Alertmanager, Grafana, Fluent Bit, CloudWatch Logs | 메트릭, 알림, 로그 수집 |

## 2. VPC와 Subnet 이론

VPC는 AWS 안에서 만드는 논리적 사설 네트워크이다. 온프레미스의 사내망처럼 IP 대역, 라우팅, 보안 경계를 직접 설계할 수 있다.

이 문제에서는 `192.168.0.0/16` VPC 안에 Public Subnet과 Private Subnet을 나눈다.

Public Subnet은 인터넷에서 들어오는 트래픽을 받아야 하는 리소스가 위치한다. 대표적으로 Internet-facing ALB와 NAT Gateway가 있다. Public Subnet이 되려면 Route Table에 `0.0.0.0/0 -> Internet Gateway` 경로가 있어야 한다.

Private Subnet은 외부에서 직접 접근되면 안 되는 리소스가 위치한다. EKS Worker Node는 Private Subnet에 둔다. Private Subnet의 리소스가 패키지 다운로드, ECR 이미지 Pull, AWS API 호출 등을 하기 위해서는 NAT Gateway 또는 VPC Endpoint가 필요하다.

고가용성을 위해 최소 2개 AZ를 사용한다. 한 AZ 장애가 발생해도 다른 AZ에서 서비스가 계속되도록 하는 것이 핵심이다.

## 3. Internet Gateway와 NAT Gateway

Internet Gateway는 VPC와 인터넷을 연결하는 출입구이다. Public Subnet의 기본 라우트가 IGW를 바라보면, Public IP가 있는 리소스는 인터넷과 통신할 수 있다.

NAT Gateway는 Private Subnet의 리소스가 인터넷으로 나갈 수 있게 해주지만, 인터넷에서 Private 리소스로 직접 들어오지는 못하게 한다. 따라서 EKS Node처럼 외부 노출이 불필요한 컴퓨팅 자원에 적합하다.

이 문제에서 Private Subnet은 NAT Gateway를 통해 외부와 통신해야 한다. AZ별 NAT Gateway를 두면 한 AZ 장애가 다른 AZ의 외부 통신까지 끊는 상황을 줄일 수 있다.

## 4. Security Group 이론

Security Group은 AWS 리소스 단위의 가상 방화벽이다. 인바운드와 아웃바운드 규칙으로 트래픽을 제어한다.

이 과제의 원칙은 다음과 같다.

- ALB는 CloudFront에서 들어오는 HTTP/HTTPS 요청만 받는다.
- EKS Node/Pod는 인터넷 전체에서 직접 접근되면 안 된다.
- EKS 애플리케이션으로 들어오는 트래픽은 ALB에서만 와야 한다.
- 문제 유의사항에 따라 80/443 outbound는 any open 가능하다.

보안그룹에서 `0.0.0.0/0`을 무분별하게 열면 감점 포인트가 된다. 특히 EKS Cluster, NodeGroup, Pod 쪽 인바운드에 Any IP를 허용하지 않아야 한다.

## 5. S3와 CloudFront 이론

S3는 객체 스토리지이다. 정적 웹 파일, 이미지, 배포 파일처럼 파일 단위 데이터를 저장하는 데 적합하다.

CloudFront는 CDN이다. 사용자는 CloudFront 엣지 로케이션에 접속하고, CloudFront가 Origin인 S3, ALB, Lambda로 요청을 전달한다.

이 문제에서 CloudFront를 단일 진입점으로 쓰는 이유는 다음과 같다.

- 정적 파일은 캐싱하여 빠르게 제공할 수 있다.
- API는 캐싱하지 않고 항상 Origin으로 보낼 수 있다.
- WAF를 붙여 SQL Injection, XSS, 과도한 요청을 방어할 수 있다.
- 사용자는 하나의 도메인으로 웹과 API를 모두 사용할 수 있다.

S3는 직접 퍼블릭으로 열지 않고 CloudFront OAC를 통해서만 접근시키는 것이 좋다. 이렇게 하면 사용자가 S3 버킷 URL을 직접 알아도 객체에 접근할 수 없다.

## 6. CloudFront Behavior 이론

CloudFront는 Path Pattern별로 다른 Origin과 Cache Policy를 적용할 수 있다.

이 문제에서는 다음 분리가 중요하다.

| 경로 | 성격 | 캐싱 |
|---|---|---|
| `/`, `/static/*` | 정적 웹 | 캐싱 활성화 |
| `/booking` | 예약 생성 API | 캐싱 비활성화 |
| `/v1/book` | 예약 조회 API | 캐싱 비활성화 |

POST 요청은 사용자의 데이터를 저장하는 요청이다. 캐싱하면 같은 응답이 재사용되거나 요청이 의도대로 Origin에 전달되지 않을 수 있으므로 캐싱을 비활성화해야 한다.

GET 조회 API도 예약 데이터가 바뀔 수 있으므로 문제 요구사항에서는 캐싱을 비활성화하는 것이 맞다.

## 7. WAF 이론

AWS WAF는 웹 요청을 검사해 악성 패턴을 차단하는 방화벽이다. CloudFront 앞단에 붙이면 전 세계 엣지에서 공격 요청을 걸러낼 수 있다.

이 과제의 WAF 요구사항은 세 가지이다.

1. SQL Injection 방어
2. XSS 방어
3. Rate Limiting

SQL Injection은 사용자의 입력값을 SQL 명령처럼 해석하게 만드는 공격이다. DynamoDB를 쓰더라도 API 계층의 일반적인 웹 공격 방어를 요구하는 의미로 이해하면 된다.

XSS는 악성 스크립트를 입력해 다른 사용자 브라우저에서 실행되도록 만드는 공격이다.

Rate Limiting은 특정 IP가 짧은 시간에 너무 많은 요청을 보내면 차단하는 방식이다. 문제에서는 1분간 200개 이상의 요청을 차단해야 한다.

## 8. ECR 이론

ECR은 AWS의 컨테이너 이미지 저장소이다. Docker 이미지를 저장하고, EKS나 ECS 같은 실행 환경이 이미지를 Pull해서 컨테이너를 실행한다.

이 문제에서 ECR에 요구되는 보안 요소는 다음과 같다.

- Private Repository
- KMS CMK 암호화
- Push 시 취약점 스캔
- 태그 정책

태그 불변성은 운영에서 중요하다. `v1.0.0` 같은 릴리스 태그가 나중에 다른 이미지로 덮어씌워지면 재현성과 추적성이 깨진다. 반면 개발용 `latest` 태그는 덮어쓰기를 허용하는 경우가 많다. 문제는 이런 운영 관점을 반영해 `v1.*` 계열 태그를 보호하라고 요구한다.

## 9. Container와 Dockerfile 이론

컨테이너는 애플리케이션 실행에 필요한 파일, 런타임, 설정을 이미지로 패키징해 어디서든 같은 방식으로 실행하게 해준다.

이 문제의 `book-linux-amd64_v1.0.1`은 이미 빌드된 Golang 실행 파일이다. 따라서 Go 소스코드를 빌드하는 Dockerfile이 아니라 실행 파일을 이미지에 복사하는 Dockerfile을 작성한다.

중요 포인트는 다음과 같다.

- 지급 파일을 반드시 사용한다.
- 실행 권한을 부여한다.
- 컨테이너 포트 8080을 노출한다.
- amd64 바이너리이므로 linux/amd64 이미지로 빌드한다.

## 10. EKS 이론

EKS는 AWS가 관리하는 Kubernetes 서비스이다. Kubernetes Control Plane은 AWS가 관리하고, 사용자는 Worker Node와 애플리케이션 리소스를 운영한다.

이 과제에서 EKS를 쓰는 이유는 다음과 같다.

- 컨테이너 애플리케이션을 고가용성으로 운영
- Deployment를 통한 선언적 배포
- Service와 Ingress를 통한 트래픽 연결
- Pod Identity를 통한 IAM 권한 분리
- Prometheus, Fluent Bit 같은 운영 도구 배포

Fully Private Cluster는 외부 인터넷에서 Kubernetes API Endpoint로 접근할 수 없게 하는 구성이다. 운영 보안성이 높지만, 관리 작업은 VPC 내부 환경이나 CloudShell/VPN/Bastion 등 접근 경로를 신중하게 준비해야 한다.

## 11. EKS Control Plane Log 이론

EKS Control Plane Log는 Kubernetes API 서버, 감사 로그, 인증 로그 등 클러스터 제어 영역의 이벤트를 CloudWatch Logs에 저장한다.

문제에서 모든 Control Plane 로그 저장을 요구하는 이유는 다음과 같다.

- 누가 어떤 Kubernetes API를 호출했는지 추적
- 인증/인가 실패 분석
- 클러스터 장애 원인 분석
- 보안 감사 대응

일반적으로 활성화 대상은 다음이다.

- API server
- Audit
- Authenticator
- Controller manager
- Scheduler

## 12. Kubernetes Cluster Domain 이론

Kubernetes 내부 DNS 기본 도메인은 보통 `cluster.local`이다. Service 이름은 다음과 같은 FQDN으로 해석된다.

```text
service.namespace.svc.cluster.local
```

문제는 이를 `wsc2026.skills.local`로 변경하라고 요구한다. 그러면 내부 서비스 FQDN은 다음과 같은 형태가 된다.

```text
service.namespace.svc.wsc2026.skills.local
```

이 설정은 클러스터 생성 시 kubelet 또는 CoreDNS 설정과 관련되며, 이미 생성한 뒤 바꾸기 어렵다. 따라서 EKS 생성 단계에서 반영해야 한다.

## 13. NodeGroup 분리 이론

NodeGroup을 Addon과 Application으로 분리하는 이유는 운영 안정성 때문이다.

Addon NodeGroup에는 클러스터 운영에 필요한 시스템 컴포넌트가 올라간다.

- CoreDNS
- AWS Load Balancer Controller
- Prometheus
- Alertmanager
- Grafana
- 기타 운영 도구

Application NodeGroup에는 실제 서비스 Pod만 올린다.

이렇게 분리하면 애플리케이션 부하가 시스템 컴포넌트를 밀어내는 일을 줄일 수 있고, 시스템 업데이트와 애플리케이션 배포를 독립적으로 관리할 수 있다.

Kubernetes에서는 `nodeSelector`, `nodeAffinity`, `taints/tolerations`를 사용해 Pod 배치 위치를 제어한다.

## 14. Deployment 이론

Deployment는 Kubernetes에서 Pod를 선언적으로 관리하는 리소스이다. 사용자는 원하는 replica 수와 컨테이너 이미지를 선언하고, Kubernetes가 실제 상태를 맞춘다.

문제의 핵심 요구사항은 다음이다.

- 최소 2개 Pod
- AZ 간 균등 분산
- Probe 3종 설정
- ConfigMap으로 환경변수 관리
- Pod Identity로 DynamoDB 권한 부여
- CPU/Memory request와 limit 설정

replicas 2는 한 Pod 장애에도 서비스가 완전히 중단되지 않게 한다. AZ 분산은 특정 가용영역 장애에 대비하기 위한 구성이다.

## 15. Probe 이론

Kubernetes Probe는 컨테이너 상태를 확인하는 메커니즘이다.

| Probe | 역할 |
|---|---|
| startupProbe | 애플리케이션 시작 완료 여부 확인 |
| readinessProbe | 트래픽을 받을 준비가 됐는지 확인 |
| livenessProbe | 프로세스가 살아 있고 정상인지 확인 |

이 문제에서는 세 Probe 모두 `/health:8080`으로 설정한다.

readinessProbe가 실패하면 Service Endpoints에서 제외되어 트래픽을 받지 않는다. livenessProbe가 실패하면 컨테이너가 재시작된다. startupProbe는 초기 구동이 느린 앱이 livenessProbe에 의해 너무 일찍 죽는 것을 방지한다.

## 16. ConfigMap 이론

ConfigMap은 Kubernetes에서 비밀이 아닌 설정값을 관리하는 리소스이다.

문제에서 `AWS_REGION`, `TABLE_NAME`을 Dockerfile이나 Deployment에 직접 적지 말고 ConfigMap으로 관리하라고 한 이유는 설정과 실행 이미지를 분리하기 위해서이다.

이미지는 동일하게 유지하고, 환경별로 ConfigMap만 바꾸면 다른 테이블이나 리전으로 배포할 수 있다.

단, 비밀번호나 API Key 같은 민감 정보는 ConfigMap이 아니라 Secret을 사용해야 한다.

## 17. Pod Identity와 IAM 이론

Pod Identity는 Kubernetes ServiceAccount와 AWS IAM Role을 연결해 특정 Pod에만 AWS 권한을 부여하는 방식이다.

기존에는 Node IAM Role에 많은 권한을 붙이는 실수가 많았다. 하지만 Node Role에 `dynamodb:*`를 붙이면 해당 노드의 모든 Pod가 DynamoDB 권한을 간접적으로 가질 수 있어 위험하다.

이 문제의 의도는 다음과 같다.

- EKS Pod는 DynamoDB에 데이터를 삽입하는 권한만 가진다.
- Lambda는 DynamoDB 데이터를 조회하는 권한만 가진다.
- 각각 필요한 테이블 수준 권한만 부여한다.

이것이 최소 권한 원칙이다.

## 18. Service와 Topology Aware Routing 이론

Kubernetes Service는 Pod 집합에 안정적인 접근 지점을 제공한다. Pod IP는 재시작할 때 바뀔 수 있지만 Service 이름과 ClusterIP는 안정적이다.

문제에서 "동일 가용 영역 내 Pod로 우선 라우팅"을 요구한 이유는 Cross-AZ 트래픽 비용을 줄이기 위해서이다.

AWS에서는 AZ를 넘는 데이터 전송에 비용이 발생할 수 있다. ALB가 A AZ에서 요청을 받았는데 B AZ의 Pod로 계속 보내면 불필요한 Cross-AZ 비용이 생긴다. Kubernetes의 topology aware routing 또는 Service annotation을 통해 같은 AZ의 endpoint를 우선 사용하도록 구성할 수 있다.

## 19. PodDisruptionBudget 이론

PDB는 자발적 중단 상황에서 최소 가용 Pod 수를 보장하는 Kubernetes 리소스이다.

자발적 중단의 예시는 다음과 같다.

- 노드 드레인
- 클러스터 업그레이드
- 노드 그룹 롤링 업데이트

문제는 최소 1개 Pod가 가용 상태를 유지하라고 요구한다. 따라서 `minAvailable: 1`인 PDB를 둔다.

## 20. ALB와 AWS Load Balancer Controller 이론

AWS Load Balancer Controller는 Kubernetes Ingress 또는 Service를 AWS ALB/NLB로 변환해주는 컨트롤러이다.

사용자가 Ingress를 만들면 컨트롤러가 AWS API를 호출해 ALB, Target Group, Listener Rule을 생성한다.

이 문제에서 ALB는 다음 역할을 한다.

- EKS 내부 Pod 앞단의 HTTP 진입점
- CloudFront의 ALB Origin
- `/booking` 요청을 애플리케이션 Service로 전달
- 잘못된 경로는 403 반환

ALB는 internet-facing이지만, 문제는 CloudFront를 통해서만 접근하도록 요구한다. 따라서 CloudFront prefix list 또는 Custom Header 검사를 통해 직접 접근을 제한해야 한다.

## 21. DynamoDB 이론

DynamoDB는 서버리스 NoSQL Key-Value/Document 데이터베이스이다. 테이블의 기본 키를 기준으로 빠른 읽기/쓰기를 제공한다.

이 문제의 기본 키는 `client_id`이다. 이 값은 변경하면 안 된다.

하지만 GET API는 `booking_id`로 조회해야 한다. 기본 키가 아닌 속성으로 효율적으로 조회하려면 GSI가 필요하다.

GSI는 Global Secondary Index이다. 원본 테이블과 다른 키 구조로 조회할 수 있게 해준다.

PITR은 Point-in-Time Recovery이다. 실수로 데이터를 지웠거나 잘못 덮어쓴 경우 특정 시점으로 복구할 수 있다.

Deletion Protection은 테이블 삭제 실수를 막는다.

PAY_PER_REQUEST는 요청량에 따라 과금되는 온디맨드 모드이다. 시험이나 예측 어려운 워크로드에 적합하다.

## 22. Lambda 이론

Lambda는 서버를 직접 관리하지 않고 코드를 실행하는 서버리스 컴퓨팅 서비스이다.

이 과제에서 Lambda를 쓰는 이유는 지급된 `book` 애플리케이션에는 GET 메서드가 없기 때문이다. POST는 EKS 앱이 처리하고, GET 조회는 Lambda가 DynamoDB를 읽어 처리한다.

Lambda Function URL은 API Gateway 없이 Lambda에 HTTPS 엔드포인트를 제공한다. CloudFront의 Origin으로 연결할 수 있다.

환경변수 `TABLE_NAME`은 Lambda가 조회할 DynamoDB 테이블명을 알기 위해 필요하다. 문제는 환경변수도 전송 중과 저장 중 CMK로 암호화하라고 요구한다.

## 23. KMS 이론

KMS는 암호화 키를 생성하고 관리하는 서비스이다. CMK는 Customer Managed Key로, 사용자가 직접 정책과 권한을 관리하는 키이다.

이 과제에서 KMS를 많이 요구하는 이유는 데이터 보호와 권한 분리를 평가하기 위해서이다.

KMS 정책 작성 시 주의점:

- root principal에 모든 권한을 열지 않는다.
- `kms:*` 같은 전체 권한을 남발하지 않는다.
- 서비스별 최소 권한만 부여한다.
- 키 관리자와 키 사용자를 분리한다.

암호화는 크게 저장 중 암호화와 전송 중 암호화로 나뉜다. KMS는 주로 저장 중 암호화에 쓰이며, HTTPS/TLS는 전송 중 암호화에 쓰인다.

## 24. CloudWatch Logs와 Fluent Bit 이론

CloudWatch Logs는 AWS의 로그 저장/조회 서비스이다.

Fluent Bit은 경량 로그 수집기이다. Kubernetes에서는 DaemonSet으로 배포해 각 노드의 컨테이너 로그를 수집한다.

이 문제의 요구사항은 단순 로그 전송이 아니라 다음 조건을 포함한다.

- 애플리케이션 액세스 로그 수집
- `/health` 로그 제외
- JSON 로그 파싱
- CloudWatch Logs로 전송
- Grafana에서 CloudWatch Logs 데이터소스로 조회

`/health` 로그를 제외하는 이유는 헬스 체크가 매우 자주 발생해 실제 사용자 API 로그 분석을 방해할 수 있기 때문이다.

## 25. Prometheus 이론

Prometheus는 시계열 메트릭 수집 시스템이다. Kubernetes 환경에서 Pod, Node, Service의 메트릭을 주기적으로 Scrape한다.

Prometheus의 주요 개념:

- Metric: CPU 사용률, 요청 수, 응답 시간 같은 수치 데이터
- Scrape: 대상에서 메트릭을 주기적으로 가져오는 것
- PromQL: Prometheus Query Language
- Retention: 메트릭 보존 기간

문제에서는 보존 기간을 7일로 설정해야 한다.

## 26. Alertmanager 이론

Alertmanager는 Prometheus Alert Rule에서 발생한 알림을 받아 그룹화, 중복 제거, 라우팅하는 도구이다.

이 문제의 Alert는 운영 장애를 감지하기 위한 기본 지표들이다.

- CPU 과다
- 메모리 과다
- Pod Not Ready
- 에러율 증가
- 응답 지연
- CrashLoop

Alert 조건의 `for: 3m`, `for: 1m` 같은 값은 일시적인 튐과 실제 장애를 구분하기 위해 사용한다.

## 27. Grafana 이론

Grafana는 여러 데이터소스를 연결해 대시보드를 만드는 시각화 도구이다.

이 문제에서는 Grafana에 다음 데이터소스를 연결한다.

- Prometheus: 메트릭
- Alertmanager: 알림 상태
- CloudWatch Logs: 애플리케이션 로그

대시보드 상단 변수로 NodeGroup과 Namespace를 만들면 같은 패널을 여러 범위로 필터링할 수 있다.

색상 임계치를 적용하는 이유는 운영자가 수치를 읽지 않아도 상태를 빠르게 판단할 수 있게 하기 위해서이다.

## 28. 캐싱 이론

캐싱은 Origin에 매번 요청하지 않고 중간 저장소의 응답을 재사용하는 방식이다.

정적 파일은 캐싱에 적합하다.

- `index.html`
- `main.jpeg`
- CSS/JS/Image

API는 캐싱에 주의해야 한다.

- POST는 상태를 변경하므로 캐싱하면 안 된다.
- GET도 실시간성이 중요하면 캐싱하지 않는다.

이 문제에서 S3는 캐싱 활성화, ALB와 Lambda는 캐싱 비활성화가 요구된다.

## 29. 최소 권한 원칙

최소 권한 원칙은 필요한 주체에게 필요한 작업만 허용하는 보안 원칙이다.

이 문제에서 최소 권한이 적용되는 대표 지점:

- EKS Pod Role: DynamoDB 쓰기 권한만
- Lambda Role: DynamoDB 조회 권한만
- EKS Node Role: 노드 운영에 필요한 관리형 정책만
- KMS Key Policy: 서비스별 필요한 암복호화 권한만
- Security Group: 필요한 포트와 소스만 허용

AdministratorAccess, `*:*`, `kms:*`, `dynamodb:*` 같은 넓은 권한은 편하지만 감점 위험이 크다.

## 30. 문제 풀이 핵심 암기표

| 요구사항 | 핵심 답 |
|---|---|
| 리전 | `ap-northeast-2` |
| VPC | `192.168.0.0/16` |
| Public Subnet | hub-sub-a, hub-sub-b |
| Private Subnet | app-sub-a, app-sub-b |
| DB | DynamoDB `wsc2026-book-table`, PK `client_id` |
| 조회 인덱스 | `booking_id` GSI |
| ECR | `wsc2026-book-ecr`, KMS, scan on push |
| EKS | `wsc2026-eks-cluster`, private, control plane logs |
| Addon Node | `wsc2026/node=addon` |
| App Node | `wsc2026/node=application` |
| Namespace | `wsc2026`, `observability` |
| ConfigMap | `book-config` |
| Deployment | `wsc2026-book-deploy` |
| Service | `wsc2026-book-svc` |
| Ingress | `wsc2026-book-ingress` |
| Pod Role | `wsc2026-book-pod-role` |
| ServiceAccount | `wsc2026-book-sa` |
| PDB | `wsc2026-book-pdb` |
| S3 | `wsc2026-static-<영문4>-<비번호>-bucket` |
| Lambda | `wsc2026-book-get-function` |
| ALB | `wsc2026-app-alb` |
| CDN | `wsc2026-cdn` |
| WAF | `wsc2026-waf` |
| Grafana Password | `Skills$#$@!` |

## 31. 채점 관점에서 가장 중요한 포인트

채점자는 보통 "있는가", "이름이 맞는가", "연결됐는가", "동작하는가"를 본다.

가장 중요한 실동작 검증은 다음이다.

1. CloudFront `/` 접속 시 정적 페이지가 보인다.
2. CloudFront `/booking` POST가 EKS 앱까지 도달한다.
3. DynamoDB에 `client_id`, `username`, `email`, `concert_name`, `created_at`, `booking_id`가 저장된다.
4. CloudFront `/v1/book?booking_id=...` GET이 Lambda를 통해 조회된다.
5. EKS Pod가 2개 이상 정상 Running이다.
6. Probe와 PDB가 설정되어 있다.
7. CloudWatch Logs와 Grafana에서 로그/메트릭이 보인다.
8. CloudFront에 WAF가 연결되어 있다.
9. KMS와 IAM 권한이 과도하지 않다.

이 과제는 리소스 개수가 많기 때문에 마지막에 이름, 리전, 태그, IAM 권한, 경로 매핑을 점검하는 시간이 반드시 필요하다.

